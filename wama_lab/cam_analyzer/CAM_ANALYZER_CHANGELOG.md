# Cam Analyzer — Journal des modifications (traçabilité & annulation)

> **Règle** : TOUTE modification qui change le comportement (positionnement, cap, distances,
> tracking, affichage) reçoit une entrée ici — quoi / pourquoi / comment annuler / validation.
> **Annulation** : `git revert <commit>` (chaque entrée = 1 commit atomique), puis
> `cp wama_lab/cam_analyzer/static/cam_analyzer/js/index.js staticfiles/cam_analyzer/js/index.js`
> si du JS est touché, et **restart du process WSL2** si du Python est touché.
> Si une régression de qualité est constatée, remonter ce tableau du plus récent au plus
> ancien pour identifier le changement fautif.
>
> Chaîne complète : [`CAM_ANALYZER_CHAINE_TRAITEMENT.md`](CAM_ANALYZER_CHAINE_TRAITEMENT.md).
> Spécificités **projet** ENA_CASA (données/calibration/rig) : [`projects/ENA_CASA.md`](projects/ENA_CASA.md).

---

# État courant & RESTE À FAIRE

> Absorbe l'ex-`CAM_ANALYZER_TOPDOWN_STATUS.md` (archivé). Photo au **2026-07-21**. Le détail
> historique est dans le journal daté plus bas ; ici = **où on en est + ce qui reste**.

## Cause racine (audit 2026-07-15) — et son traitement

Le basculement `6509cc2` a mis la position en **100 % pinhole** (échelle correcte mais **bruitée
±20 %** via hauteur de bbox) → orientation, fusion 360°, fantômes, stationnés en héritent. Traité
depuis : estimateur de pitch (`homography_estimator.py`, désaccord 14,55→**3,05 m** ×5, k1 écarté),
Kalman+RTS (`trajectory_smoother.py` → `world_en`), ground calib 2a (⚑ OFF), recalage ortho 2b
(rapport seul). **La position sous-jacente était la cause — pas 360°/fantômes/orientation.**

## FAIT (synthèse par thème — détail = journal daté)

Position/échelle (pinhole→pitch ×5, Kalman+RTS, ground calib 2a flag OFF) · Tracking 360° + fusion
(verrou de chaîne gid, enchaînement fin de Live, trajectoires fusionnées) · Fantômes/reconstitution
(comblement ≤6 s, héritage mesures, stationnés ancrés position monde) · Filtres reflets/artefacts ·
Cap/orientation (consensus axial, fondu ratio↔trajectoire) · Marquages monde SAM3 (⚑) · Intersections
/branches apprises · Ortho/recalage IGN + mini-carte orientable · Live/complétion + préemption ·
Levier antenne GPS · UI toolbar par familles + bascules ⚑ génériques.

## ⭐ ÉTAT 2026-09-05/07 — l'inventaire, la pose navette, et l'ordre de travail acté

> **La carte vivante des traitements est `CAM_ANALYZER_CHAINE_TRAITEMENT.md §INVENTAIRE`** (13 passes,
> 43 leviers de correction, 3 réfutations, cadre de fusion, 6 modèles IA — tout MESURÉ dans le code le
> 05/09). Ce qui suit est l'ordre de travail acté avec Fabien ce jour-là et son état.

| étape | contenu | état |
|---|---|---|
| inventaire | §INVENTAIRE + 3 réfutations (IMU jamais consommé · voie DLT conditionnée · hypothèse EMA repli ③) | ✅ `478e0151` `aa44318b` |
| ① pose navette | ⚑ `shuttle_filter` (OFF) — Kalman+RTS (`kinematics.rts_smoother`, commun), `effective_gps_track` = point d'accès unique, 6 consommateurs, miroir JS | ✅ `05ea8776` |
| ②③④ visibilité | ⚑ `sam3_homography` (ON) · `placement_source` par détection (G7 se compte) · ⚑ `display_ema` (ON, live) | ✅ `0204021e` |
| ⑤a manifestes | kind `function` exporté (58/58), chaque entrée du registre attestée valide | ✅ `e1c2cd6a` |
| **⑤b facette estimateur** | `estimates` / `uncertainty` / `derived_from` (+ `estimate_field`) sur les ports de sortie — le socle de la fusion (§INVENTAIRE E) ; rôle `group` des entrées (marche C) ; 1ᵉʳ consommateur `fusion.fuse_estimates` (1/σ², refus si `derived_from` partagé) ; 9 producteurs relevés depuis §C | ✅ **GO Fabien 09/09, LIVRÉ le jour même** (entrée du 2026-09-09) — σ chiffrées : cap filtré 3° (synthétique, provisoire), pinhole ±20 % ; les 7 autres `declared` (se confrontent, ne pèsent pas) |
| **⑥ test D.3** | ⚑ `display_ema` OFF sur session ENA (dérive des garés cesse-t-elle ?) puis ⚑ `shuttle_filter` ON → lire `Filtre navette` / `Source de placement` / `Cohérence placement`, `placement_spread` OFF vs ON | 🟡 **VOLET OBJECTIF JOUÉ le 09/09 sur P97 réelle** (1,2 M détections, 4352 gids) — chiffres et méthode : `CHAINE §D.3 ⭐`. « Tous en ③ » est FAUX (90 % ont un `world_en`). ⚠ **Ma 1ʳᵉ conclusion (« 70 % écartés parce que vus < 4 s ») était FAUSSE et est RECTIFIÉE dans le même §** : sélection circulaire (candidats choisis par un étalement calculé sur du LISSÉ, or un track court a mécaniquement peu d'étalement). Mesure refaite sur les grandeurs non positionnelles : **2114 véhicules recevables (54,4 %) → 55 ancrés**, donc **97,4 % des recevables sont éliminés par les critères de POSITION**, pas par la durée ; lequel exactement est indéterminable depuis le persisté. **Verdict Fabien, plus large et fondé : le filtre des garés n'a JAMAIS fonctionné** (navette à l'arrêt 77 % du temps) → 🔴 **CHANTIER**. ⭐ **MESURE DÉFINITIVE le même jour, par le CODE lui-même** (2ᵉ recadrage de Fabien : « regarde ce que fait le code plutôt que supposer ») — filtre INSTRUMENTÉ (`stationary_rejects`) puis `annotate_global_tracks` EXÉCUTÉ sur la session réelle, écritures neutralisées (237 318 bloquées, base intacte, 94 s) : **`trop_etale` 45,4 % · `vu_moins_de_4s` 38,4 % · `moins_de_5_obs` 13,2 % · RETENUS 1,8 % · `trop_rapide` 0,8 % · intersection 0,3 %**. La cause dominante est l'ÉTALEMENT : *le filtre exige une précision de placement que la chaîne ne fournit pas* (pinhole ±20 % ⇒ plusieurs mètres à 20 m). Mes deux analyses externes étaient fausses TOUTES LES DEUX. Aussi mesuré : `placement_spread` = **0,85 m**, et G7 enfin compté — **46 % pinhole, 10 % repli dégradé** sous ce qu'on lit « projection sol ». ⏳ Restent les bascules (recalcul + œil sur la carte) |
| accéléromètre | axes X/Y du capteur **non mesurés** (seul Z ≈ 0,95 g) → identifier l'axe avant par corrélation avec dv/dt du GPS filtré AVANT tout modèle à accélération commandée | ⏳ mesure |
| σ du filtre | 0,8 m/s² / 2,0 m **provisoires** → réétalonner sur `placement_spread` | ⏳ |
| #7 bâtiments IGN | `fetch_buildings` n'a qu'un consommateur (`sky_mask`) : rien n'affiche les emprises — palier JS à part | ⏳ |
| `locate_anything` | poids présents, `capabilities={}`, `backend_ref` vide : la capacité n'existe pas ; PoC bloqué par les crashs GPU | ⏳ |
| **boutons de lancement** (question Fabien 07/09) | **inventaire mesuré : 11 boutons + 13 ▶ par passe, pour 4 intentions** — 1 doublon strict (« SAM3 seul » ≡ ▶ `sam3_markings`), 2 « tout relancer » (« Analyser » = `start_analysis`, qui **efface** toutes les `DetectionFrame` sans confirmation ; « Tout relancer (force) »), **3 « Compléter »** pour 2 notions distinctes (passes manquantes/périmées vs **tranches de temps** non couvertes), 4 lancements hors pipeline (calibration : Calib. SAM3, Test SAM3, recalage/correction ortho) + l'extraction. ⚠ `run_passes` lance les passes de CALCUL **en parallèle** (`.delay()` par passe) alors que `_DEPENDS_ON` les ordonne — latent. Proposition : ▶ par **ligne d'étage** (📷 Analyse / 🧮 Calculs) avec gating dérivé de `_DEPENDS_ON` (Calculs grisé tant qu'aucune analyse n'est `completed`) et **chaîne Celery en ordre topologique** ; retrait des deux doublons de tête ; un verbe par notion (« Compléter les passes » / « Étendre la couverture »). Artefact « Réagencement du volet droit » (05/08) = **v1 partiellement livrée** : Q1 (`indicators` passe) ✅, Q2 (étages) ✅, Q3 (⚑ dé-flotté/scindé) ✅ ; **Q4** (lecture chiffrée) et **3b** (toggles « Vue » hors registre) restent ouverts | ✅ **07/09 (GO Fabien) : ▶ par étage + gating + chaîne topologique + registre `PASSES` + doublons retirés + un verbe par notion** (entrée du 2026-09-07). Restent Q4, 3b |
| **pipeline schéma-driven** (question Fabien 07/09) | **état mesuré : 0 %.** Le kind `pipeline` du manifeste = graphe **Studio** (`StudioPipeline.graph`, nœuds `app`/`source`/`sink`), jamais exporté (pas de dossier `manifests/pipelines/`). Le pipeline cam_analyzer est déclaré **5 fois en code** : `PassType.choices`, `pass_tracking._WATCHED` / `_STAGE` / `_DEPENDS_ON` / `_PER_CAMERA_PASSES`, `views.dispatch_map`. Le panneau se rend déjà DEPUIS `get_passes_status` (label, `stage`, statut) — ce sont les **boutons** qui sont du HTML dans le JS. Le pont existe : **20 passes sont déjà des `FunctionSpec` app-bound** (`cam_analyzer.<key>`, `impl` = la tâche). ⚠ **La cible n'est PAS à proposer, elle est DÉCIDÉE** : `WAMA_DATA_WORLD §9undecies.2` — **D13 tranchée le 2026-08-24** (« un seul kind `pipeline`, étendu d'un nœud `function` ; la différence app/fonction se traite dans l'EXÉCUTEUR, qui dispatche sur `kind` ») + plan de convergence §9 (items **C** : `group` sur `PortSpec` + accesseur `function_node_ports()` de MÊME forme que `studio_node_ports` ; **D** : le nœud `function` dans le validateur + le dispatch de `run_pipeline_task`). Le Studio ne connaît aujourd'hui **aucun** `FunctionSpec` (0 référence dans `wama/studio/`). Ordre d'exécution pour cam_analyzer : ① registre unique `PASSES` (patron `features.FEATURES`) dont dérivent les 5 déclarations ; ② boutons/gating rendus depuis ce registre ; ③ D13 items C+D dans le commun (Studio), puis le registre s'exporte en manifeste `pipeline` à nœuds `function` — **une représentation, deux éditeurs** (§9undecies) | ✅ **①② le 07/09 ; C + D + ③ le 09/09 (GO Fabien « C + D, Studio inclus »)** — le kind accepte le nœud `function`, l'exécuteur du Studio dispatche sur le kind (pure = synchrone en process, app-bound = `impl` pollée), `manifests/pipelines/cam_analyzer.json` = 13 nœuds `function` + 14 liens. Restent : charger ce manifeste DANS le canvas (le Studio lit `StudioPipeline`, pas le corpus), exécuter une passe app-bound depuis le Studio contre un worker réel, la porte d'ingestion Data (marche E). Première rédaction de cette ligne (07/09 16h) la présentait à tort comme une proposition |

**Rien de tout cela n'a tourné sur données réelles ni au navigateur** (GPU interdit sur le poste ;
le smoke du 05/09 atteste seulement : page cam_analyzer HTTP 200, 0 erreur JS, JS servi = le bon).

## RESTE À FAIRE (priorisé — qualité/cohérence vue de dessus)

**P1 — le cœur (position) :**
1. **Métrique A/B objective** (préalable/instrument) : ✅ **#1 CÂBLÉE (2026-08-05)** — étalement
   monde RMS des stationnés (`placement_spread`, brique commune **pure** `geometry`), persistée dans
   `results_summary['placement_spread']` + **console avec l'état ⚑ `auto_ground_calib`** (relever
   OFF puis ON). ⏳ **Reste** : exposer `distance_source` par détection (split par source — réserve
   **G7**) ; métriques **#2** (désaccord sol⟷pinhole⟷ortho) et **#3** (discontinuité au hand-off) ;
   lecteur UI/rapport dédié ; **validation navigateur** de l'apparition console/`results_summary`.
   C'est ce qui permet de **valider** P1.2/P1.3 sans « à l'œil » — et c'est une **brique commune** réutilisable.
2. **Terminer 2a (pitch)** : corriger le placement mixte **G7**, puis basculer `auto_ground_calib`
   ON sur une session ENA, **mesurer** l'A/B, décider du défaut.
3. **Appliquer l'offset ortho 2b** (~5,1 m mesuré) derrière un **nouveau flag** (aujourd'hui rapport seul).
4. **Calibration jointe 2c** (pitch + échelle réconciliés avec l'ortho).
5. **Passe de validation NAVIGATEUR systématique** : la majorité des commits 07-17→07-20 sont
   **compile/parse-only** (« navigateur à valider »). Rejouer une session ENA (ex. `4da52df3`),
   relancer « Calculer les indicateurs » (recale les `world_en` avec l'antenne), vérifier à l'écran.

**P2 — compléments :** voie/vidéo ancré yolopv2 détectés (audit #3) · FOV réel 55↔97° à confirmer
(audit #4) · vitesse/distance unifiées par track (`track_speed_unified` = **stub, 0 consommateur**) ·
mask_geometry / YOLO-OBB (non implémenté) · dépassement depuis trajectoires monde par `global_track_id`.

## Gaps vérifiés dans le code (2026-07-21 — audit de reprise)

| # | Constat (fichier) | Gravité | Statut |
|---|---|---|---|
| **G7** | `auto_ground_calib` ON → `ground_ego` retombe **silencieusement** sur pinhole hors portée (`multicam_tracker.py:204`) → **placement MIXTE** sans indication → **fausse tout A/B** du flag. Corriger avant/avec la métrique (clé = `distance_source`). | Haute | vérifié |
| **G8** | `_run_global_tracking` avale les échecs de `learn_branches`/`aggregate_markings` (`except non-blocking`) → clés `results_summary` **périmées en silence**. | Moyenne | rapporté |
| **G2** | Bloc SAM3 inline **mort** dans `process_session_task` + `_use_sam3_fallback` **jamais défini** (`tasks.py:1336`) = `NameError` latent, masqué car le garde est toujours faux (SAM3 tourne via la tâche enchaînée). | Basse | vérifié (mort par design) |
| **G4** | Marque de traçabilité au FOV **codé en dur** `{front:60,rear:60,left:90,right:90}` (`tasks.py:1562`) divergent du `DEFAULT_FOV_V_DEG` réellement appliqué → la trace peut mentir. | Basse | rapporté |
| ~~G3~~ | *(affirmé par l'audit : `fov_v_used` non écrit par la passe inline)* — **RÉFUTÉ** : `tasks.py:1268` l'écrit bien par caméra. Fausse alerte conservée pour mémoire. | — | réfuté |

## Non-régression (OBLIGATOIRE à chaque modif) — voir procédure détaillée en fin de doc

CHANGELOG + 1 commit atomique · `py_compile` + `manage.py check` · **parse esprima** du JS · **sync**
`staticfiles/` si JS · migrations sur **les DEUX bases** (Windows + WSL2) · **restart WSL2** si Python ·
pas de tests destructifs (user id=1 = Fabien réel, `transaction.atomic()`) · pas de `cd` en préfixe ·
**une amélioration comparable = un flag ⚑** (`utils/features.py`), jamais de `if` ad hoc.

---

## 2026-09-11

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(ce commit — GARDES)* | **Les 3 trous que le balayage mécanique a trouvés dans le commit précédent.** Question de Fabien : *« as-tu bien tout consigné, ajouté les tests nécessaires ? »* — la réponse mesurée était **non**. Chaque symbole ajouté confronté à `grep -rl <symbole> --include="tests*.py"` : (1) 🔴 **`compute_shuttle_filter` n'était nommée par AUCUN test** — donc rien ne gardait que ⚑ `imu_command` ON déclenche réellement la commande. **Toutes mes gardes vivaient DANS la brique pure**, aucune à la COUTURE applicative, là où la bascule rencontre la fonction → 4 tests (`LaBasculeCOMMANDE_vraiment_le_filtreTest`) : OFF ⇒ pas commandé, ON ⇒ commandé + le rapport le dit, ON **sans IMU** ⇒ repli sur le filtre libre sans planter, et le σ vient de la CONSTANTE (pas d'un littéral `0.25` qui casserait muettement). (2) `kalman_rts_cv(command=)` n'était testé qu'à travers `filter_gps_points` → 3 tests directs sur la brique commune, dont **« sans commande, résultat INCHANGÉ »** — c'est elle qui sert aussi le tracking des OBJETS. (3) `DEFAULT_SIGMA_A_COMMANDED` / `sigma_a_commanded` n'étaient nommés nulle part. | *Un livrable gardé à 90 % n'est pas gardé : le défaut restant est précisément celui qu'on ne voit pas.* Le balayage du rituel (§2a bis) trouve ce que la relecture ne trouve pas, parce qu'il est MÉCANIQUE — je « savais » avoir testé la commande, et c'était vrai d'un seul côté de la couture. | `tests_ego_pose` **19 `OK`** (12 avant). Aucun code de production modifié : ces tests décrivent l'existant, ils ne le corrigent pas — *et aucun n'a échoué en naissant, ce qui est le seul cas où un test ajouté après coup n'apprend rien de neuf sur le code, seulement sur sa couverture.* |
| *(ce commit)* | **⚑ `imu_command` : l'accéléromètre entre en COMMANDE du filtre navette.** (1) `kinematics.rts_smoother.kalman_rts_cv` accepte `command=callable(t)->(ux,uy)` en m/s² : la commande entre dans la PRÉDICTION (`x_pred = F·x + B·u`), évaluée au MILIEU de l'intervalle propagé ; **le passage arrière RTS est inchangé parce que sa récursion lit `x_pred`**, qui la porte déjà. (2) `driving.ego_trajectory_filter.filter_gps_points` accepte `accel_long` (accélération LONGITUDINALE) + `sigma_a_commanded` (**0,25**, du résidu mesuré 0,202) : **DEUX PASSES**, la 1ʳᵉ non commandée donne le cap qui projette l'accélération en ENU — un accéléromètre mesure dans le repère du VÉHICULE, le filtre dans celui du TERRAIN, et rien d'autre ne donne la rotation. Là où le cap est tenu, c'est le dernier connu qui sert (hypothèse explicite et bornée : à ce régime la navette démarre dans l'axe de sa voie). Le rapport gagne `commanded`, `sigma_a_libre`, `command_shift_median_m`/`_p95_m`, `command_speed_delta_median_kmh`. (3) `ego_pose.longitudinal_accel_series` : série lissée ~0,5 s (bande passante du pas de propagation, pas la vibration à 10 Hz) et **biais réestimé À L'ARRÊT** (−0,215 m/s² sur P97) — un capteur à ~1° d'assiette lit g·sin(assiette) en permanence, et c'est à l'arrêt qu'on le voit pur ; repli annoncé si les arrêts manquent. (4) `IMU_FORWARD_AXIS='ax'` / `IMU_FORWARD_SIGN=+1` déclarés **à un seul endroit**, avec la mesure en commentaire — ⚠ ils valent pour CE rig : un autre montage les invalide, et ils passeront alors par le profil. (5) ⚑ `imu_command`, **défaut OFF**, scope `compute` → **18 bascules** (17 avant). Aucune UI à écrire : le panneau se génère du registre. | Réponse à Fabien (*« on ne peut pas corriger les positions GPS avec l'accélération x ? »*) : oui, **en commande du filtre existant**, pas en source de position (le biais donne 72 m de dérive à 30 s). Le levier 15 portait depuis le 05/09 la mention « pas d'accéléro en commande (**axes non mesurés**) », et le module nommait l'étape dans son propre docstring : c'était le seul verrou, la mesure du 10/09 l'a levé. | **A/B SUR DONNÉES RÉELLES** (P97, lecture seule — `compute_shuttle_filter` non appelée), arbitré par la vitesse **Doppler** `$GPVTG` qu'aucun des deux filtres ne consomme : libre `σa=0,8` **0,232 m/s** · **témoin `σa=0,25` sans commande 0,261** (PIRE) · **commandée 0,223**. ⭐ **Le témoin a renversé la lecture** : le 1ᵉʳ A/B changeait DEUX choses à la fois (commande ET σa) et affichait « +4 % », juste mais inattribuable. Baisser σa SEUL dégrade → le gain **propre au capteur**, à σa identique, est **+14,8 %** (12,6 % en mouvement) et n'est **pas récupérable par un réglage** ; le gain NET sur la config actuelle reste **+4,0 %**. ⚠ **Le banc synthétique annonçait +50 %** : il nourrit le filtre avec l'accélération VRAIE, là où `ax` réel ne partage que r²=0,32 avec elle — *un test de gain synthétique mesure le MÉCANISME, jamais le capteur*. Tests : **5** `AccelerationCommandeeTest` (gain en vitesse ; position non dégradée ; **la commande est ARRIVÉE** — garde contre le câblage muet, un écart nul passerait inaperçu ; `accel_long=None` **strictement identique** à l'existant, rapport compris ; **la commande est TOURNÉE PAR LE CAP** — deux caps opposés doivent pousser en sens opposés, sans quoi le filtre resterait plausible et faux) + **5** `CommandeAccelerometreTest` (axe déclaré = axe mesuré ; biais estimé à l'arrêt et non en moyenne globale ; biais retiré rend l'accélération réelle ; absence d'IMU annoncée ; repli annoncé). Périmètre `wama_data` + `cam_analyzer` + `studio` + catalogues : **887 `OK`** avant les 5 derniers, **892** après. `manage.py check` 0 issue. ⚠ **Aucun recalcul en base, aucune validation navigateur** (la bascule n'a d'effet qu'au recalcul). Annulation : `git revert` — `command=None` et `accel_long=None` rendent EXACTEMENT le comportement d'avant (gardé par un test), la bascule disparaît du registre avec le panneau. |

## 2026-09-10

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(2ᵉ commit — MESURE seule)* | **Le cap par `ax`+`ay` : la relation tient à l'échelle 1, l'intégration perd quand même.** Question de Fabien : *« en combinant l'accélération x et y, ne peut-on pas estimer le cap ? »* — ma phrase « un accéléromètre n'observe pas le lacet » était **trop catégorique**. Mesuré : `ay·g = c × (v·ω)` avec **c = −1,004** (r = −0,45, décalage +0,4 s, biais −0,078 m/s² où se loge le dévers) — la relation non-holonome tient **à l'échelle 1** sur données réelles. **Mais** sur les **61 segments** où le filtre tient le cap faute de déplacement, **tenir** erre de **4,8°** en médiane contre **65,5°** en intégrant `ω = ay/v` (tenir gagne 54 fois sur 61 ; 30° contre 49° même sur les 9 segments qui tournent de plus de 20°). Cause **structurelle** : l'estimateur divise par `v`, et le cap n'est mauvais QUE lorsque `v` est petit — à 0,5 m/s un virage à 10°/s ne fait que **0,087 m/s²**, soit **3× sous le plancher de bruit** de l'axe (0,27 m/s²). `ay` est informatif dans la bande **2-3 m/s** (r = 0,65). Conclusion identique à `ax` : **mesure dans un filtre, pas intégrateur autonome** ; tenir le cap à l'arrêt demande un **gyroscope**, absent des canaux. Consigné en `CHAINE §D.4 ⑥`. | Une question qui se re-pose si le résultat NÉGATIF n'est pas écrit : l'intuition est juste (l'échelle le prouve), c'est le domaine d'emploi qui ne l'est pas. | ⚠ **Ma 1ʳᵉ version de cette mesure était FAUSSE et a été jetée** : l'échelle y était ajustée sur `v > 3 m/s` — or cette navette plafonne à 5,1 m/s et y roule DROIT (σ(ω) 1,1°/s contre 3,6°/s à 2-3 m/s). J'ajustais le modèle là où son signal est ABSENT : il rendait `c ∈ [0,3 ; 24,9]` au lieu de −1,00, et Q2/Q3 en héritaient sans que rien n'ait l'air cassé. Le signal d'alarme a été un `r = +0,11` là où la mesure du matin donnait −0,44 — *deux mesures du même objet qui divergent accusent la sonde avant l'objet*. Lecture seule : aucune écriture, aucun code modifié. |
| *(1ᵉʳ commit — MESURE seule, aucun code modifié)* | **L'accéléromètre : axes identifiés, décalage démenti, pente écartée, résidu chiffré.** Chantier ③ de la file. (1) **Axes** : `ax` = **avant** (`+ax` = accélération), `ay` = latéral (négatif en virage à droite), `az` = vertical (+0,938 g) — par **trois discriminants indépendants** (gravité / corrélation à dv/dt / corrélation à v·ω), chacun réclamant un axe DIFFÉRENT, stables sur les 4 quarts de la session. (2) **Synchro** : les `sample_ts` des CSV **sont** les `@ts` du `.rec` (écart **0,0000 s**, 6 canaux) — l'import préserve la base commune ; `gps_time_offset = 0,7239` est la synchro GPS↔**vidéo** (`video_ts[0] − gps_ts[0]`), sans rapport. (3) **Un décalage de +0,6 s que j'avais annoncé RÉEL était le mien** : contre la vitesse **Doppler** `$GPVTG` (indépendante de ma différence de positions et de mon Kalman), r monte de 0,41 à **0,57** et le décalage passe à **−0,3 s**. (4) **Pente écartée** : l'altitude `$GPGGA` donne une pente de 4,5 % médiane / 18 % au p95 sur 50 m (= bruit vertical GPS) ; la régression ne gagne que 0,317 → 0,356 de r² avec un coefficient de **−0,055** au lieu de ~1 → **garder l'altitude au parsing n'aurait rien apporté** (piste que j'avais proposée le matin même). (5) **Résidu** : σ(dv/dt − k·ax·g − b) = **0,202 m/s²** contre `DEFAULT_SIGMA_A = 0,8` → **4,0×**. Échelle **encadrée 0,333 … 1,037** (erreurs sur la variable explicative) : **compatible avec 1** — mes deux annonces antérieures (`k ≈ 0,16` puis `0,30`) lisaient une atténuation de régression comme un défaut du capteur. (6) **Effet de bord mesuré sur ⚑ `shuttle_filter`** : le Kalman `σa = 0,8` **atténue l'accélération de ~35 %** (0,163 contre 0,246 m/s² au Doppler) — 1ʳᵉ mesure objective pour le chantier « réétalonner σa/σm ». Consigné dans `CAM_ANALYZER_CHAINE_TRAITEMENT §D.4` (+ levier 15 et ligne 16 du §C mis à jour). | Chantier ③ de la file du `§CLÔTURE 2026-09-09 (soir)` : *« identifier l'axe avant par corrélation avec dv/dt du GPS filtré — une MESURE, les axes X/Y ne sont écrits nulle part »*. Question de Fabien en cours de route (*« il y a bien x, y et z, de quoi parles-tu ? »*) : les 3 canaux EXISTENT ; ce qui manquait est leur correspondance au repère du véhicule, que rien ne déclare — `parse_accel` renomme des suffixes de fichiers, sans sémantique. Puis (*« on ne peut pas corriger les positions GPS avec l'accélération x ? »*) : oui, **en commande du filtre existant**, pas en source de position — le biais de −0,16 m/s² donne 8 m de dérive à 10 s, 72 m à 30 s. Le levier 15 disait « pas d'accéléro en commande (**axes non mesurés**) » : c'était le seul verrou, il est levé. | **Aucune écriture** : base intacte (lecture seule de `AnalysisSession`), `.rec` et CSV ouverts en lecture, aucun fichier de code modifié — donc **rien à annuler** ; les deux `.md` se révoquent par `git revert`. Mesures rejouables : scripts dans le scratchpad de session (jetables, non versionnés) ; sources = session P97 `4da52df3` + `media/cam_analyzer/1/input/ENA_CASA/20220404_124000_RecFile_Data/`. ⚠ **Rien n'a été validé au navigateur** (aucune surface n'a bougé). ⚠ Le σ pour la facette estimateur n'est PAS établi : 0,202 m/s² est un résidu contre un Doppler lui-même bruité, donc une **borne haute** ; et l'excès de variance ×1,7 de `ax` reste inexpliqué (le gyroscope manque aux canaux enregistrés). |

## 2026-09-09

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(ce commit, 2ᵉ palier — D13)* | **D13 codée : le pipeline cam_analyzer EST un manifeste `pipeline` à nœuds `function`, et le Studio sait exécuter une fonction.** (1) Kind `pipeline` (commun) : `NODE_KINDS` += `function` ; `node_kind()`/`function_key()` = les deux SEULS lecteurs de la convention de palette `app='function:<clé>'` ; `graph_to_body()` extrait (kind `function` + `function`, `app=None`) ; `register_pipeline_source(key, fabrique)` — les pipelines déclarés EN CODE ont leur registre, keyé, que le kind ne connaît pas (même leçon que `load_all`). (2) Exécuteur `studio/tasks.py` : dispatch SUR LE KIND — `pure` → `spec.fn(1ᵉʳ port, ports suivants par NOM de port, **params typés par ParamSpec)` synchrone, `TypedFrame` en mémoire entre nœuds, **facette estimateur du port posée sur `meta['estimate']`** (c'est ce que `fuse_estimates` lit) ; `app`-bound → `impl` (tâche Celery) lancée avec les params du nœud, arguments REQUIS par introspection de la signature (`session_id`…), pollée comme un job ; nœud-source **`dataset_input`** (CSV → `TypedFrame` du type DIT par l'utilisateur, jamais deviné) ; la Sortie range un `TypedFrame` en CSV (`document`). (3) `launch_graph` accepte un nœud fonction (clé au catalogue) ; `api_nodes` sert les fonctions par `function_node_ports` (même forme que les apps) + `data_types` ; `api_run_options` sert leurs params ; JS : groupe « Fonctions (N) » dans la palette, `dataset_input` construit depuis la taxonomie SERVIE, `to_port` = ID de port (deux ports d'une fonction partagent souvent le rôle `travail`), relecture des graphes anciens par rôle. (4) **③ cam_analyzer** : `Pass.function` (clé du catalogue quand elle diffère de `cam_analyzer.<key>` : `depth` → `depth_analysis`), `pipeline_graph()` (forme canvas) et `pipeline_manifest()` inscrits sous la clé `cam_analyzer` ; **3 specs déclarées** pour que CHAQUE passe soit un nœud (`extraction` — avec la facette du cap brut, levier 10, `declared` —, `intersection_windows`, `depth_calc`) et **`prediction` → `indicators`** (un nom pour la passe, la tâche et la fonction) ; `manifest_export --kind pipeline` → `manifests/pipelines/cam_analyzer.json` (13 nœuds, 14 liens), inclus dans l'export complet et le `--check`. | GO Fabien 09/09 « C + D, Studio inclus ». D13 (`WAMA_DATA_WORLD §9undecies.2`, 24/08) : un protocole réel TRAVERSE les mondes ; la différence app/fonction se traite dans l'exécuteur, jamais dans le schéma. Sans (4), le registre `PASSES` du 07/09 restait « ce qui s'exportera quand le kind l'acceptera » — trois passes n'avaient pas de `FunctionSpec`, donc pas de nœud possible ; les déclarer EST le geste (skill : « une sortie non déclarée n'existe pas pour le système »). `dataset_input` n'est pas un contournement de la marche B (nature `dataset` dans la taxonomie média) : c'est la card d'entrée de `FUNCTION_CARDS §5`, le type est déclaré, pas deviné. | **9 tests `studio/tests_function_nodes`** : validateur (kind + clé exigée), traduction canvas→manifeste, lancement (fonction seule lançable ; inconnue refusée avant dispatch), **chaîne `dataset_input` → `ego_track_filter` → `fuse_estimates` → Sortie exécutée EN PROCESS** (cap fusionné 37° ± 1, σ = la 3° déclarée sur le port, CSV rangé en médiathèque), amont non typé refusé avec un message qui nomme la porte, `app`-bound sans `session_id` refusé AVANT toute file, palette (forme, super-types, taxonomie), params (ParamSpec + introspection) · **3 `tests_pass_registry`** (chaque passe est une fonction du catalogue ; le manifeste s'extrait et est VALIDE — 13 nœuds `function`, liens = `depends_on`, layout vide ; forme canvas chargeable) · 52 tests du périmètre `OK` · `check_js` 70/0, `staticfiles` synchro · `manifest_export --check` complet à jour (corpus : functions **62**, pipelines **1**) · `doc_facts --only wama_data` régénéré (`modules.py` : l'Analyzer n'est plus bloqué par D13 mais par sa SURFACE). **⚑ SMOKE NAVIGATEUR FAIT le 09/09 (serveur relancé par Fabien) — le trou ① est COMBLÉ pour le canvas** : palette « Fonctions (62) » rendue à côté des 10 apps ; nœud fonction POSÉ ; inspecteur correct (badge `fonction · pure`, description, les 3 `ParamSpec` en champs d'exécution) ; port servi par son **ID** (`data-port=track`, `types=geo_track`) ; **lien tracé** depuis « Jeu de données » (typage par intersection : cible marquée `compatible`) ; **lien Texte → fonction REFUSÉ** (`prompt` ∌ `geo_track`) ; **0 erreur JS**. ⚠ Deux enseignements : **(a) la sonde était suspecte avant le code** — `.studio-node` `.last` est un locator DYNAMIQUE, mes deux clics tombaient sur le MÊME nœud et le canvas refusait à raison (« pas de boucle sur soi ») ; figer par index a suffi, et sans ce réflexe je concluais à un défaut de connexion ; **(b) un défaut RÉEL que seul le navigateur montrait** : les ports d'une fonction s'affichaient avec leur clé technique et la `description` DÉCLARÉE de chaque `PortSpec` n'était rendue nulle part — elle est désormais l'infobulle du port (`track — geo_track` + la phrase), le libellé restant la CLÉ parce que c'est elle que `to_port` sérialise. **Reste non fait au navigateur** : EXÉCUTER un pipeline de fonctions depuis l'UI (bouton ▶ Exécuter). **Ce qui N'EST PAS couvert, nommément** (relevé le 09/09 en repassant sur la couverture) : ① le **lancement** depuis l'UI ; ② le **JS lui-même** (`installDatasetSource`, groupe « Fonctions », repli `data-port`→`data-group` au chargement d'un graphe ancien) — aucun harnais JS n'existe dans le dépôt, `check_js` ne lit que la syntaxe : seul un smoke le prouverait ; ③ une fonction `app`-bound **réellement exécutée** contre un worker Celery (le test s'arrête au refus AVANT dispatch, ce qui est le chemin qu'on voulait garder) ; ④ `fuse_estimates` sur **données réelles**, faute d'une 2ᵉ source de cap chiffrée ; ⑤ le manifeste `pipeline` **rechargé dans le canvas** (l'import n'existe pas — marche E). ⚠ **4 tests ajoutés APRÈS coup ont trouvé un vrai défaut** : un lien dont le `to_port` ne correspond à aucun port de la fonction (graphe sauvegardé avant ce jour, où `to_port` portait le RÔLE ; ports intégrés du JS, qui n'ont pas d'id) rangeait la donnée sous une clé jamais lue — le port requis ressortait « non alimenté », un message qui accuse le graphe alors que c'est le nom du lien qui a vieilli. L'exécuteur retombe désormais sur le premier port, comme le JS le fait déjà au chargement. Annulation : `git revert` — le kind retombe sur `source\|sink\|app`, l'exécuteur ignore le nœud, le corpus `pipelines/` se supprime ; `prediction`→`indicators` est un renommage de clé de catalogue (corpus régénéré, aucun consommateur par chaîne : vérifié au grep). |
| *(ce commit, 1ᵉʳ palier — ⑤b + C)* | **⑤b + marche C : un port DIT ce qu'il estime, ce qu'il vaut, d'où il vient — et quel rôle il joue.** (1) `PortSpec` (commun, `function_catalog.py`) gagne **`group`** (entrée : `travail`\|`reference`, vocabulaire EMPRUNTÉ à `app_modes.INPUT_TYPES[*]['port']`) et la **facette estimateur** (sortie, optionnelle) : `estimates` (vocabulaire fermé `ESTIMATED_QUANTITIES` → unité de σ), `uncertainty` (nombre \| `{field}` \| `{model: relative\|held\|declared}`), `derived_from` (⊆ `NATIVE_SOURCES` : gps, bbox, image, segmentation, depth_map, imu, orthophoto, road_map), `estimate_field`. `validate_port_facet` = la MÊME règle au catalogue (tests) et au kind manifeste `function` (ingest). `port_estimate_meta(port)` = la forme que porte `TypedFrame.meta['estimate']` à l'exécution. (2) **`function_node_ports(key)`** rend la MÊME forme que `studio_node_ports` (`{inputs:[{id,label,group,types,multi}], output}` + `outputs`), types de sortie = type ∧ super-types (le canvas apparie par intersection). (3) **1ᵉʳ consommateur** : `wama_data/functions/fusion/estimates.py` — `fuse_series` (noyau pur) / `fuse_estimates` (wrapper, `FunctionSpec` JOIN) : alignement temporel à tolérance, σ ligne à ligne, moyenne pondérée 1/σ² **vectorielle pour un cap**, σ fusionnée 1/√Σ1/σᵢ² ; **REFUSE** (ValueError nommant la donnée native) deux sources dont `derived_from` se recouvrent ; **ÉCARTE** ce qui n'est pas chiffré (`declared`) ou tenu (`held`). (4) **9 producteurs déclarés depuis §INVENTAIRE C** : `ego_track_filter`/`shuttle_filter` (cap, gps, `held` σ 3°), `ego_rotation` (yaw, image, declared), `distance` (pinhole ±20 %, bbox), `depth_analysis` (distance, depth_map), `ground_calib` (bbox+gps) / `depth_ground_plane` (depth_map+segmentation) — le même angle par deux données natives, donc fusionnables —, `global_tracking` (position, bbox+gps), `ortho_recalage` (offset, orthophoto+segmentation), `indicators` (ttc). Rôle `reference` sur 8 entrées de fait (`track` à côté des détections, `road_map`). Corpus `manifests/functions/` régénéré : **59** (52 réécrits pour `group`, +`fuse_estimates`). | GO Fabien 09/09 sur la forme soumise le 05/09 (deux décisions : ⑤b « les 3 champs » ; D13 « C + D, Studio inclus »). §INVENTAIRE E : sans σ, fusionner = poser des poids arbitraires ; le critère fusion/confrontation est l'**indépendance** — leviers 1 et 40 viennent de la même bbox, ils se confrontent. La facette généralise `distance_source` (une provenance par VALEUR) sur les sorties existantes, **pas de 9ᵉ registre**. C et ⑤b modèlent tous deux `PortSpec` : décidés et livrés ensemble. Les σ `declared` ne sont pas un aveu de paresse mais l'état MESURÉ : aucune mesure A/B n'existe pour ces leviers (§C, colonne « mesure ») — le jour où elle existe, elle s'écrit sur le port et la fusion la prend d'elle-même. | Tests : **11** fusion (pondération 1/σ², circulaire 350°+10°=0°, tenu écarté et compté, tolérance temporelle, source seule = cas dégénéré, **refus corrélé** nommant `bbox`, `declared` ne pèse pas, wrapper TypedFrame + facette héritée) · **6** nouveaux au `FunctionCatalogConformiteTest` (toute facette valide, rôle emprunté aux apps, forme = `studio_node_ports`, super-types en sortie, 9 producteurs attestés + la facette VOYAGE dans le manifeste extrait, le kind refuse une facette bancale) · **3** propres à l'app (`tests_function_specs` : optionnel ⇒ reference, trace ego = reference sauf au filtre) · `ego_trajectory_filter` + `ego_rotation` 36 tests inchangés · `manifest_export --check --kind function` à jour (59). ⚠ **Rien n'a tourné sur données réelles** : la fusion n'a encore AUCUN appelant dans la chaîne cam_analyzer (les deux sources de cap indépendantes — filtre GPS et `ego_rotation` — ne sont pas câblées ensemble : `ego_rotation` n'a pas de σ). Annulation : `git revert` — champs additifs, `port_dict` rend la forme d'avant quand la facette est absente ; le corpus se régénère. |

## 2026-09-07

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(ce commit)* | **① Le pipeline est déclaré UNE fois, et se lance par ÉTAGE.** (1) `pass_tracking.PASSES` (dataclass `Pass` : `key`, `stage`, `depends_on`, `watched`, `per_camera`, `task`, `gpu`) — les **six** déclarations du même graphe en dérivent : `_WATCHED`, `_STAGE`, `_DEPENDS_ON`, `_PER_CAMERA_PASSES`, la liste `order` en dur de `get_passes_status`, `views.dispatch_map` (→ `dispatch_table()`) ; `PassType` reste la source des libellés/valeurs persistées, `tests_pass_registry` atteste l'égalité des deux ensembles. `depth ← yolo_detect` et `depth_calc ← depth` **déclarées** (absentes avant : aucune cascade de péremption). (2) `run_passes` : les calculs demandés sont **chaînés en ordre topologique** (`topological_order`, Kahn stable ; Celery `chain` de signatures `.si`) au lieu de N `.delay()` parallèles ; **gating 409** si un calcul est demandé sans aucune `DetectionFrame`. (3) JS : **▶ tout** sur chaque ligne d'étage (📷 Analyse / 🧮 Calculs), `runStage()` lit les passes servies (jamais une liste écrite en JS), exclut `extraction` (panneau RTMaps) et `depth` sauf ⚑ `depth_estimation` ON, confirme avant l'analyse (GPU) ; ▶ Calculs grisé tant qu'aucune détection n'est `completed`. (4) **Doublons retirés** : « SAM3 seul » ×2 (barre + panneau, ≡ ▶ `sam3_markings`) avec `startSam3Only`/`refreshSam3OnlyButton` ; « Analyser » ×2 **conservé comme CTA mais rebranché** sur `runStage('analyse')` — l'ancien chemin `start_analysis` **effaçait toutes les `DetectionFrame` et le `results_summary` sans confirmation** (vue laissée servie, plus appelée du front). (5) **Un verbe par notion** : « Compléter les passes (manquantes + périmées) » vs « **Étendre la couverture** (parcours / fenêtres) ». | Question Fabien 07/09 : *« beaucoup de boutons de lancement… doublon ou simplification ? lancer indépendamment toute l'analyse et tous les calculs ? boutons sur les lignes du pipeline ou idée plus subtile ? »* — et *« rendre les boutons schéma-driven, basés sur le pipeline, lui-même schéma-driven »*. Inventaire mesuré : 11 boutons + 13 ▶ pour 4 intentions, dont un doublon strict, deux « tout relancer » (l'un destructif), trois « Compléter » pour deux notions. L'idée subtile n'est pas un bouton de plus : **le serveur connaissait déjà les étages et le graphe** — le ▶ d'étage et son gating en DÉRIVENT. C'est la marche ① vers **D13** (`WAMA_DATA_WORLD §9undecies.2`) : les passes sont déjà des `FunctionSpec` app-bound ; ce registre est ce qui s'exportera en manifeste `pipeline` à nœuds `function` quand le kind les acceptera (items C+D du plan de convergence, dans le commun, Studio à partitionner). | **20 tests** cam_analyzer OK (registre : ensembles égaux, dépendances résolues, tout calcul remonte à l'analyse, empreinte des six dicts CONSERVÉE hors les 2 dépendances ajoutées, ordre topologique — `conflicts` jamais avant `distance`, chaque `task` existe et porte `.si`) · `check_js.sh` 67/0 · `check_templates` 0 défaut (`{% comment %}`) · import réel de `views` + `pass_tracking` OK · `staticfiles` synchro · smoke navigateur (brique `ui_smoke`, compte de test, sans VLM) : **page OK, HTTP 200, 0 erreur JS, 5 onglets** ; JS SERVI vérifié au `curl` = le fichier du jour (305 179 o : `runStage`/`data-rp-stage` présents, `refreshSam3OnlyButton`/`startSam3Only`/`rpStartSam3OnlyBtn` absents) · suite complète **1685 tests `OK`** (11 skipped) — ⚠ le run précédent portait, outre mon test mal écrit, **1 échec fugace hors périmètre** (`gateway.QrAppariementTests…url_publique` : lien QR vide) qui passe 3/3 seul et n'a pas reparu : dépendant de l'ordre, à attribuer (pas touché). ⚠ **Le smoke a une raison d'être ici** : un 3ᵉ appel `refreshSam3OnlyButton()` survivait dans `loadActiveProfile` — `check_js` (syntaxe) ne pouvait pas le voir, il aurait planté au chargement du profil ; trouvé par grep des symboles retirés avant le smoke. ⚠ **Gabarit modifié → HUP gunicorn** avant de juger la page (les gabarits sont en cache par worker). ⚠ **Aucun run réel** de la chaîne de calculs (GPU interdit pour l'analyse ; les calculs, eux, sont CPU — relançables sur la session ENA via ▶ Calculs). Annulation : `git revert` (le registre remplace des dicts à valeurs identiques ; le ▶ d'étage et le chaînage sont additifs ; les boutons retirés étaient des doublons). |

## 2026-09-05

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(ce commit)* | **Trois trous de VISIBILITÉ/COMMUTABILITÉ comblés (②③④ de l'ordre acté), chacun derrière un ⚑ dont le défaut = état d'aujourd'hui.** ② ⚑ **`sam3_homography`** (ON, compute) : la voie DLT sur passage piéton (`camera.ground_homography`) devient commutable là où elle est consommée — gate de l'analyse (`tasks.py` ~1281), `marking_world._projector_for` (paramétrique si OFF), `lane_estimator` (pas d'estimation si OFF) ; le forçage de `profile.geometry_enabled` par la calibration SAM3 est **conservé mais annoncé en console**. ③ **`placement_source` par détection** dans `annotate_global_tracks` ∈ {`ground:homographie`, `ground:depth`, `pinhole`, `pinhole_relaxed`} + compteurs → `results_summary.placement_sources` + ligne console « Source de placement : … » (`_run_global_tracking`). ④ ⚑ **`display_ema`** (ON, **live**) : les deux EMA α=0,3 (distance, latéral) du repli d'affichage ③ (`index.js`, blocs `topDownDist`/`topDownLat`) deviennent commutables **sans recalcul**. Registre : **17 bascules**. | Inventaire du matin : ② la voie « débranchée » était CONDITIONNÉE et s'appliquait sans bascule (D.2) alors qu'elle est prouvée biaisée — un levier doit se comparer ; ③ le repli `ground_ego → pinhole` était **silencieux** (gap G7 depuis le 2026-07-21) : un A/B ⚑ ON/OFF comparait du pinhole à un MÉLANGE sans le dire — désormais le mélange se **compte** ; ④ l'hypothèse D.3 (EMA = retard de phase = garés qui « suivent la navette ») exigeait un recalcul pour être testée — un ⚑ live la rend testable à l'œil ET au chiffre. Défaut ON partout : rien ne change tant qu'on ne bascule pas (règle acceptée par Fabien : corrections d'abord, test D.3 en dernier, tout commutable). | `check_js.sh` 67/0 · `staticfiles` synchronisé · `manage.py check` 0 · import réel des 5 modules touchés OK (le `check` ne voit pas les imports paresseux) · 90 tests du périmètre OK · suite complète **1628 tests `OK`** (11 skipped). ⚠ **Aucun run réel ni smoke navigateur** : la ligne « Source de placement » et l'effet de `display_ema` sont à relever sur une session ENA. ⚠ `placement_source` n'est écrit que sur les détections associées (une détection `relaxed` sans correspondance est ignorée par le tracker, donc non persistée sauf si sa frame est sauvée pour une autre) — cohérent avec le fait qu'elle ne participe pas au placement. Annulation : ⚑ ON = historique ; `git revert` retire le champ (additif, ignoré du reste). |
| *(ce commit)* | **⚑ `shuttle_filter` (OFF, compute) — la pose NAVETTE est filtrée pour la première fois.** (1) **Brique pure** `wama_data/functions/driving/ego_trajectory_filter.py` : `filter_gps_points` (noyau, listes de dicts) + `filter_ego_track` (wrapper `TypedFrame → TypedFrame`, `fn=` du FunctionSpec `ego_track_filter`, ENRICHER) — Kalman vitesse-constante + RTS sur lat/lon (repère ENU local), **cap dérivé de la vitesse lissée**, tenu sous `heading_min_speed_mps` (défaut **1,0 m/s**, mesuré) ; colonnes ajoutées `lat_f/lon_f/speed_f_kmh/heading_f/heading_f_held`, brutes intactes ; rapport A/B dans `meta` (déplacement RMS, écart de cap médian, part tenue). (2) **Le lisseur Kalman+RTS remonte dans le commun** : `wama_data/functions/kinematics/rts_smoother.py::kalman_rts_cv` (port exact), `utils/trajectory_smoother.smooth_track` **délègue** — non-régression attestée par `tests_trajectory_smoother.py` sur une **empreinte numérique enregistrée AVANT le déplacement** (5 lignes, 1e-5). (3) **Câblage** : `ego_pose.compute_shuttle_filter` (CALCUL stocké dans `results_summary.shuttle_filter`, appelé en tête de `_run_global_tracking` si ⚑ ON, ligne A/B console) ; `ego_pose.effective_gps_track` = **point d'accès UNIQUE** de la pose côté serveur, relu à chaque appel (OFF ⇒ brut même si un calcul traîne) ; **6 consommateurs basculés** (`multicam_tracker`, `prediction_adapter.annotate_prediction_indicators`, `homography_estimator` — l'ancre ego-motion de 2a —, `marking_world`, `intersection_branches`, `artifact_filter`) ; **fenêtres et couverture restent sur le brut** (elles délimitent l'analyse, pas une position). JS : `_applyShuttleFilter` au point d'ingestion unique `renderMiniMap`, **avant** `_applyOrthoCorrection`, apparié par `ts` — miroir exact du serveur ; `shuttleFilter` chargé aux deux points de lecture de `results_summary`. `function_specs` : `cam_analyzer.shuttle_filter` (app-bound). | Inventaire du matin (§INVENTAIRE D.1, levier 15 « INEXISTANT ») : la navette roulait sur GPS brut interpolé linéairement, le seul lissage du cap vivait dans le JS en repli ③, et **aucun des 14 ⚑ ne touchait la pose navette** — alors que §B montre qu'elle contamine toutes les détections d'une frame et que §[2] la désigne comme LA source d'erreur angulaire dominante. Ordre acté avec Fabien : corrections d'abord (chacune derrière un ⚑), test D.3 en dernier. Pas d'accéléromètre en entrée de commande : **les axes X/Y du capteur ne sont mesurés nulle part** (seul Z ≈ 0,95 g), l'orienter à l'aveugle serait pire que rien → étape suivante = identifier l'axe avant par corrélation avec dv/dt du GPS filtré, une MESURE. | **Brique** : 12 tests dont le décisif — cap brut médian **> 8°** d'erreur (le piège est réel) vs cap filtré **< 1/3** ; position RMS divisée par > 1,6 ; convergence < 2 s après arrêt franc (RTS = pas de retard de phase) ; **limite documentée** : à ±2 m de bruit GPS, ≥ 75 % des caps tenus à l'arrêt, pas 100 %. Seuil 0,5 → 1,0 m/s **après mesure** (0,17 médian / 0,50 max à ±1 m ; 0,33 / 1,01 à ±2 m). **Non-régression objets** : empreinte à 1e-5 ✓. `check_js.sh` 67/0 · `staticfiles` synchronisé · `manage.py check` 0 · catalogue **58** fonctions, **15** bascules · suite complète **1628 tests `OK`** (11 skipped) — ⚠ un 1ᵉʳ run avait rendu 1 échec dont le détail a été perdu (sortie tronquée par mon `tail`), non reproduit ni au 2ᵉ run complet ni sur les 4 périmètres ciblés ; l'autre instance a commité entre-temps des fichiers `model_manager`/`settings.py` alors non commités — cause plausible, non prouvée. ⚠ **Aucun smoke navigateur ni run réel** : la ligne A/B console et le rendu filtré sont **à relever sur une session ENA** (⚑ ON → « Calculer les indicateurs » → lire `Filtre navette : …` puis `placement_spread` OFF vs ON). ⚠ Sigma (0,8 m/s², 2,0 m) **PROVISOIRES** — à réétalonner sur `placement_spread`. Annulation : ⚑ OFF (défaut) = état d'avant au bit près ; `git revert` retire la brique. |
| *(ce commit)* | **INVENTAIRE EXHAUSTIF mesuré dans le code** → `CAM_ANALYZER_CHAINE_TRAITEMENT.md §INVENTAIRE` (nouvelle section, ~200 lignes) : **A.** les 13 passes dans l'ordre d'exécution (fonction, entrées, traitement, sorties écrites, état) + A.11 l'ordre INTERNE de `annotate_global_tracks` en 12 étapes ; **B.** le chemin d'une bbox jusqu'à la carte, avec l'insertion de chaque correction ; **C.** **43 leviers de correction** relevés (grandeur, source, fichier:ligne, ⚑ ou constante, état, métrique A/B) ; **D.** trois affirmations RÉFUTÉES ; **E.** cadre de la fusion de données (incertitude, indépendance, provenance des valeurs, risques) ; **F.** les 6 modèles IA et leur apport. Doc seule, **aucun code touché**. Corrections in-place : §[2] (IMU), §[4] (voie ancienne), §[7] (`annotate_prediction_task` n'existe pas ; trou max 2,5 s, pas 1 s), `Décisions actées §7`, date. | Demande Fabien : « lecture et consignation exhaustive AVANT tout pour ne pas réinventer ou recâbler un traitement non tracé de bout en bout ». Déclencheur : la doc de design disait la fusion IMU **faite**, le code ne l'a **jamais faite** — `git log -S imu_track` rend UN commit (`89353afd`, 2026-07-09, la création). Trois réfutations mesurées : **D.1** IMU parsé/stocké/jamais lu, `use_imu` 0 consommateur, **aucun filtre sur la navette** (Kalman+RTS = objets mobiles seulement, position seule) ; **D.2** l'homographie SAM3 « débranchée » est **CONDITIONNÉE** (`geometry_enabled ∧ ground_homography`), et la calibration SAM3 **force** `geometry_enabled=True` — `marking_world` l'utilise sans condition ; **D.3** hypothèse pour les garés qui « suivent la navette » : **EMA α=0,3 en repli ③** (`index.js:3191`) = retard de phase, cohérent avec l'état mixte ①/②/③ et avec le critère « jamais stationné près d'une intersection ». Ce que la liste C révèle : **13/43 leviers en ⚑**, les 30 autres sont des constantes non comparables ; **un seul** levier touche la pose navette (JS, cap) alors que §B montre qu'elle contamine tout ; **un seul** chiffre A/B (`placement_spread`) juge une configuration entière, jamais un levier. | **Tout lu dans la source** : 30 modules `utils/`, `tasks.py` (passes, `_run_global_tracking`, calibration SAM3), `models.py`, `views.dispatch_map`, `index.js` aux points d'ingestion/rendu, `kinematics/extrapolation`, historique git (`-S`). Aucune ligne recopiée d'un `.md` — quand doc et code divergeaient, le code a tranché et la divergence est consignée. ⚠ **D.3 est une HYPOTHÈSE** : test objectif proposé (relever les gids qui dérivent → ancre ? `world_en` ? repli ?), sans GPU. ⚠ Le levier 15 (Kalman navette) est INEXISTANT, pas retiré : rien à restaurer, tout à écrire. Ordre acté : inventaire → filtrage navette (après test D.3) → facette estimateur → fusion. Annulation : `git revert` (doc seule). |
| *(ce commit)* | **Correction de l'inventaire (D.2) + ordre de travail révisé.** (1) §D.2 disait « le tracker ne l'ignore pas » : **FAUX** — `multicam_tracker` lit `config.ground_calib` (2a) et `pinhole_ego`, jamais `camera.ground_homography` ni `ground_xy` ; le JS ne lit `ground_xy` que si `distance_m` est absent (`index.js:3215`). Consommateurs RÉELS de la voie SAM3→DLT : l'analyse (champs écrits, exploités par le seul export), **`marking_world`** (marquages sur la carte) et **`lane_estimator`** (largeur de voie). Impact réel mais ÉTROIT. Le ⚑ que l'utilisateur connaît (`auto_ground_calib`) porte sur 2a, câblé de bout en bout — la voie ancienne n'a AUCUN ⚑. (2) §D.1 gagne l'**asymétrie** : le seul lissage du cap navette est JS (repli ③) ; tout le serveur hérite du cap brut. Zéro ⚑ sur la pose navette. | Questions Fabien : « ce n'est pas un câblage conditionnel ? » (D1 — non : `use_imu` n'a que sa définition, aucun `if`, grep tous types de fichiers) ; « il y a un switch de comparaison pour l'homographie » (D2 — oui, `auto_ground_calib`, mais sur 2a) ; « pourquoi le test D.3 en premier ? » — **ordre révisé, accepté** : corrections d'abord, test D.3 en dernier, à la condition que CHAQUE correction passe derrière un ⚑ (tous OFF = état d'aujourd'hui, donc rien n'est perdu ; puis ON un à un pour attribuer). | Doc seule. Ordre de travail acté : ① ⚑ `shuttle_filter` (Kalman+RTS sur `gps_track`, accéléro en commande, cap dérivé de la trace filtrée, appliqué au point d'ingestion UNIQUE serveur `shuttle_trajectory` + JS `cachedGpsTrack`, patron `ortho_correction`) ; ② D.2 : ne plus forcer `geometry_enabled` à la calibration SAM3, conditionner `marking_world` ; ③ G7 : écrire `distance_source` au repli `ground_ego→pinhole` ; ④ repli ③ : exposer/remplacer l'EMA à retard de phase (⚑) ; ⑤ `manifest_export --kind function` puis facette estimateur ; ⑥ test D.3 tous OFF puis ON. |

## 2026-09-04

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(ce commit)* | **Trois briques PURES de référentiel géo + une correction de manifeste. AUCUN comportement cam_analyzer changé — rien n'est encore appelé depuis l'app.** (1) **`geo.ign_road_map`** (`wama_data/functions/geo/ign_vector.py`) : adaptateur BD TOPO → port `road_map`, **seconde** voie d'alimentation à côté du CSV de projet (`load_road_map_csv`, tracé tiers) qu'elle ne remplace pas. (2) **`geo/osm_vector.py`** (nouveau module, jumeau d'`ign_vector`) : `osm_control_nodes` (`stop`, `give_way`, `traffic_signals`, `crossing`, `mini_roundabout`, passage à niveau) via Overpass, `osm_road_map` (référentiel **mondial** avec `oneway`/`lanes`/`maxspeed`), et `nearest_control()` qui rend en MÈTRES l'écart entre un marquage détecté et la règle déclarée. (3) **`geometry.ego_rotation`** : lacet/tangage de la caméra entre deux images par ajustement 3 paramètres sur des correspondances de points du décor (rotation = décalage uniforme, translation avant = expansion radiale — deux signatures séparables sans profondeur), plus `yaw_disagreement()`. Ne consomme aucune image ni OpenCV : l'appariement reste à l'appelant. (4) **Correction de manifeste** : `ign_roads` et `road_branches` déclaraient produire `road_map` alors qu'ils rendent `coords` en (lon, lat) dans une liste de dicts → repassés en `table`. | Question de Fabien : que tirer d'OSM / bases géo / dépôts de vision sans intégration lourde, sachant que l'ortho IGN, la BD TOPO vecteur et le map-matching sont **déjà là**. Réponse mesurée : (a) le port `road_map` n'avait qu'une voie, un **CSV construit à la main**, alors qu'`ign_roads` publiait déjà le port — trois écarts empêchaient le chaînage, dont **un silencieux** (axes inversés) ; (b) OSM est le SEUL à publier la sémantique de priorité, que `learned_branches` et `world_markings` **apprennent aujourd'hui sans aucune vérité terrain** — la règle du domaine (« ce qu'un référentiel publie en vecteur ne se détecte pas visuellement ») s'y applique ; (c) un SLAM complet est écarté (fx ≈ 134 px sur 384×248, plafond de précision déjà assumé, doublon avec `homography_estimator`+Depth Pro+Kalman), mais la **rotation seule** donne une 2ᵉ source de cap indépendante du GPS, donc un désaccord chiffrable là où le cap n'a aujourd'hui aucun contradicteur. | **38 tests neufs, tous OK** (8 `tests_ign_vector` + 14 `tests_osm_vector` + 16 `tests_ego_rotation`) · **suite complète 1553 tests `OK`** (6 skipped) · `manage.py check` 0 issue · catalogue **55 fonctions**, port `road_map` = 2 producteurs légitimes (contre 4 déclarés dont 2 faux avant). **Contre-épreuves explicites** — chaque piège silencieux est testé par son RÉSULTAT, pas sa forme : axes IGN inversés ⇒ `section_id=None` partout sans exception (vérifié en mutation : le cas corrigé recale à 3,0 m, le cas bugué rend `None`) ; convention de signe du lacet testée dans les deux sens (droite ⇒ positif) ; le test du modèle 3-paramètres vérifie D'ABORD que l'estimateur naïf (« médiane des Δx / f ») produit bien un lacet fantôme > 1° sur points asymétriques, sinon il ne prouverait rien. ⚠ **Aucun smoke navigateur, aucun appel réseau réel** (Overpass et le WFS sont remplacés dans les tests) : la 1ʳᵉ requête OSM réelle reste à passer. ⚠ **Rien n'est branché** : pas de bascule ⚑, pas d'appel depuis `tasks.py` — donc pas de flag à poser tant qu'aucun comportement ne change. **Reste à faire** : câbler `ego_rotation` derrière un ⚑ avec extraction Lucas-Kanade sur points hors bbox + ligne A/B `désaccord lacet vu ↔ GPS` ; confronter `world_markings` aux `osm_control_nodes` (écart médian en m) ; alimenter le map-matching par `ign_road_map` là où aucun tracé de projet n'existe. Annulation : `git revert <commit>` — aucune fonction n'a de consommateur, la correction de manifeste ne casse rien (seuls les tests neufs citent ces clés). |

| *(ce commit)* | **`ego_rotation` remis au formalisme `pure` + le 4ᵉ registre déclaratif gagne ses tests de conformité — et un bug d'exécution préexistant tombe avec.** (1) `geometry/ego_rotation.py` gagne son **wrapper** `ego_rotation(matches: TypedFrame, …) -> TypedFrame` (patron `placement_metrics` : noyau sur tuples + wrapper typé) ; `fn=` désigne désormais le wrapper et non le noyau. Sortie SCALAR = le lacet, diagnostic (`pitch`, `expansion`, `n_inliers`, `residual_px`, `usable`) dans `meta`. (2) **`tests_catalogues.py::FunctionCatalogConformiteTest`** — le `FUNCTION_CATALOG` (55 entrées) était le seul registre déclaratif **sans aucun test** : clés cohérentes, types de port dans la taxonomie, `app`-bound avec `impl`+`app`, `pure` avec `fn` appelable, contrat `TypedFrame → TypedFrame` (budget de dette nommé, **ne peut que descendre**), et **clés de `ParamSpec` acceptées en mots-clés par la signature**. (3) **Bug trouvé par ce dernier contrôle, dès sa 1ʳᵉ exécution** : `coding_replay`/`coding_events`/`coding_segments` déclaraient `ParamSpec('protocol')` pour un paramètre nommé `protocole` → `view.apply()` aurait levé `TypeError: unexpected keyword argument`. Renommé côté Python (un paramètre est une API : anglais), 3 appels de test suivis. | Question de Fabien (« tu as bien construit dans le formalisme WAMA ? ») — vérifiée plutôt qu'affirmée, et la réponse était **non pour une des quatre fonctions**. `view.apply()` appelle `spec.fn(entrée_typée, **params)` : un `fn` prenant des tuples et rendant un `dict` casse à l'exécution pendant que `can_connect` répond oui et que le catalogue l'affiche chaînable. Mesure faite avant de poser le garde : **26/30 des pures chaînables honoraient déjà le contrat** — la convention existait, elle n'était ni écrite ni vérifiée, donc destinée à dériver. Le contrôle est posé là où vivent déjà ceux des 3 autres registres, dont l'en-tête décrivait exactement ce défaut (« en ajoutant une entrée la suite ne tombait pas, elle devenait muette »). | **1553 → 1593 tests, suite complète `OK`** (10 skipped) · ⚠ la 1ʳᵉ exécution a été **ROUGE**, et sur ce commit même : le garde de nommage `wama_data/core/tests_naming.py` a repéré `cle` — identifiant abandonné à la migration D28 — réintroduit dans le fichier de test neuf (`for cle in …`). Renommé `key`. *Un garde qui attrape l'auteur du garde suivant : c'est le signe qu'il travaille.* · les 3 dettes de contrat antérieures (`depth_contact_distance`, `depth_ground_plane`, `trajectory_offset`) sont **nommées** dans `CONTRAT_ASSUME`, pas masquées : une 4ᵉ fait tomber le test. ⚠ Le contrôle de contrat passe par les **ANNOTATIONS** — c'est un PROXY assumé et documenté dans le test : il atteste que l'auteur a écrit le contrat, pas que `fn` rende vraiment un `TypedFrame` (la vérification forte exigerait d'appeler chaque fonction sur une donnée valide, hors de portée d'un contrôle générique). Il suffit à attraper le cas réel qui l'a motivé. Annulation : `git revert <commit>` — le renommage `protocole`→`protocol` est le seul changement de comportement, et il **répare** un appel qui ne pouvait pas fonctionner. |

| *(ce commit)* | **`yaw_disagreement` gagne son garde d'honnêteté — la métrique MENTAIT à basse vitesse.** Nouveau paramètre `reference_held` : quand le cap de référence est TENU (gelé) au lieu d'être mesuré, la fonction rend `None` au lieu d'un écart. Docstring du module réécrite pour citer la cause racine nommée par `CHAINE §[2]` et la borne de `§Calibration sol`. | **Question de Fabien : « as-tu bien lu la philosophie du cam_analyzer, son pipeline et ses modes de comparaison ? »** — non, pas en entier : j'avais la doctrine par le skill, mais ni `CAM_ANALYZER_CHAINE_TRAITEMENT.md` en entier, ni `utils/features.py`. La lecture a trouvé le défaut. `ego_pose.py:139` tient le cap au dernier connu sous 0,30 m de déplacement (« aucun gyroscope ») : ce n'est plus une mesure mais une CONSTANTE, de dérivée nulle. Comparer le lacet vu à cette constante rendait un désaccord **exactement égal au lacet vu** — un artefact du gel présenté comme une erreur de cap, et **maximal là où la vision est la plus fiable et le GPS le moins** (arrêt, manœuvre lente). C'est le gap **G7 transposé au cap** : `§[4]` exige déjà qu'un A/B s'appuie sur `distance_source` pour ne pas comparer de l'homographie à du repli pinhole silencieux. Le drapeau est porté par l'APPELANT et non déduit d'un seuil de vitesse : un cap d'API navette (`EgoPose.source == 'shuttle_api'`) reste fiable à l'arrêt — seul l'appelant connaît la source. **Deux lectures qui ne changent rien au code mais tout à sa justification** : (1) `§[2]` désigne ce cap comme **LA source d'erreur angulaire dominante** et la chiffre (±10-25° à basse vitesse ; 8 m × 15° ≈ 2 m d'arc) — `ego_rotation` vise donc une cause racine NOMMÉE, pas une amélioration spéculative, et la complémentarité est structurelle (le GPS se dégrade quand la navette ralentit, la vision s'améliore) ; (2) l'insight fondateur du 2026-07-20 (ANGLE par le mouvement seul / ÉCHELLE par géométrie connue, « limite mathématique ») pose exactement la frontière où `ego_rotation` s'arrête — j'y étais arrivé par l'argument de résolution, la doctrine y arrive par la géométrie. | 74 tests du périmètre OK puis **suite complète 1595 tests `OK`** (10 skipped). Deux tests neufs verrouillent le garde : cap gelé ⇒ `None` ; **et la même paire SANS gel ⇒ 12,0 °/s**, sans quoi le test passerait aussi sur une fonction qui rend toujours `None`. Aucune bascule ⚑ posée — conforme à `§2a` (« la bascule seule ne termine pas, la métrique si ») : rien n'est branché, donc rien n'est comparable. **Piste ouverte, PAS vérifiée** : 2a résout l'angle en prenant l'ego-motion GPS pour ancre ; une rotation visuelle pourrait remplacer cette ancre plutôt que seulement la contredire — à mesurer avant d'y toucher. Annulation : `git revert <commit>` (le garde ne peut que SUPPRIMER des mesures fausses ; aucun consommateur). |

## 2026-08-22

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(ce commit)* | **Chemins d'import seulement — AUCUN changement de comportement.** WAMA Data est sorti de `wama/common/data/` vers une racine `wama_data/` (monde à part entière). Les 6 fichiers cam_analyzer qui consommaient ces fonctions suivent : `views.py`, `tasks.py`, `utils/prediction_adapter.py`, `utils/multicam_tracker.py`, `utils/depth_estimator.py` (→ `wama_data.functions.*`) et `function_specs.py` (→ `wama.common.catalog.*`). Un commentaire de `static/…/index.js` cite un chemin Python : mis à jour aussi, statics recopiés. **Zéro ligne de logique touchée** — c'est un remplacement de chaînes d'import. | Doctrine des MONDES : un monde n'est pas un sous-dossier du substrat. La frontière a été posée pour PROTÉGER cam_analyzer : le registre de fonctions et la taxonomie de types restent dans `common/catalog/`, donc le Lab ne dépend pas du monde Data pour déclarer ses propres fonctions — `function_specs.py` ne change que de **2 lignes** (le chemin du catalogue), pas de structure. | **245 tests OK** (`wama_data` + `wama.common` + `wama_lab`) · `manage.py check` : 0 issue · vérification dédiée : les 19 fonctions `cam_*` sont toujours au catalogue (39 au total, identique à avant le déport), et les 3 modules cam_analyzer + les 4 modules `wama_data` consommés s'importent. ⚠ **Restart du process WSL2 requis** (Python touché). Annulation : `git revert <commit>` — la réécriture est mécanique et intégralement réversible. |

## 2026-08-06

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(ce commit)* | **Volet droit — ⚑ Comparer DÉ-FLOTÉ + SCINDÉ par scope (Palier A du chantier volet droit, ex-Q3).** (1) `#featModesPanel` n'est plus `position-absolute` en surimpression de la mini-carte pivotée (fragilité de superposition, cf. skill « aucun pane custom sur la carte tournée ») → **docké** sous `#camAnalyzerMapToolbar` (`base.html`), style aligné sur `calibPanel`, toggle `featModesBtn` **inchangé** (simple `d-none`, aucun positionnement à retirer). (2) `renderFeatPanel` (`index.js`) **auto-génère deux sous-groupes PAR SCOPE** (métadonnée-driven, lecture de `f.scope` déjà servi par `utils/features.py`, zéro nouvelle logique) : **⚡ Affichage — immédiat** (`scope !== 'compute'`) et **⟳ Calcul — à relancer** (`scope === 'compute'`). La logique de ligne (checkbox / `onchange` / POST override) est **inchangée**, seulement extraite en helper `_buildFeatRow`. | Q3 du handoff volet droit (`REPRISE_2026-08-06`) validé Fabien : le menu mélangeait bascules immédiates et bascules exigeant un recalcul sans matérialiser la coupure, et flottait sur la carte. La scission suit « UI DEPUIS les métadonnées » (le champ `scope` existe déjà) et le modèle posé par Fabien 2026-08-06 : **affichage = lit des résultats DÉJÀ stockés (zéro recalcul) vs calcul = recalcul**. Prépare le Palier B (replier les toggles « Vue » 360°/Prédiction/Garés/Voie dans le registre, mêmes `id` → comportements préservés). | `check_js.sh` **51/0** · statics copiés vers `staticfiles/cam_analyzer/` · **template modifié → HUP gunicorn requis avant smoke**. ⚠ **Smoke navigateur NON relancé** cette édition — à vérifier : ouvrir ⚑ Comparer → panneau docké sous la barre, 2 sous-en-têtes (6 flags immédiats / 8 recalcul), plus de flottement sur la carte. Aucun comportement de bascule changé (mêmes `key`/`onchange`). Annulation : `git revert <commit>` (présentation seule, aucune donnée ni calcul touché). |

## 2026-08-05

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| `5ddd83e` | **Modèle de profondeur DA3 onboardé via le mécanisme en place** (prospect→pull→register→discover→select). `settings.MODEL_PATHS['vision']['depth']` = `models/vision/depth-anything` ; `model_registry._discover_depth_models()` — scan **filesystem** (source générique `huggingface`, **aucun import lab→core** : la découverte reste dans la couche model_manager) émettant `huggingface:da3metric-large` (task=depth-estimation, vram 6 Go, `is_downloaded` dérivé des poids réels) ; DIR du squelette `depth_estimator.py` aligné sur `depth-anything`. Téléchargé : `pull_model depth-anything/DA3METRIC-LARGE --category vision --family depth-anything` → 1 `model.safetensors` **~1,34 Go**. | Tester la chaîne prospection+installation fraîchement livrée en s'en servant pour de vrai (Fabien 2026-08-05) et **combler le seul trou** : aucun `_discover_*` ne scannait un dossier profondeur. Source `huggingface` générique (ni nouvelle `ModelSource`, ni inversion core→lab). Contrainte VRAM **RTX 4090 24 Go** (pas le R760xa) → sélection VRAM-aware, 6 Go ≪ budget. | `manage.py check` 0 issue · hook isolé émet l'entrée · **après download** : `is_downloaded=True`, ligne `AIModel huggingface:da3metric-large` (type vision, task depth-estimation, `platform_ref` auto), `check_model_taxonomy` passe. ⚠ Sync écrite base **Windows** (venv_win) ; base WSL2 *live* se recale au prochain `sync_models` (poids sur disque partagé, hook committé). ⚠ **Poids ≠ utilisable** : loader `estimate_depth` non écrit (à faire contre l'API réelle de DA3, GPU côté runtime). Annulation : `git revert 5ddd83e` + suppression du dossier de poids. |
| *(ce commit)* | **Métrique A/B objective #1 CÂBLÉE dans cam_analyzer : étalement monde RMS des stationnés.** Calcul en fin de `annotate_global_tracks` (`multicam_tracker.py`, juste avant le `return`) à partir de `track_hist` filtré par `_stat_set` — la **distribution complète** par observation, pas les médianes de `stationary_anchors` (qui donneraient RMS=0 par construction) — via la brique commune **pure** `wama_data.functions.geometry.placement_spread`. Persistée dans `results_summary['placement_spread']` (agrégat + détail par track) et **remontée en console avec l'état du flag ⚑ `auto_ground_calib`** (`_run_global_tracking`, `tasks.py`). Capacité déclarée `cam_analyzer.placement_spread` (INDICATOR, binding=app) dans `function_specs.py`. **Lecture seule : ne modifie AUCUN placement.** | Instrumenter l'A/B ⚑ `auto_ground_calib` ON/OFF sur un **CHIFFRE** (règle mémoire `feedback-ab-objective-metric`) : la quasi-totalité des commits 07-17→07-20 portaient « navigateur à valider » sans jamais être tranchés à l'écran. C'est le **préalable P1.1** — décider si la projection sol réduit réellement l'étalement des stationnés, plutôt qu'« à l'œil ». Brique en `common/` car destinée à WAMA Data (réutilisable par tout pipeline posant des objets en monde). | `py_compile` OK (3 fichiers) · `manage.py check` 0 issue · catalogue = **2 entrées** (`placement_spread` pure + `cam_analyzer.placement_spread` app) · **smoke numérique** de l'extraction `track_hist` : immobile parfait→0.0 m, bruit ~1 m→0.894 m, track <3 obs ignoré, agrégat correct. ⚠ **Pipeline GPU non rejouable sur cet hôte (WSL2 GPU interdit)** → apparition console + `results_summary` sur **session réelle À VALIDER au navigateur / runtime WSL2**. Réserve **G7** : le placement peut être MIXTE (pinhole silencieux hors portée) sans `distance_source` — l'A/B compare ici **deux configs entières** (flag ON vs OFF), honnête au niveau agrégat ; le split par source reste à faire (métrique #2). Annulation : `git revert` (métrique additive, aucun placement changé). |
| *(ce commit)* | **Préparation de l'intégration profondeur (piste [E]) — DÉCLARATIF, rien branché.** Flag MAÎTRE `depth_estimation` (défaut **OFF**, `utils/features.py`) + squelette **INERTE** `utils/depth_estimator.py` (contrat `load`/`estimate_depth`/`unload` ⇒ `NotImplementedError`, `DEPTH_MODEL_ID='depth-anything/DA3METRIC-LARGE'`, DIR à fallback, **aucun poids téléchargé**, module importé par personne) + **plan d'intégration** consigné en §[E] : les 5 usages → points d'accroche pipeline + **métriques A/B** + flags dédiés. **PoC #1 = usage 4** (re-calage du plan de sol), validé sur `placement_spread` déjà câblée. | « Préparer sans implémenter » (règle *rien d'implémenté sans en avoir discuté* + piste exploratoire à **chiffrer avant tout code**). Point fort tracé : usages **3/4/5** tombent exactement sur les métriques A/B **#2/#1/#3** → la profondeur se validera sur le MÊME instrument, jamais « à l'œil ». | `py_compile` OK · `manage.py check` 0 issue · flag présent (défaut `False`) · squelette `is_available()=False`. **Aucun comportement changé** (flag OFF, module non importé). Coordination (partition) : tâche `depth-estimation` **DÉJÀ déclarée** (`ModelTask`) ; restent `settings.py::MODEL_PATHS['vision']['depth']`, déclaration DA3 `model_registry`/catalogue (**session catalogue — DEMANDER**), protocole `PROTOCOLES` du banc. ⚠ GPU interdit WSL2 → inférence côté runtime/R760xa. Annulation : retrait flag + fichier (additifs). |
| *(ce commit)* | **Profondeur — usages 3 (réciproque pinhole) + 1 (reflets) câblés en MESURE-ET-RAPPORT, sous le MÊME flag global.** `depth_estimator.depth_distance_report(session)` : UNE seule passe Depth Pro échantillonnée (≤12 frames, partagée par les deux usages), appelée en fin de `_run_global_tracking` (`tasks.py`) quand ⚑ `depth_estimation` est ON. Écrit le champ **additif** `depth_distance_m` (profondeur métrique au contact-sol de la bbox) et **ne bascule aucune source** existante — le placement reste pinhole/homographie (usages PARALLÈLES, pas superposés). Deux **lignes A/B console indépendantes** : usage 3 = désaccord médian profondeur↔pinhole et ↔homographie (m, profondeur = 3ᵉ source) ; usage 1 = désaccord des détections marquées `artifact` vs propres (un reflet de vitrage projette une profondeur incohérente → confirme/infirme `artifact_filter`). Persisté `results_summary['depth_report']`. Protocole banc `depth-estimation` inchangé. | Poursuite demandée par Fabien (« les corrections sont parallèles pas superposées ») : chaque usage se greffe à son propre point, un flag unique, une ligne A/B par usage → observables séparément. Mesure-et-rapport d'abord (comme le recalage ortho 2b) : on lit les chiffres au run GPU AVANT de basculer une source. | `py_compile` OK · `manage.py check` 0 issue. ⚠ **NON fumé GPU/navigateur** (WSL2 interdit) → les désaccords et la confirmation reflets sont **à relever côté runtime/R760xa** (flag ON, session ENA rejouée, lire les lignes `[DepthPro] Distance (usage 3)` et `[DepthPro] Reflets (usage 1)`). Usages **2 (statique/mobile) et 5 (occlusion) différés en 2ᵉ passe** : exigent une profondeur compensée de l'ego / ordonnée au hand-off. Annulation : `git revert <commit>` (flag OFF par défaut → aucun effet sans opt-in ; le champ `depth_distance_m` est additif, ignoré du reste du pipeline). |
| *(ce commit)* | **Profondeur monoculaire BRANCHÉE (1ère passe) — DA3 → Apple Depth Pro, loader écrit, plan de sol câblé, UN SEUL flag global.** (1) **Swap de modèle** DA3 → `apple/DepthPro-hf` (natif `transformers`, `DepthProForDepthEstimation`, **métrique + focale/FOV estimés**, Apache-2.0) : `settings.MODEL_PATHS['vision']['depth']` = `models/vision/depth-pro`, `model_registry._discover_depth_models()` émet `huggingface:depthpro` (hf_id `apple/DepthPro-hf`, vram 8 Go, task depth-estimation, scan filesystem — pas d'import lab→core), ancienne ligne DA3 (pk 226) purgée du catalogue, dossier `vision/depth-anything/` + checkpoint erroné `apple/DepthPro` (poids `depth_pro.pt` non chargeable par `transformers`) supprimés. (2) **Loader réel** `utils/depth_estimator.py` : `load`/`estimate_depth`/`estimate_ground_plane_ph` (keep_loaded, `HF_HUB_CACHE` posé avant import HF, fp16, `post_process_depth_estimation` → profondeur métrique + focale). Le plan de sol = déprojection des pixels **roulables** (road_mask, repli 40 % bas) → nuage 3D → **RANSAC** (seed fixe) → `(pitch_deg, height_m)`, gardes physiques. (3) **Câblage A/B loyal** : `estimate_ground_plane_ph` ne fournit que le **couple candidat** ; le SCORING reste dans `homography_estimator.estimate_camera(..., seed=)` → `placement_spread` calculée à l'identique pour profondeur vs homographie vs pinhole. `store_ground_calib` lit ⚑ `depth_estimation` : ON → graine profondeur (repli grille si échec) ; `tasks.py` déclenche la calib sur `auto_ground_calib` **OU** `depth_estimation` et console `plan de sol : profondeur\|homographie\|pinhole`. (4) **Protocole banc** `depth-estimation` (`bench.py::_bench_depth` — latence/couverture/médiane m/focale, pas une note de qualité). | Réutiliser le mécanisme prospection+installation en conditions réelles PUIS attaquer la profondeur (Fabien 2026-08-05), en **auto-validation cette session**. Depth Pro retenu vs DA3 : natif `transformers` **et intrinsèque estimé** — exactement ce que le re-calage du plan de sol consomme (le monoculaire manque d'échelle absolue, la focale estimée la fournit). **UN SEUL flag global** `depth_estimation` pour TOUTE l'amélioration profondeur (décision Fabien : pas de sous-flags par usage qui polluent ⚑ Modes ; les scopes ne se recouvrent pas). 1ère passe **parallèle à l'existant** (ON/OFF) : seul risque = gain insuffisant → on itère. | `py_compile` OK (6 fichiers) · `manage.py check` 0 issue · catalogue `Apple Depth Pro \| apple/DepthPro-hf \| dl=True \| vram=8.0 \| task=depth-estimation`, `sync_models` +1, `check_model_taxonomy` passe · config `architectures=['DepthProForDepthEstimation']`. ⚠ **SMOKE GPU/NAVIGATEUR NON EXÉCUTÉ** (GPU interdit sous WSL2 sur cet hôte) → l'inférence Depth Pro, la déprojection et le gain `placement_spread` restent **À VALIDER côté runtime/R760xa**. ⚠ **Convention de signe du pitch NON validée** (`atan2(nz, -ny)`, positif = caméra piquée) : un signe faux se lit **immédiatement** sur `placement_spread` (A/B console) — 1er point à vérifier au run. ⚠ Base **live = WSL2** (le `manage.py` Windows y écrit directement, `_resolve_db_host`). Annulation : `git revert <commit>` (flag OFF par défaut → aucun placement changé sans opt-in) + suppression du dossier `models/vision/depth-pro`. |
| *(ce commit)* | **Profondeur — couche de calcul EXTRAITE en briques PURES du tronc commun + greffage pipeline corrigé.** (1) **Extraction** (règle skill §3 : la logique pure va dans `wama_data/functions/<domaine>/`, PAS dans `cam_analyzer/utils/`) → nouveau `wama_data/functions/geometry/depth_geometry.py` : `deproject_depth`, `fit_plane_ransac` (RANSAC + raffinement SVD), `plane_pitch_height`, `ground_plane_from_depth`, `contact_depth` — **numpy seul, zéro Django/cv2/torch/import lab** (pas d'inversion de dépendance). `depth_estimator.py` ne fait plus AUCUNE géométrie : il charge le modèle, décode les frames, écrit en base et **DÉLÈGUE** (le RANSAC local et `_sample_depth_bbox` dupliqués sont supprimés). Nouveau type `DataType.DEPTH_MAP`. (2) **Catalogue** : 4 entrées — 2 pures auto-déclarées (`geometry.depth_ground_plane`, `geometry.depth_contact_distance`) + 2 app (`cam_analyzer.depth_ground_plane`, `cam_analyzer.depth_distance_report`) décrivant la passe couplée-session, patron `placement_spread`. (3) **2 verrous de greffage levés** : le tracker (`multicam_tracker.py`) ne bâtissait `_gproj` que sous ⚑ `auto_ground_calib` → **OU `depth_estimation`** (sinon la calib profondeur, produite mais **ignorée**, rendait l'usage 4 inerte) ; le gate `tasks.py` ne calculait la calib que si **absente** → recalcule désormais quand la **source stockée diffère de la voulue** (basculer ⚑ profondeur sur une session déjà calibrée en homographie n'était pas pris en compte). `store_ground_calib` **estampe `source`** (`'depth'`\|`'homographie'`) dans `ground_calib[pos]`. | Recadrage Fabien 2026-08-05 : « la couche de calcul doit être une **fonction du tronc commun** et **rejoindre le catalogue** » (sinon invisible du catalogue/`/model-manager/functions/`/Studio) + « l'analyse profondeur doit **se greffer au bon endroit** du pipeline ». La géométrie posée dans `utils/` violait la centralisation et n'était pas chaînable ; les 2 verrous rendaient le flag global partiellement inerte. | `py_compile` OK (8 fichiers) · `manage.py check` 0 issue · catalogue = **4 entrées profondeur** (binding pure/app confirmé), `DataType.DEPTH_MAP` résout. ✅ **Test PUR CPU** (`scratchpad/test_depth_geometry.py`, aucun GPU) : plan de sol synthétique pitch **+8,00° / hauteur 1,501 m** récupérés (fit direct **et** round-trip carte de profondeur), contact-sol exact → **la convention de signe du pitch `atan2(nz,-ny)` est désormais VALIDÉE analytiquement** (levée de la réserve du commit précédent). ⚠ Reste **NON fumé GPU/navigateur** : l'API `transformers` Depth Pro réelle, la qualité de profondeur sur images ENA et le **gain `placement_spread`** se valident côté runtime/R760xa. Annulation : `git revert <commit>` (flag OFF par défaut). |
| *(ce commit)* | **Profondeur DÉCOUPLÉE en 3 ÉTAGES + rendue VISIBLE/CONTRÔLABLE dans le pipeline + stockage db propre.** (1) **Passe `depth` déclarée** (`PassType.DEPTH`, `models.py` + `order` de `pass_tracking.py`) → **session-wide** (hors `_PER_CAMERA_PASSES`, UNE ligne pour les 4 caméras, comme demandé « pas de sous-division »). Câblée `views.dispatch_map['depth'] → compute_depth_task` (`tasks.py`, `_console`/`mark_*` ; nommée `compute_*_task` comme la famille des passes). **ÉTAGE 2 exposé en passe `depth_calc`** (`compute_depth_calc_task`, session-wide, juste après `depth`, avant `global_tracking`) = le bouton « Calculs profondeur » : relit `DepthFrame` → `store_ground_calib` (plan de sol source='depth') + `depth_distance_report` (cross-check), CPU, re-jouable sans re-inférer. (2) **ÉTAGE 1 ANALYSE** (`depth_estimator.run_depth_analysis`, GPU) : UNE inférence Depth Pro échantillonnée (≤24 frames/cam, `is_available` gardé), STOCKE la donnée brute — cartes downsamplées (long-side ≤384, **float16 `.npz` sur disque** via `DepthFrame`+`depth_output_dir`, focale **déjà mise à l'échelle** de la carte pour déprojection auto-cohérente) + profondeur de contact `depth_distance_m` écrite en **champ additif** du JSON `DetectionFrame.detections` (zéro migration pour cette part). (3) **ÉTAGE 2 CALCULS** (CPU, re-jouable SANS ré-inférence) : `estimate_ground_plane_ph` **relit** `DepthFrame` → déprojette la zone roulable (rasterisée à l'échelle carte) → RANSAC plan ; `depth_distance_report` **pure lecture** du champ stocké. Aucun `import torch/cv2` → **sûr sous WSL2/CPU**. (4) **ÉTAGE 3 AFFICHAGE** = le flag global `depth_estimation` (consomme le plan profondeur vs homographie ; overlay différé, carte déjà stockée pour l'alimenter). (5) **Catalogue** : nouveau spec `cam_analyzer.depth_analysis` (DETECTOR, sortie `DEPTH_MAP`+`detections.depth_distance_m`), `depth_ground_plane`/`depth_distance_report` reclassés `cpu_bound` lisant `DEPTH_MAP`. Modèle `DepthFrame` + **migration 0022** (gitignorée). | Recadrage Fabien 2026-08-05 : « je ne vois pas l'analyse dans le pipeline » (elle était greffée DANS `global_tracking`, invisible) + « **il y a 3 étapes : analyse, calculs, affichage/overlay** — l'analyse d'abord, les calculs ensuite » + « bien gérer le stockage db ». Le découplage rend les CALCULS re-jouables sans re-payer le GPU et **exécutables sous WSL2** (l'inférence seule reste côté runtime). Stockage cartes+contact (décision « tester le maximum » : overlay + véhicules stationnés/occlusion à traiter finement plus tard exigent la carte, pas seulement le scalaire). | `py_compile` OK (5 fichiers) · `makemigrations` → **0022** · `manage.py check` 0 issue · `migrate` OK (**base live WSL2**) · smoke import OK · catalogue **31 specs**, 5 clés profondeur + `DEPTH_MAP` résolvent. ⚠ **NON fumé GPU/navigateur** (WSL2 GPU interdit) : l'inférence Depth Pro réelle, la qualité et le gain `placement_spread` → **côté runtime/R760xa** (lancer la passe `depth` du volet PUIS les calculs). ⚠ Base **Windows** (venv_win) à recaler au prochain `migrate` si utilisée. Flag `depth_estimation` **OFF** par défaut. Annulation : `git revert <commit>` (`DepthFrame`/`depth_distance_m` additifs, ignorés du reste ; flag OFF → aucun placement changé). |
| *(ce commit)* | **Volet droit — « Indicateurs » PROMU en passe pipeline `indicators` + pipeline SCINDÉ visuellement en 📷 Analyse / 🧮 Calculs.** (Q1+Q2 validés par Fabien : « regrouper tout ce qui est analyse + calculs dans le pipeline et le scinder visuellement ».) (1) **Nouvelle passe `PassType.INDICATORS`** (`models.py`) — session-wide (hors `_PER_CAMERA_PASSES`, UNE ligne), insérée dans `order` après `global_tracking`, avant `conflicts` ; `_DEPENDS_ON=['global_tracking','distance']`, `_WATCHED=[]`. La tâche `annotate_prediction_task` est **RENOMMÉE `compute_indicators_task`** (famille `compute_*_task`) et **câblée pass_tracking** (`mark_started/completed/failed`) + `views.dispatch_map['indicators']`. CPU pur (tracks 360° + TTC/PET, `_run_global_tracking` + `annotate_prediction_indicators`), **re-jouable, sûr sous WSL2**. La vue legacy `prediction_annotate` et `function_specs.py` pointent la nouvelle tâche. (2) **Bouton `#predictionCalcBtn` « Indicateurs » RETIRÉ** du groupe map-card (`base.html`) + handler onclick mort supprimé (`index.js`) — l'action passe désormais par le ▶ du pipeline. Le groupe map-card, qui mêlait Indicateurs + Comparer + Live sous un label `Analyse` trompeur, est relibellé `Comparer / Live` (**Comparer/⚑ et Live/temps-réel INCHANGÉS** — handlers `featModesBtn`/`liveAnalysisBtn` intacts, vérifiés). (3) **Scission visuelle** : champ `stage` (`'analyse'`\|`'calcul'`) posé par `pass_tracking._STAGE` sur chaque passe de `get_passes_status()` ; `index.js` groupe le rendu du pipeline sous deux sous-en-têtes **📷 Analyse** (extraction, fenêtres, yolo, drivable+lanes, sam3, profondeur) et **🧮 Calculs** (évènements, segments, distance, calculs profondeur, tracking 360°, indicateurs, conflits) via un helper `_rowHtml(p)` — markup de ligne identique, aucun autre changement de rendu. | Un calcul (les Indicateurs) posé HORS pipeline, dans l'encart « Analyse » de la map-card, était incohérent avec le modèle mental (analyse → calculs dans le pipeline). Fabien a validé Q1 (promotion en passe) + Q2 (scission visuelle) ; Q3 (dé-flotter ⚑ Comparer) et Q4 (lecture chiffrée des indicateurs) restent différés. | `py_compile` OK · `check_js.sh` **51 fichiers / 0 erreur** · **migration 0024** (choix INDICATORS) créée + `migrate` OK (**base live WSL2**) · `manage.py check` 0 issue · statics copiés + gunicorn HUP. ✅ **Smoke navigateur Playwright RÉEL effectué** (`scratchpad/pw_verify.py`, `vd_after.png`) : pipeline rendu **📷 ANALYSE puis 🧮 CALCULS** dans le bon ordre, `#predictionCalcBtn` **absent** (False), groupe map-card = « Comparer / Live », **0 erreur console imputable** (2 « Failed to fetch » transitoires = abort de navigation dans `loadAllDetections`/`loadAnalytics` non touchés). ⚠ Tâche `compute_indicators_task` **CPU-safe non lancée en réel** (déclenchable via le ▶ du pipeline). Annulation : `git revert <commit>` + retrait du choix `INDICATORS` (passe additive, aucun placement changé). |
| *(ce commit)* | **Section EXPLORATOIRE ajoutée à `CAM_ANALYZER_CHAINE_TRAITEMENT.md` : profondeur monoculaire ([E]).** Documentation seule — **aucun code, aucun modèle installé, aucune bascule**. Cinq usages instruits : (1) discriminer les **reflets** par la discontinuité de profondeur au contour — un objet réel crée une marche, un reflet est dans le plan de la surface réfléchissante donc le champ reste continu ; (2) **statique vs mobile** en confrontant l'évolution de profondeur à celle prédite par l'ego-motion ; (3) **confrontation au pinhole**, réciproque : la profondeur couvre les cas où `H_classe` est fausse ou la bbox tronquée, le pinhole fournit l'ancrage d'échelle qui manque au monoculaire ; (4) **re-calage du plan de sol** ; (5) **ordre d'occlusion** pour la continuité du tracking. | Piste ouverte par Fabien (2026-08-05). Elle vise **deux symptômes déjà documentés ici** : la limite connue **n°7** (« reflets fantômes géants » du vitrage latéral, non couverts par `artifact_filter`, où l'analyse de transparence était la seule piste) et la cause racine du 2026-07-15 qui listait « fantômes, stationnés » comme héritant du bruit ±20 % de la position pinhole. Deux vérifications ont levé les objections que j'anticipais : le rig est fait de caméras **perspectives** (61°/31°), pas d'un capteur équirectangulaire — les modèles monoculaires s'appliquent sans reprojection ; et `depth-anything/DA3METRIC-LARGE` est **métrique et Apache-2.0**, donc sans question de licence. | **Rien à valider : aucun comportement modifié.** Les limites sont consignées dans la section elle-même — portée utile ~15-20 m (au-delà de 30-40 m le signal ne vaut plus grand-chose), coût d'un modèle de plus par image, dépendance aux intrinsèques (la même erreur de FOV qui a coûté ×3,6). Le jour où ce sera implémenté : A/B à **métrique chiffrée**, jamais visuel seul, via `manage.py bench --task depth-estimation` contre la distance pinhole sur séquences à occlusion connue. Annulation : suppression de la section. |

---

## 2026-07-29

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(ce commit)* | **RENDU de la correction ortho** : `_orthoOffsetAt()` (port fidèle de `offset_at()` — interpolation pondérée par le nombre d'appariements, **sans extrapolation** hors bornes) + `_applyOrthoCorrection()` appliqué au **point d'ingestion UNIQUE** de la trace (`cachedGpsTrack` dans `renderMiniMap`), donc propagé à la trace, la pose ego et les projections caméra sans toucher aux consommateurs. Gardé par `camFeat['ortho_correction']` : bascule OFF ⇒ trace BRUTE même si `ortho_correction` traîne dans `results_summary` (sinon l'A/B mentirait). `lat_raw`/`lon_raw` conservés par point. | Les ancres étaient calculées et stockées mais l'affichage consommait toujours `gps_track` brut : basculer ⚑ ne déplaçait rien, l'A/B restait chiffré et invisible. | Délimiteurs équilibrés (identique à HEAD : {}=1443/1443, ()=3993/3993, []=614/614). `camFeat` alimenté depuis l'API des bascules (l.482) donc la nouvelle clé y arrive. ⚠ **AUCUN runtime JS sur l'hôte** (node/deno/bun absents) → **validation navigateur À FAIRE**. Annulation : bascule OFF. |
| *(ce commit)* | **RÉGRESSION CORRIGÉE : pane Leaflet custom retiré.** Le `lanePane` introduit plus tôt dans la journée faisait **disparaître la route overlay ET la zone violette** : la mini-carte est pivotée (`setBearing`) et le plugin de rotation ne transforme que les panes standard. Remplacé par `miniMapLaneLayer.eachLayer(l => l.bringToBack())` en fin de rendu — pur Leaflet, même effet de z-order. | Signalé par Fabien après Ctrl+F5. Leçon : sur une carte pivotée, **ne jamais créer de pane personnalisé** ; l'ordre s'obtient par `bringToBack`/`bringToFront`. | Les 9 sites d'injection revenus à leurs options d'origine (diff relu intégralement). ⚠ Même limite : pas de validation JS automatisée possible sur cet hôte. |
| *(ce commit)* | **RECLASSEMENT vers `common/` + manifestes** (correction d'une dette introduite la veille). `utils/ign_vector.py` → **`wama_data/functions/geo/ign_vector.py`** (nouveau domaine `geo/`) avec 3 FunctionSpec déclarées (`ign_buildings`, `ign_roads`, `sky_mask`) ; `utils/ortho_apply.py` → **`wama_data/functions/driving/trajectory_offset.py`** avec sa FunctionSpec, `build_anchors` généralisé (`landmarks`/`mask_by_key`, accepte `ts` ou `t_enter`/`t_exit`) ; `_proxies` extrait en **`wama/common/utils/http_proxy.py`** (`ortho_markings` délègue). **Étape 2b SÉPARÉE en deux fonctions** : `cam_analyzer.ortho_recalage` (MESURE, vision+GPU) et **`cam_analyzer.ortho_correction`** (APPLICATION, calcul pur) — nouvelle tâche `compute_ortho_correction_task`, bascule renommée `ortho_recalage` → **`ortho_correction`**. Sortie `ortho_correction` désormais DÉCLARÉE. | Deux modules purs et réutilisables avaient été écrits dans l'app, sans manifeste : invisibles du `FUNCTION_CATALOG`, de `/model-manager/functions/` et du Studio, donc inutilisables ailleurs — contraire à la règle de centralisation et à la philosophie « tout fonctionne par description ». Symptôme révélateur : `ign_vector` importait `_proxies` DEPUIS l'app (dépendance inversée). La séparation mesure/application vient d'un besoin concret : recalibrer `full_trust_mask_deg` relançait sinon la segmentation SAM3 des tuiles ortho (GPU + réseau) pour un simple calcul pur. | **29 tests OK avant ET après déplacement** (`python3 wama_data/functions/driving/tests_trajectory_offset.py`) — ils ont d'ailleurs attrapé le renommage `mask_by_window`→`mask_by_key`. `manage.py check` : 0 issue. Catalogue vérifié : **24 fonctions**, les 4 nouvelles pures en `binding=pure`, et **chaînage validé** (port `recalage` : sortie de la mesure = entrée de l'application). Bascule : ancienne clé absente, nouvelle présente. Annulation : `git revert` (bascule OFF par défaut, aucun comportement actif changé). |
| *(ce commit)* | **Route + vue `ortho_correction`** (`/lab/cam-analyzer/api/sessions/<id>/ortho-correction/`) et **z-order de la mini-carte corrigé** : pane dédié `lanePane` (z-index 350 < overlayPane 400), les 9 polygones de voie/branche/marquage y sont rattachés. | La tâche `compute_ortho_correction_task` avait été livrée **sans vue ni route** : déclarée au catalogue mais impossible à lancer. Et le calque des voies était créé en DERNIER, donc dessiné AU-DESSUS des véhicules et de la trace — la bande d'intersection masquait précisément les objets à observer (signalé par Fabien). La géométrie de voie est du CONTEXTE : sa place est sous les objets. | Routes résolues (`wama_lab:cam_analyzer:ortho_correction`). `manage.py check` : 0 issue. ⚠ Le JS n'a PAS pu être validé par `node` (absent de l'hôte) — contrôle visuel des 9 sites d'injection, dont **un rendu invalide par l'édition automatique (double virgule l.2980) corrigé avant commit**. **Validation navigateur RESTANT À FAIRE** (rechargement forcé). |
| — | *Notes (dette déclarée, pas masquée) :* **(1)** `FunctionCategory` n'a pas de valeur `SOURCE` ; les fetchers IGN (aucun port d'entrée, paramétrés par un point) sont déclarés `TRANSFORM` faute de mieux — à trancher si d'autres sources apparaissent. **(2)** `cam_analyzer.ortho_correction` est en `Binding.APP` parce que la tâche lit/écrit la session, alors que **toute sa mécanique est déjà pure** (`common/.../trajectory_offset.py`) : il ne manque qu'un adaptateur de ports pour la porter en `PURE` et la rendre chaînable dans le Studio. C'est le candidat le plus simple pour étrenner ce portage. | | |

## 2026-07-28

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(ce commit)* | **Bascule ⚑ `ortho_recalage` (OFF par défaut, `scope='compute'`)** — l'étape 2b est désormais APPLICABLE en comparaison A/B, comme toute autre amélioration du positionnement. Câblage dans `compute_ortho_recalage_task` : calcul du masque satellite BD TOPO par fenêtre d'intersection, décomposition, ancres, et stockage de `results_summary['ortho_correction']` (ancres + biais caméra + masques + rapport chiffré). **Seules les ancres sont stockées** — pas de trace GPS dupliquée : tout consommateur rejoue `offset_at()`. Console enrichie : biais caméra écarté, nombre d'intersections, décalage moyen/max, atténuation moyenne. | La bascule était la condition posée par le module lui-même (« application derrière une bascule après validation visuelle »), et la règle du projet impose qu'une amélioration comparable passe par un flag déclaré, jamais un `if` ad hoc. Le panneau ⚑ Modes se génère depuis le registre : aucune UI à écrire. Corrige aussi un mode de défaillance de `build_anchors` : masque ABSENT (BD TOPO injoignable) valait `alpha=0`, donc annulation silencieuse de toute la correction — désormais `alpha=1` (seule une valeur mesurée peut atténuer). | Registre validé : 13 bascules, clés uniques, `ortho_recalage` défaut OFF, `resolve()` → False par défaut / True sur surcharge. Syntaxe des 4 fichiers OK. **29 tests unitaires OK** (3 ajoutés sur le mode de défaillance BD TOPO). Non bloquant : `try/except` autour de tout le bloc, la mesure reste intacte en cas d'échec. Annulation : bascule OFF (défaut), ou `git revert`. |
| *(ce commit)* | **Correction de trajectoire par recalage ortho — `utils/ortho_apply.py`** *(chemin renommé depuis → `wama_data/functions/driving/trajectory_offset.py`, relevé le 2026-09-11)* + tests `tests_ortho_apply.py`. `decompose()` sépare **biais caméra** (médiane globale, constant) et **biais GPS local** (écart par intersection) ; `build_anchors()` ancre les offsets locaux au milieu de chaque traversée et les **rétracte selon l'ouverture du ciel** (`ign_vector.sky_mask`, plein effet au-delà de 12° d'élévation moyenne bouchée) ; `offset_at()` interpole entre ancres pondéré par le nombre d'appariements, **sans extrapoler** hors bornes ; `correct_track()` produit une trace corrigée en conservant `lat_raw`/`lon_raw` + `corr_de_m`/`corr_dn_m`. **Fonctions pures, encore branchées nulle part** — la bascule d'activation reste à poser. | `ortho_recalage` mesurait un offset sans jamais l'appliquer (v1 = rapport seul, application prévue « derrière une bascule après validation visuelle »). L'offset mêlant biais caméra et biais GPS, l'appliquer en bloc à la trajectoire aurait absorbé une erreur d'étalonnage dans la position. La décomposition exploite leur signature spatiale : le constant est caméra, le variable est GPS (canyon urbain). La rétraction par masquage évite d'injecter du bruit d'appariement là où le ciel est dégagé. | **26 tests unitaires, 26 OK** (`python3 wama_lab/cam_analyzer/tests_ortho_apply.py`, sans Django ni GPU) : décomposition, ancrage, rétraction alpha, interpolation, maintien hors bornes, robustesse (trace vide, point sans coordonnées, fenêtre hors index). **Signe vérifié explicitement** avec amplitude : offset est négatif → longitude diminue de 5,00 m ; `dn` positif → latitude augmente de 8,00 m (dérivation documentée dans le module — une inversion serait silencieuse). Annulation : `git revert` (aucun consommateur). |
| *(ce commit)* | **Données vectorielles IGN — `utils/ign_vector.py`** *(chemin renommé depuis → `wama_data/functions/geo/ign_vector.py`, relevé le 2026-09-11)* (BD TOPO V3) : `fetch_buildings()` (emprises + `hauteur`/altitudes sol-toit/étages), `fetch_roads()` (tronçons + nature/sens/largeur), `sky_mask()` (masque d'élévation par azimut, 72 secteurs) et `mask_summary()`. Cache mémoire par bbox. **Règle posée** : ce que l'IGN publie en VECTEUR n'est pas détecté visuellement — bâtiments et routes viennent de BD TOPO ; seuls les marquages au sol (absents de toute base vectorielle) restent du ressort de la détection. **Aucun chemin existant modifié** (module purement additif, encore appelé par personne). | Le biais GPS latéral (façades à l'est) était jusqu'ici constaté, jamais expliqué : `ortho_recalage` mesure un offset global sans savoir d'où il vient. Les hauteurs BD TOPO permettent de calculer le masquage satellite réel et donc de **pondérer** le recalage au lieu d'appliquer un offset aveugle. Détecter visuellement des bâtiments alors que l'IGN les publie au décimètre avec leur hauteur serait strictement moins bon. | Smoke réel 2026-07-28 (Lyon centre 45.7578/4.8320, rayon 200 m) : **266 bâtiments** dont 264 avec hauteur (max 30,7 m), **112 tronçons**, masque moyen 9,5° / max 16,5° à l'azimut 20°, cache 2ᵉ appel 0,001 s. **Contrôle négatif** zone rurale (45.05/3.20) : 0 bâtiment, masque 0,0° → pas de masquage fabriqué. Piège vérifié : le WFS IGN attend le bbox en **lon,lat** (l'ordre lat,lon renvoie 0 entité *sans erreur*). Annulation : `git revert` (aucun consommateur à défaire). |

## 2026-07-21

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| *(doc)* | **Refactoring documentation** : 6 docs → **3 piliers** (README/CHAINE/CHANGELOG) + `projects/ENA_CASA.md` (spécificités projet façonnées manifeste). `DISTANCE_DESIGN`/`TOPDOWN_STATUS`/`CONTEXT` **archivés** (`archive/`) après absorption. Correction dérive CHAINE (date + étape [4] + 2b). Consignation des gaps G7/G8/G2/G4. | La carte de référence (CHAINE) avait dérivé derrière le changelog ; docs redondants → mises à jour multiples à chaque changement. App générique vs spécificités projet mélangées. | Aucun code touché ; `git revert` du commit doc. Contenu vivant préservé (absorbé), reste dans `git log` + `archive/`. |

## 2026-07-20

| Commit | Quoi | Pourquoi | Validation/annulation |
|---|---|---|---|
| `4069c82` | **Passes détection débloquées après complétion** : `mark_completed(yolo_detect/yolopv2_lanes)` avant le `continue` « caméra déjà couverte » | Après « Compléter l analyse », front/left/rear restaient « en cours » — le continue sautait le mark_completed de fin de boucle, la passe restait started | 8 passes réparées (running/failed→completed) ; restart WSL pour charger le fix |
| `6266444` | **Mini-carte ne fige plus** (rafLoop try/finally re-planifie toujours + validateur `_okLL` coords) + **declutter marquages** (stop_line/crossing ≤18 m seulement, « line » bruitées retirées) + **point rouge = antenne** (`antennaPoint`, arrière-droit) | Figement du playback près des marquages (throw dans le dessin tuait la boucle rAF) ; marquages « n'importe comment » ; point rouge lu comme l antenne mais au centre | Coords stockées valides (0/392 hors zone) → figement = redraw+fragilité ; représentation corrigée, chaîne juste |
| `fd8998d` | **Filtre fantômes géants** (`is_giant_reflection` : bbox >50 % image + conf <0,55) — tracker (exclusion, ⚑ artifact_filter) + JS (masquage immédiat vidéo+top-down) | Le critère cinématique ne captait pas les reflets géants fragmentés (bbox 90-99 %, conf basse) | Vérifié reflet arrière 595 s : 22/32 dets captées, vrais véhicules épargnés |
| `552dd24` | **Levier antenne sur la route virtuelle** (gabarit de voie `_sm` + marqueur initial via `antennaCorrect`) + boutons zoom mini-carte descendus (30 px) + **toolbar en 3 sections encadrées** (Calibration/Vue/Analyse) | Navette dessinée sur la ligne centrale : le gabarit de voie utilisait le GPS BRUT (antenne) alors que navette+objets sont au centre véhicule ; zoom masqué par le badge ; toolbar plate brouillonne | Audit chaîne complet (tracker/prediction/marking_world/branches/estimator déjà corrigés, origines cohérentes) ; déplacement 1,00 m vérifié ; tous boutons conservés (IDs) |
| `034912c` | **Étape 2b — recalage absolu ortho** (`ortho_markings.py`) : mosaïque tuiles IGN z19 + géo-transform Web Mercator exacte + SAM3 passages piétons sur l ortho + matching avec crossings caméra → offset par intersection ; tâche + bouton « Recalage ortho » + tracé orange sur mini-carte | L échelle/position absolue que 2a (angle) ne peut donner ; passages piétons ortho = vérité géoréférencée | Vérifié : géo-transform 0 cm, SAM3 2 crossings/intersection (conf 0,7), offset global 2,93 E/4,2 N m (~5,1 m) ; RAPPORT seul (offset non appliqué) ; validation visuelle ortho à faire |
| `48b46df` | **Étape 2a — placement par projection sol** (⚑ auto_ground_calib OFF) : `store_ground_calib` (pitch/hauteur par caméra, filtre qualité ≥6 obj + étalement ≤2,5 m) + `ground_ego`/`ground_projector_for` + branchement tracker avec fallback pinhole + auto-calcul dans _run_global_tracking | Intégrer le gain d angle (×5) derrière bascule pour A/B, sans casser le pinhole | avant 22°/1,53 m + droite 26,5°/1,6 m RETENUES, arrière/gauche REJETÉES (>2,5 m) ; tracking flag ON = 5212 tracks world_en 100% ; défaut OFF restauré ; A/B visuel ortho à faire |
| `8a19577` | **Estimator pitch×k1** (hauteur fixée physique, distorsion radiale testée) | Vérifier si le résiduel 3 m vient des dômes AXIS 110° non rectilinéaires | k1 SATURE la borne (+0,45) sans gain (désaccord 3,05→3,05) → PAS le levier ; le pitch reste le vrai gain (14,55→3,05 m ×5). Résiduel = ancrage pinhole + bruit bbox. Suite : intégrer le pitch seul derrière bascule pour A/B |
| `51b8b95` | **Branches resserrées** (span≥14 m, centreligne <8 m de la balise, ≥4 véh.) + **homography_estimator v1** (rapport seul : solve pitch/hauteur par étalement monde des statiques + ancrage pinhole) | Validation ortho : nappes diagonales = manœuvres de parking ; placement objets — mesuré baseline désaccord sol⟷pinhole 14,55 m → 3,0 m à pitch 21,5°/h 2,2 | RIEN branché sur le positionnement (flag futur) ; prochain levier = distorsion k1 |
| `7a2b36f` | **Bouton 🧭 cap navette en haut** (leaflet-rotate vendorisé, throttle 1,5°, persisté) + **Leaflet vendorisé local** (unpkg → vendors/, conformité assets locaux) | Demande utilisateur : mini-map orientée comme la vue caméra, façon GPS | esprima+check OK ; validation navigateur à faire |
| `91d85f7` | **Marquages SAM3 en monde** (⚑ world_markings, `marking_world.py`) : projection bord-bas 2-12 m via GroundProjector EXISTANT, agrégation multi-passages (amas+fusion+top-K), reclassement géométrique ∥corridor→line, affichage mini-map typé | Idée utilisateur : stop_line/crossing = bornes réelles d intersection + branche sans trafic ; 3 itérations mesurées (1498→196 segments, PCA corridor→bord bas) | stop_line 2e intersection à 74°⊥ (153 fr.) ✓ ; validation visuelle ortho à faire ; étape 2 = fusion + homography_estimator |
| `6053185` | **Interpolation SAM3 entre keyframes** (⚑ sam3_interp : morphing translation+échelle apparié par label+centroïde, fondu entrée/sortie, 2 chemins de rendu) + **cadence au profil** (`sam3_fps`, migration 0020 des 2 côtés, éditeur de profil) | Marquages visibles 1 frame/6 (clignotement) ; densifier SAM3 = coût ×2 du modèle le plus lourd — l interpolation est gratuite et exacte aux keyframes | py_compile+esprima+check OK ; validation visuelle navigateur à faire (virages = cas limite documenté) |
| `edd269e` | Branches **mutualisées par intersection physique** (lat/lon ~1 m près : 1 analyse/lieu, partagée entre passages) + ortho IGN `maxNativeZoom` 19 | Passages multiples du même carrefour → jeux de branches contradictoires superposés (« croix ») ; tuiles z20 absentes par endroits → fond vide | 28 fenêtres → 2 jeux distincts ; session réparée après restart pendant complétion |
| `0ead425` | **Levier antenne GPS** (⚑ antenna_lever, coin arrière droit ENA, config gps_antenna + 2 champs Calibration, appliqué Python+JS) + **branches apprises du trafic** (⚑ learned_branches, `intersection_branches.py`, filtre rectitude, 2 dominantes ≥3 véh., dessinées à la place de la bande symétrique, fallback conservé) + **fond ortho IGN 20 cm** (bouton 🛰 persisté) | Repère calé ~1 m à droite (GPS=antenne≠centre) ; bande perpendiculaire aveugle des 2 côtés ; largeurs de fond raster symboliques | Mesuré : déport 1,00 m ; 56 branches/28 fenêtres ; run 197 s 4631 tracks ; validation navigateur à faire (relancer Indicateurs pour recaler les world_en avec l antenne) |

## 2026-07-19

| Commit | Quoi | Pourquoi | Validé |
|---|---|---|---|
| `4672900` | **Anti-empilement live** : verrou de spawn atomique (cache.add 15 s) dans `live_cursor` + cooldown 30 s post-préemption (sortie silencieuse) dans la tâche | 1440 messages mesurés en file gpu (1427 live empilées par le POST curseur pendant le blocage) → drainage = 10 min de spam + watchdog re-déclenché | File assainie à chaud (LREM ciblé, 1433 retirés, 1 complétion + 1 indicateurs conservés) |
| `d0c70e9` | **Préemption batch > Live** : `_pause_live` avant chaque dispatch GPU, boucle live rend le slot si session PENDING/PROCESSING (check 3 s), `live_cursor` → 409 si batch en cours (le JS coupe le bouton), lancement batch coupe le Live côté client ; raisons d arrêt user/preempt (pas d enchaînement sur préemption) ; la complétion enchaîne elle-même le tracking global | Starvation : le Live persistant occupait l unique slot worker GPU → complétion > 5 min en file → watchdog « worker mort » → session failed → UI vide (diagnostic utilisateur exact) | session réparée (failed→completed, données intactes) ; zombie live à l ancien code → RESTART Celery requis |
| `a48fab0` | **Filtre artefacts cinématique** (⚑ artifact_filter, marqué jamais supprimé, exclu tracking+affichage) + **cap serveur des ancres** (consensus axial ratio sur toute la vie du track, ⚑ anchor_heading) + **prior cluster** (voisins <15 m, ⚑ heading_cluster) | Chantiers 1-2-3 validés en discussion : reflets détectés comme véhicules, cap des garés instable par frame | 91 artefacts marqués, 31/31 ancres avec cap (160,3°=axe rue) ; LIMITE : reflets « fantômes géants » fragmentés non couverts → raffinement candidat à venir |
| `699b072` | **Enchaînement auto fin de Live** : `_run_global_tracking` (cœur partagé) + passe pipeline « Tracking 360° » (GLOBAL_TRACKING, migration 0019 des 2 côtés) + arrêt Live immédiat (flag stop) + poller JS qui recharge détections et données dérivées à la fin | En sortant du Live, les nouvelles détections restaient en affichage « repli » jusqu à un « Calculer les indicateurs » manuel (validé utilisateur : auto pour le tracking ~90 s, TTC/PET reste manuel) | smoke test : 2844 tracks, passe visible completed ; navigateur à valider |
| `90ac4b1` | **Trajectoires fusionnées Kalman+RTS** : `trajectory_smoother.py` (générique, testé ×4,9) ; tracker écrit `world_en` lissé sur chaque détection (+fantômes) ; affichage = hiérarchie ancre > world_en > reconstruction (gardes par-frame réservées au repli) | Disparitions en vue de dessus pendant les manœuvres (G15) : reconstruction par frame/caméra + gardes anti-bruit jetaient l objet tracké | re-run tracking 93 s : 99 % des dets trackées du dépassement avec world_en, chaînes continues ; navigateur à valider |
| `d61652e` | **Routes perpendiculaires aux intersections** en vue de dessus : bande magenta ancrée sur la balise (repère GPS), orientée ⟂ au bearing_deg de la fenêtre, longueur 2×rayon, largeur 2 voies + axe pointillé | La route croisée n était portée que par les tuiles OSM (qui flottent vs repère GPS) → balise perçue décalée alors qu elle est bien placée (Distance min 2 m cohérent) ; demande utilisateur | parse OK ; navigateur à valider |
| `92553ce` | **Fix Live : `playheadT()`** — pas de globale currentTime, 12 gardes `typeof currentTime` silencieusement morts (curseur Live à 0, merge jamais exécuté, re-renders immédiats yaw/FOV/bascules inertes) ; + bouton Live persistant (localStorage) | Live sans effet visuel malgré une tâche serveur qui produisait (+840 s de couverture mesurés en cache/base) | parse OK ; à re-tester en lecture |
| `411c817` | **Mode LIVE — analyse au fil de la lecture** (étape 3) : `live_analysis_task` (boucle curseur+lookahead 15 s, modèles gardés chargés, zéro GPU si couvert, sortie après 90 s d inactivité, parité d inférence batch) + endpoint `live-cursor/` + bouton ⚡ Live (curseur 1,2 s, fusion incrémentale des détections 3 s) | Combler visuellement les trous d analyse pendant la lecture (discussion utilisateur : jouable et pas trop lourd — même mécanisme que la complétion, playhead = curseur de priorité) | compile/parse/route OK ; run GPU réel à déclencher (bouton) |

## 2026-07-18

| Commit | Quoi | Pourquoi | Validé |
|---|---|---|---|
| `6b43a0b` | **Complétion d analyse batch** (étape 2) : `process_session_task(completion_scope)` analyse les seules tranches manquantes via l itérateur fenêtré ; pas de wipe, pas de vidéo annotée ; `ignore_conflicts` aux bords ; endpoint `complete-analysis/` + boutons panneau pipeline avec ligne de couverture | Une session analysée « restreinte » ne pouvait produire un rapport plein-parcours qu en ré-analysant tout ; design complétion validé avec l utilisateur | compile/parse/route OK ; run GPU réel à déclencher via le bouton, puis « Indicateurs » pour souder |
| `6d518e5` | **Registre de couverture** (étape 1 analyse incrémentale) : `intervals.py` commun (testé) + `coverage.py` (get/add/reset/missing/rebuild_from_db) + consignation en fin de passe caméra + reset au wipe ; registre reconstruit pour les sessions existantes | Fondation de la complétion batch + du futur mode « analyse au fil de la lecture » | tests unitaires intervalles OK ; mesuré : sessions ENA couvertes à 22 %, 6512 s manquantes en 19 tranches pour le scope complet |
| `269cc1d` | **Éditeur de profil scindé** : modèles d ANALYSE (yolopv2 4 vues, modèle route, SAM3) toujours visibles (analysisModelsSection) ; section intersections = restriction + CRUD/carte seulement. + design « analyse incrémentale par complétion » consigné doc chaîne (scope=union des rapports, registre de couverture, coutures réparées par tracker 360°, vidéo annotée non patchée) | Le profil Dépassements ne pouvait pas configurer de modèle route (champs génériques enfermés dans la section intersections masquée) ; question archi utilisateur analyse vs rapport | IDs vérifiés par script ; navigateur à valider |
| `818036c` | **Doctrine calculs génériques** : YOLOPv2 gouverné par road_model_path (plus par report_type), fenêtres par intersections déclarées ; seule la couche segments/conflits/rendu branche sur le type de rapport ; profil Dépassements doté du modèle route (jamais eu) | Le rapport dépassements devait hériter de toutes les améliorations — calculs généraux, seule la sortie diffère (demande utilisateur) | compile+check OK ; ré-analyser une session dépassements pour valider |
| `41bef1a` | **Fenêtres d intersection converties en TEMPS VIDÉO** ((ts−offset)/scale dans recompute_intersection_windows) — convention : intersection_windows est en temps vidéo | Fenêtres bornées en temps GPS mais consommées en temps vidéo partout → décalage linéaire (scale 0,96 : +6 s→+335 s), passages ratés par l analyse restreinte, saut-vers-passage avant le centre (balise « décalée ») | mesuré : 28 fenêtres corrigées, 16 étaient VIDES de détections → ré-analyse session nécessaire |
| `65d5f38` | **Stationnés ANCRÉS en position monde** : ancre = médiane des observations du track (tracker, `stationary_anchors` {gid: [lat,lon]} dans results_summary) ; l affichage dessine les garés à position FIXE (bypass reconstruction par frame) ; cap toujours fourni par le ratio-bbox | Hors de l axe caméra, erreur de position ∝ gisement (erreur focale × \|bcx−cx\|) : le gisement balayé au passage de la navette faisait décrire un arc aux garés → jitter + rotation (diagnostic utilisateur : seuls ceux DANS l axe étaient stables) | 18 stationnés → 18 ancres persistées, coordonnées plausibles ; rendu navigateur à valider |

## 2026-07-17

| Commit | Quoi | Pourquoi | Validé |
|---|---|---|---|
| `e81fdf2` | **Fantômes : héritage des mesures** — dist_euclid_m géométrique (tooltip + couleur distance), class_name = majorité du track ; cap déjà hérité via la trace commune ; vitesse/TTC volontairement non fabriqués | Fantômes muets (tooltip vide, cyan, classe de la 1re frame) — question utilisateur héritage cap/mesures | vérifié en base : dépassement car/car, xy cohérents, d 2,5-2,7 m |
| `3fdbb41` | **Reconstitution de trajectoire des mobiles** : détections coupées au bord mais trackées DESSINÉES (le « masque » au contact de la navette) ; comblement fantôme étendu (trous ≤ 6 s, interpolation entre mesures réelles, zones derrière/à côté incluses — filtre lon≤0 supprimé) ; stationnés EXCLUS du comblement (bloc déplacé avant) | Le véhicule disparaissait pile pendant la phase de dépassement (garde bord d image) et les trous de trajectoire restaient vides sur la minimap (fill 1,2 s, avant seulement) | validé : trou G242 (1182,8→1184,4 s) comblé par 18 fantômes tous au bon gid |
| `318ff21` | **Tracking 360° : verrou de chaîne** (un track YOLO par caméra garde son gid, NN réservé aux chaînes nouvelles) ; **mesures dégradées** (bbox coupée au bord : prolonger/rejoindre un track existant, jamais créer) ; **recollement de tracklets** (stitching cinématique post-passe, prédiction depuis un fit linéaire sur la queue saine de l historique, garés jamais fusionnés) | Dépassement G432 (arrière→gauche→avant) : gid churnait (#166 = 6 gids), le véhicule au flanc disparaissait (latérale 100 % coupée au bord), tracklets arrière/avant jamais recollés (fin de track corrompue à <3 m) | validé 3 runs complets session 4da52df3 : dépassement rear #161 + front #144 = gid 242 unique ; bus front→rear 19 s/7 tracklets = gid 254 ; zéro churn |
| `556973f` | Commentaire Django `{# #}` multi-ligne affiché en texte brut dans le panneau (remplacé par `{% comment %}`) + **ordre chronologique** : bouton Calibration EN PREMIER (étape 1), puis Vue, puis Analyse | Piège connu (4e récidive) introduit par la réorganisation `a859656` ; demande utilisateur d ordre chronologique du workflow | vérifié : 26 IDs, 0 `{# #}` multi-ligne restant |
| `a859656` | **Barre vue de dessus réorganisée par familles** : VUE (360°/Préd/Garés/Voie·vidéo + Conf/Voie) · ANALYSE (Indicateurs/Comparer/debug) · panneau CALIBRATION repliable (synchro GPS, Sync .rec, yaw+FOV, H cam, sol) ; « Modes »→« Comparer » ; bouton Yaw dédié retiré (redondant, champs dans le panneau) | 18 contrôles en vrac, débordements, bouton 💾 invisible ; demande de regroupement clair (option « calibration repliée » choisie par l utilisateur) ; 360°/Préd PAS fondus dans Comparer (sémantique affichage ≠ comparaison d algorithmes) | aucun ID perdu (vérifié script) ; navigateur à valider |
| `db66027` | **FOV latéral réglable par session** (config['camera_fov'], input « FOV lat. » dans l éditeur Yaw, V dérivé de la table F1015 52↔97°H/30↔53°V) — dist_scale s ajuste SANS ré-annotation ; **tooltip top-down systématique** (classe·G·distance corrigée·vitesse rel.·TTC — l ancien affichait dist_euclid_m, champ homographie souvent absent → tooltips vides/partiels) | Distances latérales incohérentes (7-9 m pour des garés à ~3-4 m) : la vari-focale F1015 est probablement restée au réglage LARGE ~97°H, pas 55° supposé — un FOV réel plus grand que supposé SURESTIME les distances (contre-calcul #247 : 7,39 m@31°V → 4,11 m@53°V) ; tooltips erratiques (constat utilisateur) | smoke test dist_scale 0.556 OK ; à comparer 55↔97 à l écran |
| `6b07648` | **Rafraîchissement de fin des passes légères** : poller dédié aux PASSES (grâce ~10 s, arrêt quand plus aucune n est running, puis rechargement détections+sessions+panneau) ; runPasses distingue analyse lourde (polling session) et passe légère (polling passes) | Les passes découplées ne touchent pas session.status → le poller de session s arrêtait au 1er tick (status resté completed), rechargeait les ANCIENNES données et ne rafraîchissait plus — état « en cours » figé (distance, conflits — constat utilisateur) | parse esprima OK ; navigateur à valider |
| `1697158` | **Passe « Distance / vitesse / TTC » exécutable seule** : `compute_distance_task` (ré-annotation sur détections stockées, sans YOLO, FOV V courants + trace `fov_v_used`) + entrée `distance` dans le `dispatch_map` de `run_passes` | Le ▶ de la passe était un **bouton mort** : `distance` absent du dispatch → skip silencieux (succès renvoyé, rien lancé) — constat utilisateur | compile + check OK ; à lancer côté UI |
| `64b5e0f` | **Bascules ⚑ Modes** : mécanisme générique `wama/common/utils/feature_flags.py` + registre `utils/features.py` + endpoint `features/` + panneau auto-généré (vue de dessus). 4 bascules : correction FOV distances, bras de levier, cap ratio (live, effet immédiat) ; vitesse/distance unifiées par track (compute, chantier déclaré). Appliquées en UN point par côté (`camera_geometry` backend, `rebuildCamGeo` JS). + consignation des 3 leviers (unification track / cluster stationnés / YOLO-OBB) dans la doc chaîne | Pouvoir comparer AVEC/SANS chaque amélioration instantanément (demande utilisateur) ; abstraction réutilisable pour WAMA Data ; jamais de if ad hoc dispersé | compile + parse + smoke test bascules OK ; navigateur à valider |
| `4e78e78` | **Stationné vitesse-aware** (durée ≥ 4 s ET étalement/durée < 0,7 m/s, en plus du plafond 6 m) ; **hand-off couture** : gap toléré 1→2,5 s + gate croissant avec le trou (3,5 m + 1,5 m/s·Δt) ; **fondu ratio↔trajectoire pondéré par la vitesse** (ratio seul à l'arrêt, trajectoire seule ≥ 2 m/s, mélange axial entre les deux) | Véhicules ROULANTS marqués « garés » (étalement < 6 m sur un track bref) → cap ratio appliqué à tort ; perte du G à la couture avant↔latérale (véhicule coupé au bord exclu ~1-2 s > gap 1 s, constat #537/G313) ; demande de pondération par la vitesse (utilisateur) | compile + parse OK ; re-lancer « Calculer les indicateurs » puis valider navigateur |
| `6146c7a` | Fragment résiduel `ent` après l'accolade fermante de `ratioHeadingCandidates` (introduit par `558194c`) → `ReferenceError: ent is not defined` au chargement, **toute l'app UI morte** (sessions bloquées sur « chargement ») | Résidu d'édition non détecté par le comptage d'accolades (`ent` est un identifiant valide) — parse esprima ajouté à la procédure de non-régression | syntaxe re-validée par parse complet |

## 2026-07-16

| Commit | Quoi | Pourquoi | Validé |
|---|---|---|---|
| `558194c` | **Cap ratio-bbox + fusion** : cap des stationnés/lents estimé par le ratio largeur/hauteur de bbox (étendue apparente → \|θ\| vs ligne de visée, 2 candidats mod 180° départagés par continuité, EMA axiale) ; trajectoire prioritaire quand l'objet bouge. + docs CHAINE_TRAITEMENT & CHANGELOG | Cap des garés figé = axe de la rue → faux en stationnement perpendiculaire ; demande de confrontation trajectoire↔ratio (utilisateur) | math validée numériquement (face→0°, profil→90°) ; rendu navigateur à valider |
| `3dab195` | **Classe stable par track** (vote majoritaire pondéré confiance, backend `stable_class` + fallback vote live JS) ; **persistance des bascules** 360°/Préd/garés/Voie·vidéo (localStorage) | Gabarit qui sautait car↔truck au gré des frames ; bascules perdues au rechargement de page | compile + rendu à valider |
| `79a0315` | **Cap indépendant du sens de lecture** : points de trace horodatés, vecteur orienté par le signe de Δt ; purge de trace sur seek > 2 s | En lecture arrière, tous les caps s'inversaient à 180° (trace remplie dans l'ordre de lecture) | rendu à valider |
| `43cf064` | **Géométrie réelle du rig ENA** : homographie DÉBRANCHÉE de la vue de dessus (latéral pinhole avec focale HORIZONTALE réelle 110°/55°) ; FOV V réels 61°/31° (`DEFAULT_FOV_V_DEG`) + correction rétroactive ×3,6 des distances latérales (`dist_scale`, `fov_v_used`) ; **bras de levier caméras** (antenne GPS à l'arrière, caméra avant +4,5 m) ; yaw latéraux défauts ±75° ; silhouette navette étendue vers l'avant ; H cam défaut 2,4 m ; overlays latéraux corrigés | **Décalage DROIT systématique** : calibration homographie prouvée cassée (inversion de signe #546, profondeur non monotone #537) — c'était elle qui décalait tout à droite ; specs caméras + schéma d'implantation fournis par l'utilisateur | smoke tests numériques OK (#537→+4,8 m/27,6 m ; #499 garée→−10,7 m hors voie) ; rendu à valider |
| `0f8d215` | **6 correctifs cap/vitesses/yaw/hand-off** : lissage circulaire du cap ego (±2 fixes) ; fix wrap 359→1° (`_shuttle_pose_at`) ; vitesse de track EMA + rejet >15 m/s ; yaw caméras configurable (bouton 🧭 + endpoint `camera-yaw/`) ; ID global `G<n>` + badge 🅿 sur les vidéos ; « rel. » sur la vitesse ; gel du cap des stationnés + seuil 0,4→0,8 m ; badge d'état minimap | Objets qui tournent sur eux-mêmes (cap ego GPS bruité amplifié par bras de levier + wrap plein nord) ; garés « roulant » à 1-3 km/h ; hand-off 360° cassé ; boutons à l'effet invisible | smoke tests wrap/yaw OK ; rendu validé partiellement (l'utilisateur a ensuite signalé le décalage droit → `43cf064`) |
| `1508935` | Fantômes prédits masqués quand Prédiction OFF ; latéral = homographie rescalée *(⚠ ANNULÉ par `43cf064` — l'homographie s'est révélée biaisée à droite)* | Vue de dessus incohérente avec les vues caméra ; « prédiction activée par défaut » | fix 1 conservé ; fix 2 remplacé |

## Historique antérieur (contexte)

| Commit | Quoi |
|---|---|
| `7327b19` | Lissage EMA de la distance top-down (mitigation du « ramassis ») |
| `6509cc2` | Passage au positionnement pinhole en vue de dessus (cause racine identifiée par l'audit du 2026-07-15 : latéral bruité) |
| `dbeb939` | Audit complet dégradation vue de dessus (`docs/AUDIT_CAM_ANALYZER_VUE_DE_DESSUS_2026-07-15.md`) |
| `c469867`, `9961a4c` | Étapes antérieures du chantier vue de dessus (voir audit) |

## Procédure de non-régression (à chaque modification)

1. Commit atomique + entrée dans ce tableau (quoi/pourquoi/validation).
2. `python -m py_compile` sur les fichiers Python touchés + `python manage.py check`.
   Pour le JS : parse complet via esprima (le simple comptage d'accolades ne détecte pas
   les fragments résiduels d'édition — cf. hotfix du 2026-07-17) :
   `python -c "import esprima; esprima.parseScript(open('wama_lab/cam_analyzer/static/cam_analyzer/js/index.js', encoding='utf-8').read().replace('?.', '.').replace('??', '||'))"`
3. Si formule géométrique : smoke test numérique sur un cas réel documenté
   (référence : frame 6176, session `8cecc4a6`, véhicules #537/#499/#546).
4. Sync `staticfiles/` si JS/CSS.
5. Validation visuelle par l'utilisateur (navigateur) AVANT de considérer l'entrée « validée ».
