# REMOVAL_LEDGER.md — Registre des suppressions différées (résidu de portage/uniformisation)

> **Règle (Fabien, 2026-07-01)** : on **complète d'abord**, on **ne supprime rien** en cours de route,
> et on **trace ici** tout élément suspect (code mort, résidu de registre, duplication) pour le
> supprimer **proprement à la fin** de l'uniformisation de son app — une fois la chaîne A→Z complète.
> Objectif : ne rien oublier, ne rien casser, et éviter que des résidus rendent l'avancement confus.
>
> **Statuts** : ⛔ NE PAS supprimer maintenant · 🟡 prêt à supprimer (complétion faite, à valider) · ✅ supprimé.
> **Gate de suppression** : un item ne passe 🟡 que lorsque son remplaçant centralisé est en place ET vérifié.
>
> **Ajouter une ligne (règles écrites le 2026-08-27, après 4 collisions R20-R23)** :
> ① identifiant = `R` + (plus grand existant + 1), **jamais réutilisé** même après ✅ — balayer TOUT
> le fichier, pas la seule section visée ; ② la ligne va dans la section choisie par la NATURE du
> résidu (§1 catalogue · §2 UI · §3 sources de vérité) ; ③ toute création ou changement de statut
> se double d'une entrée datée au Journal ; ④ le Journal est antéchronologique strict.

---

## 1. Résidu de CATALOGUE (capabilities)

| # | Élément | Emplacement | Pourquoi résidu | Prérequis avant suppression | Statut |
|---|---|---|---|---|---|
| R1 | Clés `multilingual`, `languages_count` | `model_registry.py::_discover_transcriber_models` | **dérivables** de `languages` ; **lues par aucun consommateur** | ✅ SOURCE nettoyée (vérifié grep 2026-07-25 : 0 occurrence dans la découverte) | ✅ |
| R2 | Clé `native_diarization` | idem | nom non canonique (le reste du code utilise `supports_diarization`) ; lue par personne | ✅ SOURCE nettoyée (vérifié grep 2026-07-25) | ✅ |
| R3 | Dict `capabilities` transcriber entier (si on choisit minimal) | idem | écrit dans AIModel, **consommé par rien** côté transcriber (UI lit l'item + flags backend) | décision : garder les faits réels (reco) OU minimal ; ne PAS supprimer les faits `supports_*` (méta-app/sélection en auront besoin) | ⛔ |

## 2. Résidu d'UI (métadonnée-widget non remontée à la source)

| # | Élément | Emplacement | Pourquoi résidu | Prérequis avant suppression | Statut |
|---|---|---|---|---|---|
| R4 | `data-gen-factor` / `data-overhead` par `<option>` (ETA) | `composer/index.html` `#settingsModel` (~l.309-330) | métadonnée ETA en dur dans le template au lieu de sa source | ✅ SOLDÉ (vérifié 2026-08-19 : attributs retirés, commentaire index.html:34 « data MORTS, l'ETA lit window.COMPOSER_MODELS » — statut périmé rattrapé à l'audit fraîcheur) | ✅ |
| R5 | Champs de modale **hand-built** (coquille + champs) | `synthesizer`, `avatarizer`, `composer` (`#settingsModal`/`#jobSettingsModal`) | duplique le schéma `params.py` (panel-only aujourd'hui) | extraire `WamaParams.renderSettingsModal` (depuis enhancer/reader) + porter la modale au contexte `item` | ⛔ |
| R6 | Coquilles de modale **statiques** dupliquées | `transcriber`, `describer`, `converter` (templates) | boilerplate répété (wrapper/titre/footer) | builder commun `WamaParams.renderSettingsModal` adopté | ⛔ |
| R21 | **Barres de recherche/filtrage écrites à la main** | `licenses.html` (`#li-q` + `filtrer()`), `journal.html` (ma 4ᵉ variante, à boutons), `model_manager/index.html` (`applyFilters()`, ~50 l.) — et **2 pages sans aucune barre** (`apps.html`, `library_catalog.html`) | implémentations concurrentes du même geste, comportements divergents (avec/sans bouton, avec/sans compteur) | 🟡 EN COURS (2026-08-20) : brique `wama-filter-bar.js` + `_filter_bar.html` (apparence **model manager**, référence retenue par Fabien ; filtrage EN DIRECT ; options dérivées du DOM en client, déclarées en server). **journal** ✅ · **licences** ✅ *(empreinte 8 combinaisons identique avant/après)* · **function_catalog** ✅ **apparence seule** (cf. note) · **apps** ✅ *(ajout net — 16 cards, facette dérivée, groupes vides masqués)* · **library_catalog** ✅ *(ajout net — 9 librairies)* · **model_manager** ✅ *(apparence commune, mécanisme conservé — cf. note ; empreinte 14 combinaisons identique, à `loaded` près qui a bougé pour une raison de DONNÉE : Kokoro 82M chargé en VRAM entre les deux mesures, panneau « 1 Loaded » à l'appui)*. **R21 SOLDÉE** — 6 pages, une seule définition de l'apparence. ⚠ **Règle dégagée** : DÉCLARER les options prime sur les DÉRIVER dès que le libellé porte du sens (licences affichait « Modèles », la dérivation aurait rendu « model »). | ⛔ |
| — | *(note R21 — deux pages qui ne sont PAS de simples doublons)* | `function_catalog` : filtre un **tableau JS** et **re-génère** la grille (facette multivaluée `projects`, tri) — la brique masque des nœuds existants, la brancher imposerait de rendre les cards côté serveur, donc de réécrire la page. **Apparence mutualisée, mécanisme conservé**, sans `data-wama-filter-bar` pour qu'elle ne s'auto-monte pas. `model_manager` : 3 règles que la brique ne sait pas exprimer — `proposed` masqué partout sauf sous son filtre (156 en DOM / 100 affichés), `format=pt` matche `pt` ET `pth` (appariement flou), `status` **dérivé** de 3 booléens ; + recherche sur 3 champs. À porter avec un crochet `regle_sup(element, valeurs)` fourni par la page (mécanisme dans la brique, **politique dans l'app**) | mon cadrage initial comptait ces deux-là comme « le même geste » — c'est faux, et la capture Playwright AVANT l'a montré | 🟡 |
| R22 | **Commentaires Django multi-lignes `{ #…# }`** — leur texte s'AFFICHE (le délimiteur est mono-ligne) | `composer/index.html:171`, `model_manager/index.html:719`, `reader/index.html:121` (préexistants) + `base.html` (introduit par moi le 2026-08-20, vu au navigateur sur `/common/journal/`) | piège déjà répertorié dans ce dépôt, 7ᵉ récidive | ✅ SOLDÉ (2026-08-20) : les 4 convertis en `{% comment %}`, balayage repo-wide vérifié à 0 restant | ✅ |
| R20 | `wama-inspector.js` + `wama-inspector-autofill.js` chargés **localement** | **14 gabarits, 17 balises** (compté sur le diff `578d09f..`) : 11 apps ×1 · `apps.html`, `journal.html`, `model_manager` ×2 · `studio` ×1 — ⚠ studio ne chargeait QUE `autofill` (donc `WamaDetails` sans `WamaInspector`, jeu incomplet) | redondant : `base.html` fournissait déjà leur CSS et leurs hôtes DOM (`#inspectorInfo`, `#info-section`), mais pas les scripts ; une page transversale (journal) ne les avait donc pas du tout | ✅ SOLDÉ (2026-08-20) : montés globalement dans `base.html`, les 17 inclusions locales retirées **dans le même commit** + commentaire anti-ré-inclusion dans chacun des 14 (vérifié : 0 script restant, 1 commentaire par fichier). ⚠ **Préalable indispensable** : le `MutationObserver` de `wama-inspector.js` re-scannait TOUT le document à chaque ajout de nœuds — inoffensif sur 14 pages à cards, coûteux une fois global (filemanager/jstree, polling). Filtré sur les nœuds ajoutés AVANT de globaliser. Vérifié au navigateur : 18 pages en 200 sans redirection avec exactement 1 occurrence de chaque script ; inspecteur fonctionnel (card sélectionnée + infos remplies) sur transcriber, imager, journal ; hydratation des previews intacte sur anonymizer (19 hôtes au chargement **et** hôte injecté dynamiquement hydraté) ; 0,5 ms médiane / 300 nœuds sur une page sans card | ✅ |
| R7 | `media-picker.js` chargé **localement** | `imager`, `avatarizer` (templates) | redondant — chargé globalement dans `base.html` (mémoire) | ✅ SOLDÉ (vérifié 2026-08-19 : includes locaux retirés, remplacés par commentaires anti-ré-inclusion — même famille de bug que la double inclusion `wama-app-base.js` du 18/08, converter+describer) | ✅ |
| R10 | **Descriptions à DOUBLE source qui DIVERGENT** (apps moteur) | classes backend vs catalogue (stubs anglais whisper-tiny/…/large) | 2 sources par modèle, divergentes | **TRANSCRIBER FAIT (2026-07-02, vérifié)** : découverte importe les CLASSES backend (modules légers vérifiés ; PAS le manager — registration paresseuse=0 backend) → `description_short`=classe.description, `description`=classe.description_long ; qwen garde son court par-modèle (2 tailles/1 moteur), long = classe. 5 stubs whisper purgés (aucun sélecteur de taille, aucune FK, stat ETA existante=`transcriber:whisper` → nouvelle clé alignée). Caps canoniques à la source (R1/R2 transcriber soldés). Catalogue final = 4 entrées réelles. ⚠️ redémarrer serveur WSL2. Reste : répliquer aux autres apps moteur si divergence | 🟡 transcriber ✅ |
| R9 | **Descriptions modèle HARDCODÉES dans le registre** (au lieu du `model_config` par-app) | `_discover_synthesizer_models` + SAM3 | incohérent : imager/composer/reader lisent leur `model_config` | synthesizer FAIT (`REGISTRY_MODEL_DESCRIPTIONS`, court/long séparés) + **SAM3 FAIT** (2026-07-02 : `anonymizer/model_config.py::REGISTRY_MODEL_DESCRIPTIONS`, registre le lit) | ✅ |
| R11 | Mapping moteur↔clé-catalogue **dupliqué en JS** | `synthesizer/index.html` `WamaModelCaps.init.resolveKey` (xtts_v2→coqui-xtts hardcodé) vs `model_config.ENGINE_CATALOG_KEYS` (nouveau, source déclarée) | même pont déclaré 2× | faire lire resolveKey depuis un contexte exposé par la vue (comme tts_model_help_meta) | ⛔ |
| R12 | **VRAM-in-text dans les LABELS d'options** | `common/tts/constants.py::TTS_MODEL_CHOICES` (higgs : « 24 Go VRAM » dans le label) | double affichage avec l'aide WamaModelHelp (`· 24 Go VRAM` appendé) | retirer la VRAM du label (elle s'affiche désormais sous le select) | ⛔ |
| R15 | **Chemin maison de descriptif modèle imager** (`data-description` + `initializeModelDescriptions`) | `imager/index.js:1772` (fonction conservée), `data-description` dans les options du template | dupliquait le composant commun, courte SEULE (pas d'overlay ⓘ — constaté par Fabien) ; mon flag `model_help=True` imager était une sur-déclaration (seul match = mon commentaire) | **remplacé** (2026-07-02) par WamaModelHelp + `fetchCatalogMeta('imager')` sur les 5 selects (appel retiré, staticfiles copiés). Supprimer la fonction + les attrs `data-description` après validation navigateur | ✅ (confronté au code le 2026-08-27 : 0 occurrence `initializeModelDescriptions` dans `wama/imager/`) |
| R17 | `checkMelodyVisibility` + const `melodyGroup` (composer JS) | `composer/index.js` (~l.46/89-100) | no-op null-gardé depuis que le slot déclaré de la card (WamaInputMatch) a remplacé le champ hardcodé du volet | supprimer après validation navigateur du pilote appariement | ✅ (confronté au code le 2026-08-27 : `composer/js/index.js:93` porte « checkMelodyVisibility PURGÉE (R17) ») |
| R18 | Onglets résultat description/résumé/cohérence DUPLIQUÉS (HTML quasi identique) | `describer/index.html` (`#resultTabs`) + `transcriber/index.html` (`#tab-resume-btn`/`#tab-coherence-btn`) | même structure d'onglets maintenue 2× → divergera | extraire `common/_result_tabs.html` puis supprimer les 2 blocs locaux (migré de REPRISE_2026-07-22 item 2) | ✅ **SOLDÉ le 2026-09-07.** Le partial existe et les 2 blocs (118 lignes) sont remplacés par `{% templatetag openblock %} result_tabs '<app>' {% templatetag closeblock %}`. ⚠ Ce que l'entretemps a appris : le fichier cible **n'avait jamais existé** — les 8 « références cassées » de `check_docs` pointaient une CIBLE planifiée, pas un fichier supprimé ; `check_docs` est donc **à 0** depuis. Les facettes se déclarent dans la **spec de détail** (`register_app_detail_spec`), donc extractibles au manifeste et projetables — pas dans un dict d'interface à côté, qui aurait été un 4ᵉ chemin à côté de `PreviewRegistry`, du schéma canonique et des modales. Non-régression prouvée par comparaison des ANCRES avec les originaux sur HEAD : describer 13/13, transcriber 16/16, zéro perdue, zéro inventée — les ids sont le contrat que le JS consomme, et un id déplacé ne lève pas, il cesse silencieusement de remplir l'onglet. Critère de grille `result_tabs` (F3, enveloppe non-applicable) + 7 tests |
| R23 | **`anonymizer:clear_media`** — ancienne route de suppression par élément (`media_id` en champ de POST) | `anonymizer/urls.py:26` + `views.py::clear_media` | remplacée le 2026-08-23 par la route AU FORMAT COMMUN `delete/<pk>/`, seule servable par la brique `queue-actions.js` (qui poste un JSON vide). **Plus aucun consommateur dans le dépôt** : la clé de config `clearMediaUrl` a été retirée du gabarit au même geste. Le corps de travail est déjà PARTAGÉ (`_supprimer_media`), donc il n'y a pas deux chemins — seulement une porte d'entrée en trop | vérifier qu'aucun appelant EXTERNE (script, intégration) ne poste sur `clear_media/` ; ⚠ l'ancienne vue n'était scopée par AUCUN utilisateur — la garder ouverte prolonge cette surface | ⏳ à retirer |
| R24 | **Bascule d'onglets Bootstrap de l'enhancer** — `showImgPanel()` / `showAudioPanel()` + persistance `localStorage.enhancerTab` (~58 l.) | `enhancer/index.html:347-406` | morte **en silence** depuis que les onglets de domaine sont rendus par `WamaModes` : la brique génère ses boutons **sans `id` et sans `data-bs-toggle`** (`wama-modes.js:93`, clic géré par elle-même), donc les `getElementById('imgvideo-tab')` / `('audio-tab')` rendaient `null` et **aucun listener n'était posé**. Mesuré au navigateur : les 2 boutons absents, `localStorage.enhancerTab` **jamais écrit** (relevé `null`), et **3 des 5 ancres manipulées** — `imgActions`, `audioActions`, `dropZoneUrl` — **n'existent nulle part** dans `wama/` (0 définition). Les 2 barres de batch restantes vivent DANS les panes, donc basculent avec eux. La bascule réelle est `switchDomain()`, câblée sur l'`onChange` de WamaModes | ✅ SOLDÉ (2026-08-23) : bloc retiré, remplacé par un `{% comment %}` qui dit pourquoi. Non-régression prouvée — bascule des 2 domaines dans les 2 sens + persistance au rechargement, 0 erreur JS ; `enhancer.settings` **passe** (était le seul échec de sa famille), `.settings` **7 OK**, `.duplicate_delete` **7 OK**, grille enhancer **99 % inchangée**, `check_js` 57/0 et 56 paires/0. ⚠ **Rien ne l'avait signalée en 1 jour d'existence** : des gardes `if (x)` sur des ancres absentes ne lèvent pas | ✅ |
| R25 | **`BatchMixin.is_unitary` — ADOPTÉ, plus un candidat au retrait** | `common/models.py:47-51` ; adopté par les **10 gabarits** | ⚠⚠ **Fiche corrigée TROIS fois — les deux premières versions étaient fausses** (recadrages successifs de Fabien). ① « aucun consommateur → retirer » : constat juste, conclusion fausse. ② « brique ignorée, 28 recopies » : **alarmiste et faux** — j'avais compté comme désordre l'application d'une règle écrite. ③ RÉEL : le mécanisme batch est porté partout (`_batch_card.html` 11/11, `batch_common` 11/11, `BatchMixin` 12/12) ; `is_unitary` était une commodité née le 2026-06-30 d'une intention de portage, trois mois après que les gabarits eurent écrit `total == 1` (mars 2026) et une semaine avant que le portage se fasse AUTREMENT (`_batch_card.html`, 06/07). Jamais un seul appelant | ✅ **SOLDÉ le 2026-08-24 par ADOPTION** (arbitrage Fabien : nommer la notion vaut mieux que la recalculer). Les 10 gabarits écrivent `{% templatetag openblock %} if batch_info.obj.is_unitary {% templatetag closeblock %}` ; `obj.total == 1` en gabarit est désormais une régression. Doc §9.7 mise à jour — elle prescrivait `total`. ⚠ NE PAS convertir `total <= 1` (`queue_manipulation`) : il inclut 0, c'est « il ne restera plus rien », pas « c'est unitaire » | ✅ |
| R26 | **Le bloc « card unique vs card mère » recopié dans 10 gabarits** — et ce qui l'empêche d'être commun : **7 graphies pour appeler une card fille** | les 10 `index.html` d'app ; contrats divergents : `media=`, `gen=`+`card_label=`, `synthesis=`+`card_label=`, `item=`, `e=`, `ae=`, `gen=`+`domain=`, et **transcriber qui ne passe RIEN** (contexte implicite) | Le bloc entier (`if is_unitary → card fille seule ; else → .batch-group + `_batch_card.html` + .collapse`) est **structurellement identique dans les 10**. Question de Fabien (2026-08-24) : « pourquoi 10 gabarits, ce n'est pas centralisé dans common ? ». ⚠ Ma 1ʳᵉ réponse — « le test décide s'il faut inclure le partial, donc il est en amont et non absorbable » — **était FAUSSE** : un partial commun de plus haut niveau recevrait le template de card fille en paramètre. **Le seul vrai obstacle est la divergence des contrats** — un partial commun ne peut pas appeler 7 signatures différentes | **DEUX étapes, dans cet ordre — l'inverse est impossible** : ① uniformiser le contrat des cards filles (un seul nom de variable, `in_batch` partout, les extras comme `domain`/`card_label` passés uniformément) ; ② absorber le bloc dans un partial commun `_queue_entry.html`. ⚠ Motif connu du dépôt : **la divergence de nommage est la trace d'une brique absente** — ici elle a masqué la brique manquante pendant tout le temps où l'on regardait `is_unitary` | ⛔ |
| R19 | DEUX renderers de forme d'onde | `common/js/wama-audio-player.js::setPeaks` + renderer zoomable de `transcriber/edit.js` | le CALCUL est unifié (`common/utils/waveform.py::compute_peaks`) mais le RENDU existe en double | fusionner vers un renderer commun paramétrable (zoom = option), puis supprimer celui d'edit.js (migré de REPRISE_2026-07-22 item 4) | ⛔ |
| R27 *(ex-R20 bis, renuméroté 27/08)* | **Shims d'alignement devenus IDENTITÉ** (3) | `synthesizer/backends/base.py::CATALOG_KEYS` (map identité) + `resolveKey` JS (`synthesizer/index.html:~505`) + wrapper `_input_match_meta_enhancer` (`enhancer/views.py:167`) | l'alignement `model_key` du 18-19/08 (route SPEC §356) a rendu ces ponts app↔catalogue identitaires — gardés comme points d'accroche documentés | attendre 1-2 sessions de recul post-restart Celery (aucune divergence réapparue) puis inliner les 3 | ⛔ |
| R28 *(ex-R21 bis, renuméroté 27/08)* | Catalogue **describer STALE** (`describer:blip`, `describer:whisper`) | `model_registry.py::_discover_describer_*` + 2 lignes AIModel | entrées legacy — le describer réel tourne sur les LLM ollama (tier `get_describer_model`), blip/whisper ne sont plus son moteur | décider : purger les 2 entrées OU découvrir les vrais moteurs LLM (cohérence avec la sélection par tier) ; même geste que la purge des stubs whisper de R10 | ⛔ |
| R29 *(ex-R22 bis, renuméroté 27/08)* | Card audio enhancer sur include DIRECT (`_audio_waveform`) | `enhancer/_audio_card.html:124` | hors du mécanisme n°30 « Preview unifiée » (placeholder `data-card-preview` + hydrateur) adopté par converter/anonymizer/avatarizer le 18/08 — fonctionne, mais 2ᵉ idiome | migrer au placeholder hydraté (side=output) puis retirer l'include direct | ⛔ |
| R31 | **API `WAMA_RIGHT_PANEL` morte** (1ʳᵉ génération du volet droit) | objet JS inline `base.html:233` (`showPreview(url,type)` → `#preview-media`) + copie dans `filemanager/right_panel.html:55` — template lui-même inclus NULLE PART | 0 appelant dans tout le dépôt (mesuré 27/08) ; la préview vivante = `media-preview.js` via `wama-inspector.js` ; les conventions §22 la recommandaient encore (corrigé 27/08) | supprimer l'objet des 2 fichiers + le template orphelin `right_panel.html` après un grep de confirmation | 🟡 |
| R30 *(ex-R23 bis, renuméroté 27/08)* | `WamaModelHelp.init` **direct** (meta serveur) vs `help_source` déclaratif | `anonymizer/index.html:375` (+ transcriber `#backendHelp`, canal propre assumé) | 2 idiomes pour le même descriptif — anonymizer pré-date le champ `help_source` du schéma (adopté par 6 apps au 19/08) | porter anonymizer sur `help_source` quand son inspector sera porté (chantier tracé) ; transcriber = référence historique, à trancher | ⛔ |
| P2-bis | **Formatage déclaratif des ranges** (unit/min_label/max_label dans `Param` + rendu WamaParams) | `param_schema.py` + `wama-params.js` | la durée composer reste bespoke au volet (affichage « 10s ~20s », bornes « 10s/10min » formatées + estimation par IDs) tant que le schéma ne porte pas le formatage → dernière divergence modale/volet | enrichir le schéma PUIS générer la durée des 2 surfaces (supprime le widget bespoke) | ⛔ |
| R16 | **Avatarizer : mode pipeline à retirer À TERME** (décision Fabien 2026-07-02) | `avatarizer` (workflow_mode, section TTS de la modale, params TTS) | le pipeline (TTS→lip-sync) se fera dans le **STUDIO** quand il sera terminé → avatarizer devient **standalone-only** (audio fourni) | simplifier après la mise en place du studio : retirer mode/TTS de l'app, la modale ne garde que MuseTalk | ⛔ gaté sur studio |
| R13 | **Template orphelin** `anonymizer/upload/global_settings.html` | inclus par PERSONNE (vérifié grep include) ; porte un `#user_setting_model_to_use` DUPLIQUÉ de index.html | résidu — risque d'ID dupliqué si réactivé | supprimer après confirmation qu'aucune vue ne le rend | ✅ (confronté au code le 2026-08-27 : le fichier n'existe plus — 0 résultat `find wama -name global_settings.html`) |
| R14 | **Moteurs TTS hors catalogue** : vits, tacotron2, speedy_speech | `TTS_MODEL_CHOICES` vs découverte | sélectionnables dans l'UI mais invisibles du model_manager | **Décision Fabien : labéliser « Nouveau » (prospection)** → FAIT (2026-07-02) : 3 entrées `AIModel` créées `is_proposed=True, proposal_kind='new', confidence=0.9` (clés propres `synthesizer:vits/tacotron2/speedy-speech`, desc court/long, extra_info.recommended origin=ui-choices, installer=coqui auto au 1er usage). Vérifié : survivent au `full_sync`, visibles onglet « ✨ Proposés ». NB : entrées DATA (non re-créées par du code) — un reset DB les perd, mais la prospection HF les re-proposerait | ✅ **ANNULÉE par R32 (2026-08-28)** |
| R32 | **Les 3 moteurs TTS historiques RETIRÉS de bout en bout** : `vits`, `tacotron2`, `speedy-speech` | 8 surfaces — `common/tts/constants.py` (`TTS_MODEL_CHOICES`, `COQUI_MODEL_MAPPING`), `synthesizer/utils/model_config.py` (`SYNTHESIZER_MODELS`), `synthesizer/backends/coqui_backend.py` (`_VRAM_GB` + docstring), `synthesizer/templates/index.html` (3 bandeaux + set batch), `synthesizer/static/js/index.js` (set + 3 câblages), `synthesizer/tests.py` ×2, 3 lignes `AIModel`, 3 manifestes `models/` | **retrait DEMANDÉ par Fabien** (« modèles historiques, probablement dépassés ») et instruit sur MESURE, jamais sur l'ancienneté : **0 usage sur 103 travaux** (98 `VoiceSynthesis` + 5 `AvatarJob` ; répartition réelle kokoro 49 · coqui-xtts 53 · higgs-audio 1), `created_at == updated_at` au 2026-07-02 quand les 4 survivants ont bougé le 28/08, `capabilities` réduites au tronc, `quality_index` NULL, `hf_id` vide. R14 les avait sauvés en les labélisant « proposés » — c'est ce statut même qui a produit le défaut : **un candidat de prospection ne peut pas être simultanément une option d'exécution** (invisible de l'inventaire, offert au select). | ✅ SOLDÉ (2026-08-28) : les 8 surfaces dans le même commit ; cascade DB **mesurée avant** (collector Django : 3 lignes, 0 dépendant) ; garde-fou `is_downloaded`/`is_loaded` avant suppression ; corpus de manifestes régénéré ; trou #5 de `PROSPECTION_PIPELINE` refermé. ⚠ **Effet de bord VOULU** : les 4 survivants étant tous multilingues, `ENGLISH_ONLY_MODELS` devenait un jeu VIDE → les 3 bandeaux « ce modèle ne supporte que l'anglais » étaient devenus **inatteignables** ; retirés avec leur mécanisme (2 exemplaires divergents, template + JS, doublon signalé au 18/08 « à porter avec la phase R »). 🔴 **Besoin RÉEL laissé ouvert, et il est GÉNÉRAL — pas le cas particulier d'un moteur** (recadrage de Fabien, 28/08 : « kokoro n'est qu'un modèle parmi tant d'autres qui peuvent ne pas gérer toutes les langues en entrée »). Mesuré sur les 15 langues du select : **3 des 4 survivants ont des trous** — `bark` n'en gère pas 3 (`nl, cs, ar`), `higgs-audio` 6 (`pl, tr, ru, nl, cs, ar`), `kokoro` 8 (`de, pl, tr, ru, nl, cs, ar, ko`) ; seul `coqui-xtts` (17 langues déclarées) les couvre toutes. *La liste en dur se trompait donc DEUX fois : elle nommait 3 moteurs et en ignorait 3 autres réellement lacunaires — **un prédicat booléen (« EN-only ») ne peut pas dire une couverture, qui est un ENSEMBLE**.* L'avertissement doit RENAÎTRE dérivé des `languages` déclarées au catalogue (`lang_routing.routing_for_model`), jamais d'une liste de noms. ⚠ Écart relevé au passage, non tranché : `KOKORO_LANG_MAP` (`common/tts/constants.py`) porte **15** clés quand le catalogue déclare **7** langues pour kokoro — deux sources en désaccord sur le même fait, à réconcilier avant de recâbler l'avertissement. | ✅ |
| R8 | **Résidus ETA par-app** après centralisation depuis common | anonymizer `process.js:49`/`media_table.html:21`, `converter.js:360`, `transcriber/index.js:365` | signalé par Fabien | **VÉRIFIÉ (Claude, 2026-07-01)** : les 4 fichiers utilisent TOUS le `WamaEta` commun — **aucun résidu mort**. Preuve : transcriber:1553 documente que l'ancien `updateGlobalProgress` par-app a **déjà été retiré** à la centralisation. RAS sur ces fichiers. (Délégation wama-dev-ai abandonnée : connexion réparée mais modèle `audit` renvoie EOF 500 sur l'Ollama hôte.) | ✅ rien à supprimer |

## 3. Sources de vérité à CORRIGER (≠ suppression — mise à jour)

| # | Élément | Emplacement | Problème | Action (PAS une suppression) | Statut |
|---|---|---|---|---|---|
| F1 | Flags `inspector`/`modes`/`layout`=True **seulement transcriber** | `app_registry.py::APP_CATALOG.conventions` | périmés → l'app manager sous-déclarait l'uniformisation | `inspector=True` sur les **8 apps** qui câblent WamaInspector (avatarizer/composer/converter/describer/enhancer/reader/synthesizer/transcriber, vérifié lignes réelles) ; `modes=True` ajouté à **enhancer** (WamaModes.fetch/create) — imager déjà True ; `settings_modal_item=True` ajouté à **imager** ; ETA laissé False (réellement non implémenté, cf. CLAUDE.md) | ✅ (2026-07-01) |
| F6 | `transcriber` `modes=True` mais **n'utilise PAS WamaModes** | `app_registry.py` + `transcriber/index.html:321,352` | découvert : transcriber a **délibérément retiré le switch de mode** — Speak (temps réel) = **AFFORDANCE de card** (`show_live`), pas un mode. `modes=True` était donc faux | `modes=None` (N/A + commentaire) : la convention switch-WamaModes ne s'applique pas par design card-centric. À repasser True si « realtime=mode » est implémenté | ✅ (2026-07-02) |
| F2 | `ModelSource.choices` manque `composer`/`reader` | `model_manager/models.py` | découverte écrit ces `source` hors-enum (4+2 modèles) ; **converter = 0 modèle → N/A** | **compléter** l'enum | ✅ (migration 0008, appliquée) |
| F3 | Réconciliation C1 : `extra_info` vs `capabilities` | `model_selector._supports` | lisait `extra_info['classes']` (vide → filtre anonymizer cassé, mais **défaut latent** : aucun appelant de `select_model`) alors que `capabilities` = source | lit `capabilities` d'abord, `extra_info`/`class_list` en repli transition ; docstrings alignées | ✅ (2026-07-01) |
| F4 | `model_key` composer/reader ≠ `{source}:{id}` | `_discover_composer/reader_models` | clé de registre **nue** (`doctr`) vs convention `{source}:{id}` des 7 autres apps → `_resolve_model` (pilier traduction) ne trouvait pas les capacités → repli type silencieux | préfixer la clé de registre → alignée ; re-sync + purge des 6 entrées nues orphelines | ✅ (2026-07-01) |
| F5 | **DEUX enums `ModelSource`/`ModelType`** à synchroniser à la main | `models.py` (DB/choices, TextChoices) **vs** `model_registry.py` (découverte, Enum pur) | même concept dupliqué → dérive (le registre avait composer/reader, la DB non → F2) | **garde-fou** `checks.py` (Django system check, Warning) : `registre ⊆ DB` vérifié à chaque `manage.py check`/démarrage. Merge structurel écarté (migrations Django exigent des littéraux statiques + enums ré-exportés par views/sync). DB peut avoir des extras légitimes (`huggingface`/`custom` = cache HF gardé pour dépendances) | ✅ garde-fou (2026-07-01) |

---

## Journal
- **2026-08-28** — **Ajout R32, soldée le jour même** : retrait des 3 moteurs TTS historiques
  (`vits`/`tacotron2`/`speedy-speech`) sur les 8 surfaces + 3 lignes de catalogue, à la demande de
  Fabien. **R14 est ANNULÉE** : sa décision de 2026-07-02 (« les labéliser proposés pour qu'ils ne
  disparaissent pas ») est renversée — elle a tenu 8 semaines et a produit exactement le défaut
  qu'elle croyait éviter, un moteur invisible de l'inventaire mais offert au select. *Une entrée
  ✅ n'est pas définitive : une décision se rouvre quand la mesure la contredit.*
  ⚠ Ce qui a rendu ce retrait sûr n'est pas l'ancienneté des modèles mais **trois mesures**
  (0 usage / 103, statut de catalogue jamais touché, cascade DB vide) — l'ancienneté seule
  aurait désigné les MAUVAIS : les trois retirés datent du 2026-07-02, alors que `coqui-xtts`
  et `bark`, **créés le 2026-01-31**, sont les plus anciens du catalogue TTS et les plus
  utilisés. *Une date de création ne dit rien de la pertinence ; un compteur d'usage, si.*
  ⚠⚠ **Recadrage de Fabien le jour même** — j'avais consigné le besoin résiduel en citant kokoro
  comme s'il était LE cas ; il n'est qu'un moteur parmi d'autres. La mesure lui donne raison et va
  plus loin : **3 des 4 survivants** ont des trous face aux 15 langues du select (bark 3,
  higgs-audio 6, kokoro 8), un seul n'en a aucun. *Un exemple pris pour la règle rétrécit le
  problème à sa propre taille* — et c'est précisément le défaut qu'avait le mécanisme retiré :
  `ENGLISH_ONLY_MODELS` était un **booléen** posé sur trois noms, là où la question est un
  **ensemble** par moteur. Formuler la règle en général (« la couverture se lit dans les
  `languages` déclarées ») était la seule façon de ne pas reconstruire la même liste en dur.
- **2026-08-27 (audit /doc-sync)** — En-tête complété (règles d'allocation, après 4 collisions) ;
  **R27-R30** renumérotés (ex-doublons R20-R23) ; **R13/R15/R17 passés ✅** (confrontés au code,
  preuves en ligne) ; **ajout R31** (API `WAMA_RIGHT_PANEL` morte, 0 appelant — vérif profonde à la
  demande de Fabien).
- **2026-08-27** — **Trois retraits IMMÉDIATS (rien de différé), tous « du code qui s'exécute sans
  rien produire »** — commits `8cc68bfe` / `ec279bea` :
  ① **tri mort** — 4 vues triaient leur `batches_list` juste avant d'appeler
  `apply_queue_sort_filter`, qui re-trie **inconditionnellement** ; `c9408354` en avait retiré 3,
  `a318b7f3` le 4ᵉ, **et rien n'empêchait le 5ᵉ** → garde **textuelle** posée
  (`wama/common/tests_queue_sort.py`, 17 tests). ⚠ Un tri mort ne se détecte **pas** à l'exécution :
  par définition son effet est écrasé — seule une garde sur le SOURCE le voit.
  ② **`try/except` avatarizer** autour d'`apply_queue_sort_filter` (`wama/avatarizer/views.py`),
  retombant sur `q_sort, q_filter = '', ''` avec un journal en `debug` seul : **seul des 12 sites**
  à étouffer la brique commune. Appel nu désormais. Une brique COMMUNE doit casser **visiblement
  partout de la même façon**.
  ③ **deux ERREURS permanentes de découverte** (`wama-dev-ai.core`, `wama-dev-ai.ui`) retirées au
  **harnais** (`WamaTestRunner.build_suite` + `RACINES_HORS_DECOUVERTE`), pas dans l'outil : son
  `config.py` vit **au-dessus** du paquet, aucun import relatif ne l'atteint (essayé, MESURÉ,
  l'erreur se déplace d'un cran ; essai annulé). Deux garde-fous contre l'élagage silencieux :
  seuls les `_FailedTest` de la découverte sont reconnus, et un `test*.py` apparaissant dans une
  racine exclue **refuse** l'élagage. Suite : **997/9/2 → 1029/0/0**.
- **2026-08-19** — **Audit fraîcheur PARTIEL (question Fabien « le ledger n'est peut-être plus à
  jour »)** : 2 entrées sondées, 2 PÉRIMÉES rattrapées — R4 et R7 étaient soldées dans le code
  (commentaires anti-régression en place) avec statut ⛔ resté figé. **Les 21 autres entrées ⛔/🟡
  restent à confronter au code** (candidat `/doc-sync` dédié). Ajouts **R27-R30** *(numérotés
  R20-R23 à l'origine — collision avec les R20-R23 déjà pris en §2 ; renumérotés le 2026-08-27,
  aucune référence externe aux anciens numéros, vérifié)* (résidus de la
  session d'alignement 18-19/08) : shims devenus identité, catalogue describer stale, card audio
  enhancer hors mécanisme preview n°30, `WamaModelHelp.init` direct vs `help_source`. Rappel de
  périmètre acté au passage : le ledger trace les SUPPRESSIONS DIFFÉRÉES (code/registre) — les
  artefacts jetables de session (fichiers de test, jumelles) vivent dans les PENDINGS du handoff.
- **2026-07-01** — Création du registre. Complétion **#1** : `normalize_capabilities` branché au sync
  (`model_sync.py:168` + import) → catalogue DB canonique **sans** toucher aux `_discover_*` (R1/R2 =
  source à nettoyer en fin de parcours). `full_sync` relancé (nouveau code) → 106 modèles MAJ, **0 clé
  legacy en base**. ⚠️ process WSL2 à redémarrer sinon un refresh UI (ancien code) ré-écrit le legacy.
- **2026-07-01** — Complétion **#2 / F2** : `ModelSource` (DB) + composer/reader (migration `0008`, appliquée).
  Converter écarté (0 modèle). Découverte annexe → **F4** (model_key composer/reader ≠ `{source}:{id}`).
  Rien supprimé.
- **2026-07-01** — Complétion **#3 / F4** : préfixe `{source}:{id}` posé dans `_discover_composer/reader_models`
  → clés alignées sur la convention (7 autres apps). Re-sync (added=6) + **purge des 6 entrées nues
  orphelines** (doublons immédiats, PAS du résidu différé). `_resolve_model` (traduction) retrouve
  désormais composer/reader. Découverte annexe → **F5** (deux enums `ModelSource` DB vs registre).
- **2026-07-01** — Complétion **#4 / F3** : `model_selector._supports` lit `capabilities` (canonique) au
  lieu de `extra_info` — défaut latent (aucun appelant), filtre classes anonymizer réparé. Vérifié.
- **2026-07-01** — Complétion **#5 — capabilities des apps à trou** (base manifeste propre) : injection
  CANONIQUE + FACTUELLE dans la découverte : composer `{modalities:['audio'],task:text-to-music|audio,
  languages:['en']}` (encodeur T5 anglais), reader `{modalities:['image','document'],task:'ocr'}`,
  avatarizer `{modalities:['image','audio','video'],task:'lip-sync'}`. Re-sync → **0 trou** sauf ollama.
  ⚠️ **ollama DÉFÉRÉ** (18 vides) : backend LLM partagé, possiblement géré par **wama-dev-ai** (RAM-aware
  config.py) → NE PAS injecter sans coordonner (risque télescopage). `context_length` non inventé
  (nécessiterait `ollama show`). ⚠️ Rappel : après édition du registre, **redémarrer le serveur WSL2**
  sinon la sync du process live (ancien code) recrée les clés nues.
- **2026-07-02** — **F6** : `transcriber` `modes=True`→`None` (N/A) — Speak = affordance card, pas un mode WamaModes.
- **2026-07-02 (suite session Fable5)** — **Plan validé Fabien** : deux niveaux actés (moteur=app,
  modèle=catalogue ; centralisation finale = réflexion ultérieure) ; 1ʳᵉ passe = toutes les apps au
  niveau Transcriber + tests nocturnes, seuil conformité ~90 %, colonnes du tableau à compléter au fil.
  **Exécuté** : R10-transcriber ✅ (4 entrées réelles, desc=classes backend, caps canoniques, 5 stubs
  purgés, clé `whisper` alignée stat ETA) · R9-SAM3 ✅ · **colonne `model_help`** ajoutée à `_conv()`
  (valeurs vérifiées : True=transcriber/enhancer/imager/composer/reader, None=converter, False=describer/
  synthesizer/anonymizer/avatarizer) · **câblage WamaModelHelp SYNTHESIZER** ✅ (1er des 4) : pont déclaré
  `ENGINE_CATALOG_KEYS` (model_config) → vue `_tts_model_help_meta()` lit le CATALOGUE → template
  `#ttsModelHelp` + init (meta vérifiée : 4 moteurs, template compile). Moteurs Coqui légers
  (vits/tacotron2/speedy_speech) sans entrée catalogue → aide vide (assumé). Découvertes → R11, R12.
- **2026-07-02** — **Convention descriptions (CORRIGÉE)** : le format de référence = **transcriber**
  (`backends/*.py` : `description` + `description_long`, rendus par le composant COMMUN `WamaModelHelp`,
  index.js:1580). Ce sont **DEUX champs SÉPARÉS et INDÉPENDANTS**, PAS « court. détail » concaténé
  (mon idée initiale de dériver le court par split était FAUSSE). `description_short` = one-liner concis
  (sans VRAM, appendée par le JS) ; `description`/`long` = paragraphe AUTONOME en overlay ⓘ (peut citer
  la VRAM en prose). Appliqué à **synthesizer** (4 modèles, champs séparés, overlay=OUI vérifié).
- **2026-07-02** — **Descriptions modèle (dimensionnement)** : VRAM retirée de 8 `description_short` d'apps
  CATALOGUE (7 imager : cogvideox×2, ltx×2, mochi, flux2-klein, flux-logo ; 1 synthesizer : higgs) qui
  la codaient en dur → **double affichage** (le JS `wama-model-help.js:48` append déjà `· X Go VRAM`
  depuis `vram_gb`). **Convention actée (validée Fabien)** : la VRAM vient TOUJOURS du catalogue
  (`vram_gb`), JAMAIS du texte. Format de référence = **transcriber** : `description_short` = one-liner
  de traits (sans VRAM) ; `description` (long) = phrase(s) de détail en overlay ⓘ si ≠ court. Sources :
  imager/synthesizer descriptions hardcodées dans `_discover_synthesizer_models` (registre) pour synth,
  `model_config.py` pour imager. Vérifié : 0 VRAM-in-text restant, JS append la bonne valeur catalogue.
- **2026-07-01** — **F1 / app manager** : conformité `APP_CATALOG.conventions` remise au réel — `inspector=True`
  sur 8 apps (vérifié par lignes réelles WamaInspector), `modes=True` enhancer, `settings_modal_item=True`
  imager. `get_conformity_summary()` reflète désormais l'uniformisation réelle (le tracker ne sous-déclare
  plus l'inspecteur). ETA laissé False (non implémenté). Découvertes annexes → **F6** (transcriber modes
  ≠ WamaModes) + **R8** (résidus ETA par-app à auditer, signalés par Fabien).
- **2026-08-26** — **PARC OLLAMA : 5 retraits (~42,5 Go), décision Fabien sur inventaire mesuré**
  (catalogue qualité/benchmark + points de sélection réels + banc wama-dev-ai) : **qwen3-coder:30b**
  (18 G — bench tiers 13,6 le pire du parc, « invente des briques » au banc codegen, aucun sélecteur),
  **gemma4:26b** (17 G — dominé par gemma4:12b à 2× la VRAM, 1 SyntaxError/2 runs au banc),
  **qwen3.5:9b** (6,6 G — ex-défaut historique d'avant la résolution catalogue, dominé par gemma4:12b),
  **nomic-embed-text** (anglo-centré, rejeté pour la mémoire — raison documentée `embed.py`),
  **mxbai-embed-large** (aucun consommateur ; bge-m3 = LE substrat d'embeddings). **Gardés** :
  gemma4:12b (pivot `modele_par_defaut`), qwen3.8 (meilleur mesuré), translategemma:12b (Translator),
  bge-m3 (mémoire/RAG), qwen3.5:4b (seul LLM sous ~7 Go VRAM libre — assurance du sélecteur VRAM-aware),
  gemma4:e4b + qwen3.6:35b (wama-dev-ai : agent fiable + champion codegen, sursis jusqu'au prochain banc).
  **3 surfaces traitées le même jour** : ① `ollama rm` ×5 + réconciliation catalogue
  (`full_sync(delete_missing=True)`, removed=5, candidats `is_proposed` épargnés par construction) ;
  ② `wama-dev-ai/config.py` : 6 entrées re-pointées (debug→qwen3.6:35b, fast→gemma4:12b,
  vision_lite→qwen3.5:4b, premium→qwen3.8, embed→bge-m3, clé `gemma4_26b` RENOMMÉE `qwen38_codegen`)
  + chaîne `audit` re-tirée **Gemma seulement** (mesure du 20/08 : famille Qwen 4/4 en échec tool-use) ;
  ③ derniers noms en dur côté WAMA branchés sur la résolution catalogue (`video_describer.py`
  défaut de signature survivant du refactor 04/08, `llm_gateway_check.py --model` → `modele_par_defaut`).


## Nettoyage 2026-08-31 — session audit (GO Fabien : « en profondeur, sans rien casser »)

> Chaque retrait précédé d'un grep exhaustif de consommateurs (outil natif) ; ablations JS
> par script à ASSERTS (le script s'est refusé DEUX fois lui-même avant écriture — d'abord
> des coupes qui inséraient au lieu de remplacer, puis mes propres commentaires citant les
> morts). Mesuré après : batterie converter réel + suite complète.

| # | Élément | Emplacement | Pourquoi résidu | Prérequis avant suppression | Statut |
|---|---|---|---|---|---|
| R33 | `buildModalFormHTML` (+`transformsHTML`/`escape` nichées, ~134 l.) | `converter.js` | branche INATTEIGNABLE (wama-params chargé inconditionnellement, APP.schema jamais vide — audit B1) | ✅ grep : unique appel dans l'else mort ; else remplacé par un état d'erreur VISIBLE | ✅ |
| R34 | `readModalForm` (+ routeur `readCurrentModal` du matin) | `converter.js` | lisait des attributs que WamaParams n'émet pas (bug « Sauver comme profil », audit B2) ; le fork retiré, le routeur n'avait plus d'objet | ✅ les 2 lecteurs = `readModalViaSchema` ; test tient les morts absents | ✅ |
| R35 | repli `.job-start-btn` | `converter.js` | 0 occurrence dans les gabarits depuis le bouton de cycle commun — repli inerte par construction | ✅ grep gabarits | ✅ |
| R36 | contexte `jobs`/`profiles`/`supported_formats` + requête `ConversionProfile` par chargement | `converter/views.py::IndexView` | consommés par AUCUN gabarit (audit B6/B7) — profils via AJAX, front lit `supported_formats_json` | ✅ grep templates | ✅ |
| R37 | `import os` local (download_all) + `from django.views.generic import TemplateView` | `converter/views.py` | redondant depuis l'import module (fix ZIP) / jamais utilisé (audit B5) | ✅ | ✅ |
| R38 | filtre `compact_preview` (fichier `reader_tags.py` entier) | `reader/templatetags/` | 0 usage gabarit depuis le portage du mécanisme n°30 (le commun compacte) ; la copie serveur `views._compact_preview` RESTE (sert l'API) — trou #27 route | ⚠ **retrait INCOMPLET le 31/08, réparé le 01/09** : le `{% load %}` du partial était resté → page reader en `TemplateSyntaxError` (vu par Fabien à l'écran). Le grep « de preuve » cherchait la graphie `load reader_tags` quand le gabarit écrit `{% load i18n reader_tags wama_actions %}`. **Un templatetag a TROIS surfaces : le fichier, ses usages, et le `{% load %}` — et un retrait se vérifie sur le SYMBOLE, jamais sur une graphie d'usage.** Garde posée dans la foulée : `check_templates` signale désormais tout `{% load %}` vers une bibliothèque absente (contre-épreuve : la garde rejouée sur le défaut exact le DÉTECTE ; 2 tests) | ✅ |
| R39 | `openImagePreview`/`openVideoPreview` (bloc script ~60 l.) | `imager/index.html` | 0 appelant depuis le portage n°30 (la visionneuse tire sa navigation de `result_files`) — auto-documenté candidat | ✅ grep wama/imager + staticfiles | ✅ |
| R40 | clés legacy `global_progress`/`completed`/`pending` | `synthesizer/views.py::global_progress` | contrat commun `overall_progress`/`done` adopté le jour même ; son JS basculé, aucun autre consommateur | ✅ grep repo (tool_api, services, tests) | ✅ |
| R41 | proxys `_View` (backend→used_backend) | `reader/views.py`, `transcriber/views.py` | duplication P6 de l'audit — remplacés par `chips_by_section(values=)` (brique du jour) | ✅ comportement identique par construction | ✅ |
| R42 | (note de changelog, pas un retrait) `convert_pt_to_safetensors` collecte désormais RÉCURSIVEMENT (clés aplaties en pointé) | `common/utils/safetensors_utils.py` | un checkpoint mixte produisait un fichier PARTIEL en silence ; le résultat peut différer des conversions antérieures (audit R8) | — | 📝 |

## Portage converter 2026-09-01 (GO Fabien : colonnes + zone de composition)

| # | Élément | Emplacement | Pourquoi résidu | Prérequis avant suppression | Statut |
|---|---|---|---|---|---|
| R43 | Les ~136 lignes de FORMULAIRE de composition écrites à la main (Format de sortie, imageOptions/videoOptions/audioOptions/transformsOptions, imageQuality/resizeW/H, videoCRF/FPS, audioBitrate/Normalize, rotation, flipH/V) + leurs 3 lecteurs/écrivains JS manuscrits (readMainPanelOptions champ-par-champ, applyOptionsToMainPanel, le corps du reset à valeurs en dur dont un '192k' recopié) et les refs DOM associées (`outputFmtSel`, `imageOpts`…) | `converter/index.html` + `converter.js` (1007→798 l.) | double source avec `params.py` — cause MESURÉE de la divergence du neutre de rotation (schéma `''`, page `'0'`) ; la jumelle rendait déjà tout du schéma | ✅ remplaçant EN PLACE et mesuré au navigateur : hôte unique `#converterPanelParams` rendu du schéma (type image → 9 formats + qualité, type vidéo → 10 + CRF, dépôt crée son élément, 0 erreur console) ; les 3 affordances non-paramètres GARDÉES (badge type, profil, reset) | ✅ |
| R44 | Champs `options` + `cross_app_options` (JSONField) → renommés `*_legacy`, plus lus ni écrits (propriétés reconstruites des colonnes) | `converter/models.py` | un réglage = une COLONNE depuis le 01/09 ; les JSON ne restent que comme filet le temps de valider en usage réel | ⚠ retrait FINAL = une migration `RemoveField` ×2, à faire après quelques jours d'usage sans écart ; ⚠ la migration 0010 (Rename+recopie ÉCRITE À LA MAIN — makemigrations proposait Remove+Add = perte des données) n'est PAS versionnée (`.gitignore` migrations) : copie de sûreté au scratchpad de session | ⛔ |
| R45 | bloc **LEGACY PATHS** de `settings.py` (`MODELS_ROOT = AI-models/anonymizer/models--ultralytics--yolo`, `LEGACY_MODEL_PATHS` : `anonymizer/`, `enhancer/onnx`, `imager/wan`, `imager/hunyuan`, `synthesizer/tts`, `synthesizer/bark`) | `wama/settings.py` (~L140-157) | audit des emplacements du 02/09 : AUCUN de ces dossiers n'existe sur disque (`AI-models/` = `cache`, `models`, `sources`), le réel suit `model_locations` (catégorie = ModelType). Seul consommateur : le REPLI de l'anonymizer (`yolo_utils.MODELS_ROOT` via `model_manager.get_yolo_root()`, qui tombe d'abord sur le bon dossier) | ⚠ retrait = vérifier que `get_yolo_root()` ne dépend plus de `MODELS_ROOT` puis supprimer le bloc ET l'import de `yolo_utils.py:9` — candidat, PAS retiré (session bancs, hors périmètre) | ⏳ |
| R43 | option de voix **`custom`** (`'Voix personnalisée (clonage)'`) | `common/tts/constants.py::VOICE_PRESET_CHOICES` + ses 2 gardes (`voice_utils.resolve_voice_preset`, `tts_service._get_speaker_wav`) | sentinelle héritée d'AVANT les voix réelles (`ua_<id>`, `cv_<id>`) : elle ne désignait AUCUN fichier — `resolve_voice_preset('custom')` rendait `None`, donc le moteur parlait avec sa voix par DÉFAUT pendant que la card annonçait le clonage (constat Fabien sur les cards 307/309/310) | ✅ mesuré avant retrait : **3 travaux sur 3**, aucun avec `voice_reference` — fausse dans 100 % de ses usages ; 3 lignes migrées vers `default` (= le comportement RÉELLEMENT eu lieu, l'audio ne change pas) | ✅ |

**Écartés du retrait (vérifiés vivants)** : `views._compact_preview` (API reader) ·
`import os as _os` local restant de `converter/views.py` (utilisé sur place) · le polling de
`converter.js` (ce qui manque à la jumelle, pas un mort) · dblclick propre de reader/describer
(convergence `wama:card-expand` = chantier, pas suppression).

## Routage des caches HF + sélection VRAM — 2026-09-03 (investigation demandée par Fabien)

| # | Élément | Emplacement | Pourquoi résidu | Prérequis avant suppression | Statut |
|---|---|---|---|---|---|
| R46 | `fits_full_gpu(model_key, total_vram_gb, headroom_gb=4.0)` | `model_manager/services/memory_manager.py:163` | **aucun consommateur** dans tout le dépôt (vérifié `Grep` exhaustif le 03/09 : seule sa définition, plus des citations dans mes propres docs du jour). Introduite le 2026-07-29 par `d564771f`, dont le message dit ce qui a été retenu **à sa place, dans le même commit** : « "pas d'offload" se traduit par un BUDGET (VRAM libre − marge) passé à `select_model` ». Écrite puis contournée. | ⚠⚠ **NE PAS la retirer sans consigner ce qu'elle désigne.** Elle compare à la VRAM **TOTALE** — « le meilleur modèle qui tiendrait **si on déchargeait tout** » — quand le budget adopté compare à la VRAM **LIBRE** (« ce qui tient maintenant »). **Ce ne sont pas les mêmes questions, et la seconde n'est couverte nulle part** pour le CHOIX d'un modèle : le seul autre raisonnement sur la VRAM totale est `get_memory_strategy:542`, qui décide une stratégie d'**offload** pour un modèle DÉJÀ choisi. Trancher d'abord : la câbler (cf. le trou « qualité maximale » ci-dessous) ou l'abandonner en actant le manque. | ⏳ |
| R47 | copie orpheline `models--timm--resnet18.a1_in1k` dans `AI-models/models/vision/table-transformer-detection/` + la ligne de catalogue `huggingface:timm/resnet18.a1_in1k` qu'elle fait naître | disque + `AIModel` | **backbone timm** du DETR Table Transformer, déversé là par la mutation per-modèle de `HF_HUB_CACHE` (retirée ce jour). MESURÉ dans les deux sens : le modèle principal se résout par `cache_dir=` (il est ABSENT du cache partagé et le test passe), le backbone se résout par `HF_HUB_CACHE` (chargement en ÉCHEC quand on pointe la variable sur un dossier vide, `cache_dir=` inchangé). Depuis le retrait, le backbone vient du cache partagé — sa place légitime (`ROADMAP §5b`, convention rappelée par Fabien). | ⚠ retrait = supprimer le dossier PUIS purger la ligne de catalogue (sinon la découverte la recrée au prochain balayage). Sans risque pour le backend depuis le retrait de la mutation (test poids réels 6/6 OK), mais **non fait ici** : c'est un geste disque + catalogue, hors du périmètre d'une correction de routage. | ⏳ |
| R48 | `setup_sam3_hf_environment()` (lecture d'un fichier `token` dans le dossier du modèle, `os.environ['HF_TOKEN']`/`HUGGING_FACE_HUB_TOKEN`, et `HfFolder.save_token`) | `common/backends/sam3_processor.py` + ses 2 appelants (le backend, `cam_analyzer/utils/sam3_road_analyzer.py`) | **DERNIER site mutant un jeton HF**. Le socle a UN SEUL domicile depuis le 02/09 (`.env` → `settings.py`, promu au démarrage) : cette fonction lisait un SECOND exemplaire et l'écrivait en plus dans le `$HOME` de l'utilisateur. Son bloc de CACHE avait déjà été retiré le 06/09 — il ne restait que le jeton, donc la fonction ENTIÈRE était le résidu. ⚠ Personne ne l'avait vu parce que la garde ne comptait que les variables de CACHE et affichait un budget de 0 : *le budget disait vrai sur ce qu'il mesurait, il ne mesurait pas tout.* | ✅ retiré le 2026-09-07 AVEC ses 2 lecteurs, et la garde `tests_hf_cache_routing` couvre désormais le JETON (socle exclu explicitement : c'est LE domicile) | ✅ |
| R49 | `VENDOR_PACKAGE = 'wama.avatarizer'` + `_vendor_dir()` | `common/backends/{musetalk,codeformer}_backend.py` | ils résolvaient un **paquet Python importable** pour localiser un **dossier jamais importé** — la confusion backend/moteur incarnée (pointée par Fabien : « n'es-tu pas en train de confondre les backends et les moteurs ? »). Preuve dans le même fichier, deux lignes plus haut : les POIDS venaient déjà proprement de `settings.MODEL_PATHS`, le MOTEUR non. | ✅ remplacé par une racine DÉCLARÉE (`settings.BACKEND_VENDOR_DIR`), le sous-dossier portant le nom d'`ENGINE` ; le gitlink `codeformer` SANS URL (irréparable sur un clone frais) est retiré au passage | ✅ |
| R50 | avertissement EN DUR « This may take several minutes on first run (downloading ~5-10GB) » | `imager/tasks.py` (branche vidéo) | affiché à **CHAQUE** lancement — donc même quand les poids étaient déjà là — et avec un volume INVENTÉ. *Un avertissement permanent n'avertit plus de rien.* | ✅ remplacé par la brique commune `model_readiness` : elle ne parle que si le catalogue dit `is_downloaded=False`, et n'annonce la taille que si `disk_gb` la connaît. Mesuré : elle se déclenche sur les 3 modèles sans poids, se tait sur les autres | ✅ |
