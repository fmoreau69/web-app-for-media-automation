{
  "body": {
    "app": "",
    "binding": "pure",
    "category": "transform",
    "cost": {
      "cpu_bound": true
    },
    "impl": "",
    "inputs": [
      {
        "cardinality": "one",
        "data_type": "segments",
        "description": "Situations à trier — leurs colonnes (et `duration` dérivée) portent les conditions.",
        "group": "travail",
        "key": "segments",
        "optional": false,
        "produced_fields": [],
        "required_fields": [
          "start",
          "end"
        ]
      }
    ],
    "key": "segment_filter",
    "outputs": [
      {
        "cardinality": "one",
        "data_type": "segments",
        "description": "Les situations retenues, colonnes inchangées.",
        "key": "segments",
        "optional": false,
        "produced_fields": [],
        "required_fields": []
      }
    ],
    "owner": "",
    "params": [
      {
        "choices": null,
        "default": "",
        "description": "Liste JSON de conditions — [{\"key\": \"C1\", \"field\": \"vitesse\", \"operator\": \">=\", \"value\": 30}]. La SORTE de la colonne (numérique / texte / booléen) est LUE dans la donnée, jamais déclarée : un opérateur qui ne lui convient pas est refusé à la déclaration. Opérateurs numériques : <, <=, >, >=, ==, !=, empty, not_empty. Opérateurs texte : ==, !=, contains, not_contains, startswith, not_startswith, endswith, not_endswith, empty, not_empty.",
        "key": "conditions",
        "max": null,
        "min": null,
        "type": "json",
        "unit": ""
      },
      {
        "choices": null,
        "default": "",
        "description": "Assemblage logique en forme préfixe — ET(C1, C2), NON(C1), OU(C1, ET(C2, C3)). ET et OU acceptent 2 arguments ou plus ; XOR en prend exactement 2 ; NON, un seul. Facultatif s'il n'y a qu'une condition. Le texte est une SAISIE : il est converti en arbre et c'est l'arbre qui est conservé, si bien que deux saisies équivalentes se comparent.",
        "key": "connectors",
        "max": null,
        "min": null,
        "type": "str",
        "unit": ""
      }
    ],
    "projects": [],
    "tags": [
      "temporel",
      "conditionnel",
      "ensembliste",
      "segmentation"
    ],
    "visibility": "public"
  },
  "description": "Ne garde que les situations satisfaisant la chaîne — DURÉE comprise : la colonne `duration` (end − start) est disponible à l'évaluation même si le cadre ne la porte pas, sans entrer dans la sortie. Un segment OUVERT a une durée ABSENTE : rejeté par toute condition numérique, sélectionnable par `empty`. « Vitesse moyenne > 30 » se COMPOSE : `calc_per_segment` adjoint l'indicateur, puis ce filtre s'applique — deux fonctions chaînées, zéro concept neuf.",
  "key": "segment_filter",
  "manifest_kind": "function",
  "name": "Filtrer des situations par conditions",
  "owner": null,
  "projects": [],
  "schema_version": "1.0",
  "scope_org_unit": null,
  "scope_project": null,
  "source": {
    "ref": "FUNCTION_CATALOG:segment_filter",
    "type": "extract"
  },
  "visibility": "public",
  "world": "data"
}
