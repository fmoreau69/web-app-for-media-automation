anthropic>=0.40.0
faster-whisper>=1.1.0
pyannote.audio>=3.3.1
djangorestframework>=3.15
celery>=5.5.2
python-dotenv>=1.0.0
deep-translator>=1.11.4
Django>=5.2.1
pgvector>=0.4  # colonne vector + CosineDistance (memoire/RAG, cf. WAMA_MEMORY.md)
django-auth-ldap>=5.2.0
django-celery-beat>=2.8.1
django-celery-results>=2.6.0
django-git-submodules>=1.1.1
django-wysiwyg>=0.8.0
imageio>=2.37.0
imageio-ffmpeg>=0.4.9
#mod-wsgi>=5.0.2
more-itertools>=10.7.0
numpy>=2.3.3
ollama>=0.4.0
litellm>=1.0.0
opencv_python>=4.11.0.86
pillow>=11.2.1
pint>=0.25  # conversion d'unités à l'AFFICHAGE seul (D27, WAMA_DATA_WORLD §10) — la donnée reste dans SON unité
psutil>=5.9.0
setuptools<81  # pkg_resources retiré en setuptools 81+ ; requis par mmengine/mmpose (MuseTalk) — 2026-07-16
python-ldap>=3.4.4
PySide6>=6.9
pytube>=15.0.0
requests>=2.32.3
# bs4+lxml : utilisés depuis longtemps (url_ingest, web_search) mais jamais déclarés —
# dépendance implicite régularisée le 2026-08-29 (déjà présents dans les deux venvs).
beautifulsoup4>=4.12
lxml>=5.0
# Lecteur de doc (common/docs_catalog.py, 2026-09-11) — déjà présent dans les deux venvs comme
# dépendance de `rich` ; déclaré parce que WAMA l'importe désormais directement. `html=False`
# échappe nativement le HTML brut des .md (raison du choix face à Python-Markdown).
markdown-it-py>=4.0
safetensors>=0.4.0
segno>=1.6  # QR codes (pur Python, BSD-3, zéro dépendance) — brique commune common/utils/qr.py : QR d'appariement joint par le bot (ROADMAP §19.1) ; TOTP/Imager à venir
# torch/torchvision/torchaudio : voir requirements_torch.txt, à installer EN PREMIER.
# (L'ancienne ligne `torch>=2.8.0+cu128` ne pouvait pas marcher — PyPI n'a aucune roue avec
#  suffixe local, donc `>=` retombait toujours sur la version CPU. D'où sa mise en commentaire.)
tqdm>=4.67.1
# transformers : la borne HAUTE est structurante, pas cosmétique. Elle existait depuis le
# 2026-08-04 dans requirements_linux.txt et MANQUAIT ici — or c'est CE fichier qui gouverne
# venv_win, qui a donc dérivé jusqu'en 5.12.1 (mesuré 2026-08-22). Une majeure 5.x casse
# boson-multimodal/Higgs (<4.47), vibevoice (==4.51.3), inference-exp/-models (<5.0.0), et
# périme le patch n°1 de patches/apply_patches.py, écrit pour l'API 4.57.
transformers>=4.57,<5
ultralytics>=8.3.203
watchdog>=3.0.0
yt-dlp>=2025.6.9

# Converter — document backend (Pandoc binding)
pypandoc==1.13
pdf2docx==0.5.13  # PDF → DOCX en préservant images/tableaux/mise en forme (converter)
weasyprint==69.0  # HTML → PDF fidèle (CSS + SVG inline) sans LaTeX/rsvg-convert (converter)
playwright==1.61.0  # HTML → PDF via Chromium headless (CSS moderne + JS + responsive) ; route préférée, fallback weasyprint. Provisioning navigateur : voir start_wama_prod.sh (playwright install chromium)

# Passerelle de canaux conversationnels (ROADMAP §19) — adaptateurs de protocole.
# Optionnelles au sens strict : sans elles WAMA démarre et tourne normalement, seule la
# commande `manage.py run_gateway` refuse de se lancer (import tardif, message explicite).
discord.py>=2.4  # adaptateur Discord (1er livré : ni compte mail, ni E2EE, ni renouvellement)
# matrix-nio[e2e]  # adaptateur Tchap/Matrix — à décommenter quand l'adaptateur sera écrit
#                    (E2EE obligatoire : les salons Tchap sont chiffrés)
