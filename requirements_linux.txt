celery
deep-translator
Django
pgvector
django_auth_ldap
django-git-submodules
django-wysiwyg
imageio
imageio-ffmpeg
#mod-wsgi
more-itertools
numpy
ollama
opencv_python
pillow
psutil
bitsandbytes
# ⚠ DIVERGENCE CONNUE, non corrigée (mesurée le 2026-09-05) : `pip install -r` sur ce fichier
# annonce `Would install nvidia-nccl-cu12-2.27.5`, un RÉTROGRADAGE depuis la 2.30.4 installée.
# Chaîne : bitsandbytes → torch<3,>=2.2 → torch 2.9.1+cu128 épingle `nvidia-nccl-cu12==2.27.5`.
# C'est pip qui veut SATISFAIRE le pin de torch : nous avons une nccl PLUS RÉCENTE que celle
# qu'il déclare. Ne pas « corriger » à l'aveugle — rétrograder une brique CUDA d'un venv qui
# tourne se paierait au premier chargement de modèle, alors que rien n'échoue aujourd'hui.
setuptools<81  # pkg_resources retiré en setuptools 81+ ; requis par mmengine/mmpose (MuseTalk) — 2026-07-16
resampy>=0.4.3
python_ldap
PySide6
pytube
requests
# bs4+lxml : utilisés depuis longtemps (url_ingest, web_search) mais jamais déclarés —
# dépendance implicite régularisée le 2026-08-29 (déjà présents dans les deux venvs).
beautifulsoup4
lxml
# torch/torchvision/torchaudio : voir requirements_torch.txt, à installer EN PREMIER
# (ce fichier doit passer APRÈS pour que le pin `setuptools<81` ci-dessus reprenne la main).
tqdm
transformers>=4.57,<5  # oubli comblé 2026-08-04 — importé par musetalk/describer/olmocr/synthesizer/qwen-ASR.
                       # Borne HAUTE volontaire : les 9 patches boson_multimodal de
                       # patches/apply_patches.py sont écrits pour l'API 4.57+ (cf. AGENTS.md).
                       # ⚠ venv_win porte 5.12.1 — divergence CONNUE, non corrigée ici.
ultralytics
yt-dlp

# Converter — document backend (Pandoc binding)
pypandoc==1.13
weasyprint==69.0  # HTML → PDF fidèle (CSS + SVG inline) sans LaTeX/rsvg-convert (converter)
playwright==1.61.0  # HTML → PDF via Chromium headless (CSS moderne + JS + responsive) ; route préférée, fallback weasyprint. Provisioning navigateur : voir start_wama_prod.sh (playwright install chromium)

# ──────────────────────────────────────────────────────────────────────────────────────────
# WAMA Lab — face_analyzer (FER / DeepFace / MediaPipe + TensorFlow)
#
# ⚠ DÉLIBÉRÉMENT PAS INCLUS PAR `-r`, et c'est une MESURE. Le fichier de l'app
# (`wama_lab/face_analyzer/requirements/linux.txt`) est aujourd'hui INOFFENSIF sauf sur UNE
# ligne — `fer`, qui doit s'installer en `--no-deps`. Chaîne mesurée le 2026-09-05 :
# `fer` → `facenet-pytorch` → `torchvision`. Nos torch/torchvision viennent de
# `download.pytorch.org` en versions LOCALES (`+cu128`), absentes de PyPI : dès qu'un paquet
# oblige pip à re-résoudre `torchvision`, le résolveur retombe sur `0.17.2`, qui épingle
# `torch==2.2.2` — et rétrograde torch 2.9.1, numpy 2.3.5 et toute la pile CUDA.
#
# Installation de l'app (deux gestes, l'ordre compte) :
#     pip install --no-deps 'fer>=25.10.3'
#     pip install -r wama_lab/face_analyzer/requirements/linux.txt
#
# Un `-r` ici rendrait l'app installable d'un seul geste — et ce geste casserait le venv.
# Le portage de l'app formalisera la contrainte en `PIP_NO_DEPS = True` sur son futur backend.
#
# (Corrigé à la source ce jour : `tf-keras` non borné retenait la 2.20.1, qui exige
#  `tensorflow<2.21` et rétrogradait TF. `tf-keras>=2.21` lève le conflit sans rien toucher.)
# ──────────────────────────────────────────────────────────────────────────────────────────
