# REPRISE — 2026-08-06 · instance CAM_ANALYZER / VOLET DROIT

> **ÉTAT AU 2026-09-07 (relu contre le code, question Fabien)** — ce handoff et l'artefact
> « Réagencement du volet droit » (05/08, archivé `wama_lab/cam_analyzer/docs/volet_droit_proposition_2026-08-06.html`)
> sont une **v1 partiellement livrée** : **Q1** (`indicators` = passe) ✅ `dcf595b` · **Q2** (étages
> 📷 Analyse / 🧮 Calculs) ✅ · **Q3** (⚑ Modes dé-flotté, scindé par `scope`) ✅ Palier A 06/08 ·
> **Q4** (lecture chiffrée des indicateurs) ⏳ · **3b** (toggles « Vue » hors registre ⚑) ⏳ ·
> **le foyer UNIQUE de lancement (② ANALYSER)** ⏳ — les 3 boutons de tête existent toujours, dont
> un doublon strict et un effacement sans confirmation. L'état vivant et la proposition à jour
> (▶ par étage, gating par `_DEPENDS_ON`, registre unique des passes → manifeste `pipeline`)
> vivent dans **`CAM_ANALYZER_CHANGELOG § ÉTAT 2026-09-05/07`**, lignes « boutons de lancement »
> et « pipeline schéma-driven ». La clé de session Playwright citée §4 est **périmée** (12/08) ;
> la recette valide est celle du skill `/smoke` (compte `wama_nightly_test`, `_test_session_key`).

> Chantier : **réorganisation cohérente du volet droit** de cam_analyzer (barre d'outils +
> pipeline). Demande de Fabien : « exploration complète de tout ce qu'il y a dans le volet droit
> + agencement/design cohérent, meilleure UI/UX, simple à utiliser ».
> **Le chantier N'EST PAS terminé.** Q1+Q2 faits et commités (`dcf595b`) ; Q3+Q4 à valider par
> Fabien AVANT de coder ; une **3ᵉ incohérence** (section Vue ⇄ menu Comparer) découverte en fin de
> session, à intégrer au design d'ensemble.
> Partition multi-instances : l'instance **catalogue/taxonomie** touche `common/manifests/**`,
> l'imager et le registre modèles — **NE PAS toucher** `wama/imager/*` ni `common/manifests/**`.

---

## 1. Fait cette session — Q1 + Q2 (commit `dcf595b`, validé Fabien, smoke réel)

**Q1 — « Indicateurs » promu en passe pipeline `indicators`.** Le calcul TTC/PET + tracks 360°
était un bouton isolé (`#predictionCalcBtn`) dans l'encart « Analyse » de la map-card, HORS
pipeline = incohérent. Désormais :
- `PassType.INDICATORS` (session-wide, après `global_tracking`, avant `conflicts`) ;
- tâche `annotate_prediction_task` → **`compute_indicators_task`** (famille `compute_*_task`),
  câblée `pass_tracking` (`mark_started/completed/failed`) + `views.dispatch_map['indicators']` ;
- CPU pur, re-jouable, **sûr sous WSL2** ; l'action passe par le ▶ du pipeline (bouton retiré du volet) ;
- `prediction_annotate` (vue legacy) + `function_specs.py` pointent la nouvelle tâche.

**Q2 — pipeline scindé visuellement 📷 Analyse / 🧮 Calculs.** Champ `stage` (`analyse`|`calcul`)
posé par `pass_tracking._STAGE` sur chaque passe ; `index.js` groupe le rendu sous deux
sous-en-têtes (helper `_rowHtml`, markup de ligne inchangé).

**Validation** : py_compile · check_js.sh 51/0 · **migration 0024** + migrate (base live WSL2) ·
check 0 issue · **smoke Playwright RÉEL** (`vd_after.png`) : pipeline scindé correct, `#predictionCalcBtn`
absent, groupe map-card = « Comparer / Live », 0 erreur console imputable. ⚠ tâche `compute_indicators_task`
CPU-safe **non lancée en réel** (via ▶ pipeline). Détail complet : CHANGELOG entrée du haut, `2026-08-05`.

---

## 2. Inventaire RÉEL du volet droit (barre d'outils `base.html` lignes 30-95)

Trois groupes `td-group` dans la barre d'outils :

| Groupe (label actuel) | Boutons / contrôles | Nature |
|---|---|---|
| **Configurer** | `Ouvrir` (calibration, dropdown) | config |
| **Vue** | `topDown360Btn` 360° · `predictionBtn` Prédiction · `hideParkedBtn` Garés · `laneVideoBtn` Voie/vidéo · slider `overlayConfSlider` (Conf ≥) · slider `laneWidthSlider` (Voie) | **bascules d'affichage codées EN DUR** |
| **Comparer / Live** | `featModesBtn` Comparer (→ ouvre `#featModesPanel`) · `liveAnalysisBtn` Live · `copyDebugBtn` | ⚑ registre + temps réel |

`#featModesPanel` = le menu déroulant, **14 flags** du registre `utils/features.py`, `position:absolute`
au-dessus de la mini-carte. Répartition par `scope` :
- **`scope='live'` (6, effet AFFICHAGE instantané, zéro recalcul)** : `fov_dist_correction`,
  `mount_lever_arm`, `heading_ratio`, `artifact_filter`, `anchor_heading`, `sam3_interp` ;
- **`scope='compute'` (8, nécessitent de RELANCER le calcul des indicateurs)** : `antenna_lever`,
  `auto_ground_calib`, `ortho_correction`, `world_markings`, `learned_branches`, `heading_cluster`,
  `track_speed_unified`, `depth_estimation`.

---

## 3. Les incohérences RESTANTES — à trancher AVEC Fabien avant de coder

### 3a. (Q3) Le menu ⚑ Comparer mélange deux natures + flotte sur la carte pivotée
Le panneau liste les 14 flags **sans séparer** les `live` (bascule d'affichage, immédiate) des
`compute` (changent le RÉSULTAT, exigent un re-calcul des indicateurs). Le tooltip évoque un `⟳`
mais rien ne matérialise la coupure. De plus il est `position:absolute` **au-dessus de la mini-carte
pivotée** (le skill interdit tout pane Leaflet custom sur cette carte — ici c'est un panneau HTML, pas
un pane, mais la fragilité de superposition reste). **Proposition à valider** : dé-flotter le panneau
(le loger sous « Configurer » ou en volet dédié) ET le scinder visuellement par `scope`
(« Affichage / immédiat » vs « Calcul / relancer »). Le champ `scope` existe DÉJÀ dans `features.py` —
la scission est une lecture de métadonnée, pas une nouvelle logique (principe « UI DEPUIS les capacités »).

### 3b. (NOUVEAU, soulevé par Fabien en fin de session) Deux systèmes de bascules parallèles
La section **Vue** (360°, Prédiction, Garés, Voie/vidéo) est un **second système de toggles codé en
dur, HORS du registre ⚑**. Le skill impose « toute amélioration comparable = un flag ⚑, jamais un
`if` ad hoc » — or ces boutons sont des bascules d'affichage qui échappent au registre. L'utilisateur
a donc des bascules **à deux endroits** avec deux modèles mentaux. Pire, `predictionBtn` « Prédiction »
(affichage des fantômes/TTC prédits) **entre en collision de nom** avec la nouvelle passe `indicators`
(qui CALCULE les TTC/PET). **Question de design pour Fabien** : faut-il
1. faire remonter les toggles « Vue » dans le registre avec un `scope='display'` (et générer la
   section Vue DEPUIS le registre, comme le ⚑ panel), unifiant les deux systèmes ; OU
2. garder deux zones mais avec une frontière claire : **Vue = ce que je VOIS** (affichage pur,
   instantané) / **Comparer = comment c'est CALCULÉ** (recalcul) — et y ranger chaque flag `live`
   côté Vue, chaque `compute` côté Comparer selon son `scope` ?

L'option 1 est la plus fidèle à la philosophie WAMA (métadonnée-driven, un seul registre). À trancher.

### 3c. (Q4) Pas de lecture chiffrée des indicateurs
La passe `indicators` produit des TTC/PET mais **aucun encart du volet ne les affiche en clair**
(on ne voit que la coloration sur la carte via `predictionBtn`). **Proposition à valider** : un petit
encart numérique (TTC/PET min, nb d'insertions/conflits) — emplacement proposé sous « Exports ».
À valider : QUELS chiffres comptent pour Fabien avant de choisir la forme.

---

## 4. Recette de travail (reproduire l'exploration Playwright)

Playwright est **installé et la méthode établie** (ne PAS prétendre l'inverse). Scripts dans le
scratchpad de session : `mk_session.py`, `pw_verify.py`, `pw_voletdroit.py`, `pw_tall.py`.
- **Auth** : frapper une session Django via **venv_win** (écrit dans la Postgres **live WSL2**),
  injecter le cookie **`wama_sessionid`** (PAS `sessionid` — `SESSION_COOKIE_NAME`, settings.py:452)
  dans le contexte Playwright. Clé de session `x820n3yqo3lno4fc4qmu8i7henxqfmt5` **valide jusqu'au
  2026-08-12** ; session cam de test `fdad2686-8a37-4aae-83f4-1508e91cd07d` (ENA_CASA 4 cam, complétée).
- **Sélection de session** : `localStorage['camAnalyzer:activeSession:1'] = '<uuid>'` AVANT init,
  puis re-`goto` (domcontentloaded → networkidle).
- **Capture pleine hauteur** : viewport haut (1600×2560) puis `element.screenshot` sur
  `.right-panel-content` (sinon clip à la zone visible).
- Navigateurs sous WSL2 (`~/.cache/ms-playwright`), lancés par le python venv_linux.
- **Ouvrir le panneau calibration** (`Ouvrir`) fait partie de l'exploration demandée — non re-capturé
  cette session, à faire au prochain passage.

Proposition UI (v1, à actualiser après Q3/Q4/3b) — archivée dans le dépôt le 2026-08-21 :
[wama_lab/cam_analyzer/docs/volet_droit_proposition_2026-08-06.html](wama_lab/cam_analyzer/docs/volet_droit_proposition_2026-08-06.html)

---

## 5. Contraintes NON négociables (héritées)

- **Aucune charge GPU sous WSL2** sur ce poste (crashs hôte, bug MS WSL #40732). Vaut pour
  `manage.py shell` (`django.setup()` importe `torch.cuda` → mort silencieuse) → interroger la base
  en **psql direct** ou via **venv_win** côté Windows. Tests GPU = côté runtime/R760xa (Fabien).
- **Git** : TOUJOURS `git commit <chemins explicites> -m`, JAMAIS `git add .`/`-A` ni commit sans
  pathspec (index partagé entre instances). `git status` avant. Commit local au palier ; **push =
  demander**. **Ne PAS commiter** `wama/imager/*` (autre instance) ni `common/manifests/**`.
- **Skill cam-analyzer** : toute bascule comparable = flag ⚑ (`features.py`), jamais `if` ad hoc ;
  A/B objectif chiffré en console (jamais visuel seul) ; **CHANGELOG obligatoire dans le MÊME commit** ;
  défaut OFF pour bascule non validée ; si smoke GPU/navigateur non lancé, l'écrire au CHANGELOG.
- **JS > 5000 lignes** : édition ciblée, jamais regex/script, puis `bash scripts/check_js.sh`.
- JS/CSS modifié → copier vers `staticfiles/cam_analyzer/` ; template modifié → **HUP gunicorn**.
- Migrations **gitignorées** dans ce projet. Commentaire Django multi-ligne = `{% comment %}`, jamais
  `{# #}` (piège récidivant).
- **user id=1 = compte réel de Fabien** — aucun test destructif.
- Une sortie/traitement non déclaré dans `function_specs.py` est **invisible** du catalogue/Studio.

---

## 6. Prochaine session — ordre recommandé

1. `/reprise` puis lire ce fichier + le dernier CHANGELOG cam_analyzer.
2. **Re-fumer l'exploration Playwright** (§4), **y compris ouvrir le panneau calibration** (non fait).
3. **Présenter à Fabien les 3 arbitrages** (3a Q3 / 3b nouveau / 3c Q4) — il a dit ne pas bien
   comprendre les points différés : les lui **expliquer avec captures** avant de coder.
4. Coder seulement après validation, palier par palier, chaque bascule = flag ⚑ + métrique chiffrée.
